# 朋友圈（Moments）实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 交付好友可见的图文/视频动态闭环（发布 / 信息流 / 点赞 / 评论 / 互动红点 / 删除 / 内容安全打标）三端可用，并顺带补上个人状态（K11）与导航结构调整。

**Architecture:** 后端沿用既有 repository → service → handler 三层，**全部可见性判定收敛在 `MomentsRepository.visiblePostsScope` 一个作用域方法**，所有读写路径必须经过它；媒体不新建对象归属表，沿用「按现有数据反查」的对象 ACL 模型，给 `ObjectACLRepository` 加 moments 分支。前端新建独立 `MomentsScreen`（不塞进已 1000+ 行的 `ChatScreen`），走 shared 的 api/store/WS 帧分派三段式，媒体上传复用既有预签名直传与 canvas 抽帧。

**Tech Stack:** Go 1.25 + Gin + GORM + PostgreSQL(jsonb/GIN) + goose 迁移 + gorilla/websocket；React 18 + TypeScript + zustand + react-i18next + Tailwind + MSW + Vitest + Playwright；Tauri（桌面/Android）。

**Spec:** `docs/superpowers/specs/2026-09-13-moments-design.md`（本计划的全部决策依据；冲突以 spec 为准）

## Global Constraints

每个任务的验收都隐含以下全仓约束，逐条摘自仓库规范与 spec：

- **分支纪律**：全程在 `feature/moments` 上开发，禁止直接提交 dev/main；最终 `--no-ff` 合 dev，禁止 squash。
- **Commit message**：禁止写版本号前缀（不得出现 `feat(v0.5/G1):` 这类）；用 `feat(moments): ...` / `fix(moments): ...`。
- **Commit 纪律**：完成一个完整可测的交付面再提交，禁止逐点碎提交。
- **注释语言**：注释一律中文，禁止写进度/批次信息（不得出现「第二批」「TODO 下批做」）。
- **文档覆盖**：所有导出符号必须有 JSDoc（TS）/ godoc（Go）。
- **i18n**：UI 文案零硬编码，全部走 `t("...")`；新增 key 必须四语言（zh-CN / en-US / ja-JP / ko-KR）同步补齐，`node scripts/check-i18n.mjs` 必须绿（它同时校验死键与代码→locale 双向对账）。
- **浏览器兼容**：`build.target` 保持 `es2019`，产物不得出现 `?.` / `??` 未转译语法（旧 WebView Chrome 74 会白屏）。
- **圆角**：类名上限 `rounded-lg`（本仓 lg = 16px），禁止 `rounded-xl` / `rounded-2xl`（`rounded-full` 圆形除外）。
- **根字号**：html 根字号 14px，Tailwind rem 刻度缩水 14/16；要精确 px 用方括号任意值（如 `h-[38px]`）。
- **骨架屏**：加载态用固定宽高骨架，CLS 必须为 0。
- **MSW**：前端新接口必须补 mock，覆盖正常/空/错误/加载四态。
- **启动命令**：一律走 `package.json` scripts（`pnpm dev:*` / `pnpm build:pkg`），禁止手敲底层命令。
- **Go 路径**：模块缓存路径用 `go env GOMODCACHE` 取，禁止写死 `~/go`（本机 GOPATH 在 `~/env/go/GOPATH`）。
- **覆盖率门禁**：本机 Go 缺 `covdata`，`go test -coverprofile` 的**退出码不可当失败信号**；判断测试是否真通过用不带 `-coverprofile` 的 `go test ./...` 数 `^ok` / `^FAIL`。
- **推 dev 前**：必须本地跑通 CI 全量（见 Task 16）。
- **MASTER_PLAN**：`docs/MASTER_PLAN.md` 是实施计划唯一依据，本批的进度/范围变动当次回写（Task 16）。

---

## File Structure

### 后端新增

| 文件                                                     | 职责                                                                               |
| -------------------------------------------------------- | ---------------------------------------------------------------------------------- |
| `server/internal/database/migrations/018_g1_moments.sql` | 四张 moments 表 + GIN 索引 + users 三列                                            |
| `server/internal/model/moments.go`                       | `MomentPost` / `MomentLike` / `MomentComment` / `MomentActivity` + 媒体结构 + 枚举 |
| `server/internal/repository/moments_repo.go`             | 可见性作用域 + 帖子/点赞/评论/互动全部数据访问                                     |
| `server/internal/service/moments_service.go`             | 发布校验、敏感词打标、点赞幂等、评论、activity 写入与 WS 推送、DTO 组装            |
| `server/internal/handler/moments.go`                     | REST 端点 + 请求结构 + 错误映射                                                    |
| `server/internal/handler/admin_moments.go`               | admin 删帖/删评论 + 审计日志                                                       |

### 后端修改

| 文件                                              | 改动                                                                   |
| ------------------------------------------------- | ---------------------------------------------------------------------- |
| `server/internal/repository/object_acl_repo.go`   | `CanRead` 加 moments 分支；`ReferencedKeys` 加 moments 分支            |
| `server/internal/ws/protocol.go`                  | `TypeMomentsActivity` 常量 + `MomentsActivityPayload`                  |
| `server/internal/ws/golden_server_frames_test.go` | `payloadPrototypes` 登记新帧 + decode 分支                             |
| `contracts/server-frames.golden.json`             | 新增 `moments.activity` 用例                                           |
| `server/internal/router/router.go`                | moments 路由组 + admin 路由 + 依赖装配                                 |
| `server/internal/model/user.go`                   | `StatusEmoji` / `StatusText` / `StatusExpiresAt` + `EffectiveStatus()` |
| `server/internal/model/admin_action_log.go`       | 两个 moments 删除动作常量                                              |
| `server/internal/service/user_service.go`         | `UpdateProfile` 改 patch 结构体 + 状态字段                             |
| `server/internal/handler/user.go`                 | `UpdateProfileRequest` 加状态字段；公开资料吐有效状态                  |
| `server/internal/ws/presence_integration_test.go` | 改用 `testutil.NewRedis(t)`，去 skip                                   |

### 前端新增

| 文件                                          | 职责                                    |
| --------------------------------------------- | --------------------------------------- |
| `packages/shared/src/api/moments.ts`          | REST 封装 + DTO↔前端类型映射            |
| `packages/shared/src/store/momentsStore.ts`   | feed/游标/未读互动/乐观点赞             |
| `packages/ui/src/MomentMediaGrid.tsx`         | 1/2/3/4/9 图布局 + 视频单卡             |
| `packages/ui/src/MomentPostCard.tsx`          | 单帖卡片                                |
| `packages/ui/src/MomentsScreen.tsx`           | 三端编排：feed + 顶栏（发布 / 铃铛）    |
| `packages/ui/src/MomentComposeView.tsx`       | 发布页                                  |
| `packages/ui/src/MomentActivitiesView.tsx`    | 互动消息页                              |
| `packages/ui/src/UserStatusEditor.tsx`        | K11 状态编辑                            |
| `packages/ui/src/useBackTo.ts`                | `useStickerBack` 改名后的通用返回键拦截 |
| `apps/web/src/pages/Moments*.tsx`（4 个）     | web 路由页壳                            |
| `apps/desktop/src/pages/Moments*.tsx`（4 个） | 桌面路由页壳                            |

### 前端修改

| 文件                                             | 改动                                               |
| ------------------------------------------------ | -------------------------------------------------- |
| `packages/shared/src/ws/chatSocket.ts`           | `ServerFrames` 加 `moments.activity`               |
| `packages/shared/src/hooks/useChatBootstrap.ts`  | 注册 `moments.activity` handler                    |
| `packages/shared/src/store/resetStores.ts`       | 登出清 momentsStore                                |
| `packages/shared/src/mocks/handlers.ts`          | moments 全端点 mock                                |
| `packages/shared/src/api/users.ts`               | `PublicProfile` / `ProfilePatch` 加状态字段        |
| `packages/ui/src/MainLayout.tsx`                 | 底栏收藏→朋友圈；设置沉底；侧栏去收藏；朋友圈红点  |
| `packages/ui/src/SettingsScreen.tsx`             | 行模型统一（view/route 两类）+ 收藏行 + 桌面商城行 |
| `packages/ui/src/Avatar.tsx`                     | `statusEmoji` 可选 prop                            |
| `packages/ui/src/useStickerBack.ts`              | 删除（内容迁到 `useBackTo.ts`）                    |
| `packages/ui/src/index.ts`                       | 导出新组件                                         |
| `apps/{web,desktop}/src/App.tsx`                 | moments 四条路由                                   |
| `packages/design-system/src/i18n/locales/*.json` | `moments.*` / `settings.*` 新 key（四语言）        |

---

## 任务批次（派发用）

按仓库既定纪律「SDD 派发按完整功能/层合批，禁止逐 task 派 agent」，16 个任务分三批：

| 批次 | 任务    | 说明                                                   |
| ---- | ------- | ------------------------------------------------------ |
| A    | 1 – 8   | 后端 G1 全层（含 ACL/GC、admin、K11 后端），一个 agent |
| B    | 9 – 14  | 前端 G2 全层（含导航调整、K11 前端、i18n），一个 agent |
| C    | 15 – 16 | 测试收尾与合库，主会话自己做（涉真机与合并决策）       |

A 与 B 文件树不重叠（后端 `server/` vs 前端 `packages/` + `apps/`），但 B 依赖 A 的 DTO 字段名与 WS 帧形状 —— **A 先行，B 在 A 交付后派发**；若要并行，B 必须严格按本计划里写死的 DTO/帧字段名编码，不得自行改名。

---

## 批次 A — 后端 G1

### Task 1: 迁移与模型

**Files:**

- Create: `server/internal/database/migrations/018_g1_moments.sql`
- Create: `server/internal/model/moments.go`
- Modify: `server/internal/model/user.go`（加三列 + `EffectiveStatus()`）
- Modify: `server/internal/model/admin_action_log.go`（加两个动作常量）

**Interfaces:**

- Produces:
  - `model.MomentPost` / `model.MomentLike` / `model.MomentComment` / `model.MomentActivity`
  - `model.MomentMediaItem{Key, ThumbKey, Duration, W, H}`
  - 枚举：`MomentMediaKindNone/Image/Video`、`MomentVisibilityFriends/Private`、`MomentActivityKindLike/Comment`
  - `func (u *User) EffectiveStatus(now time.Time) (emoji, text string)`
  - `model.AdminActionDeleteMomentPost` / `model.AdminActionDeleteMomentComment`

- [ ] **Step 1: 写迁移文件**

`server/internal/database/migrations/018_g1_moments.sql`：

```sql
-- +goose Up
-- +goose StatementBegin
-- 朋友圈四表。可见性判定不落表，按 contacts/blocklists 现有数据实时推导
--（见 repository.MomentsRepository.visiblePostsScope），避免好友关系变更后
-- 需要回填历史帖子的可见性快照。
CREATE TABLE IF NOT EXISTS moments_posts (
    id          UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    user_id     UUID NOT NULL REFERENCES users (id) ON DELETE CASCADE,
    content     TEXT NOT NULL DEFAULT '',
    -- media 为数组：图片 1-9 项 {key,w,h}；视频恰 1 项 {key,thumb_key,duration,w,h}。
    -- 图与视频互斥，故用单列 + media_kind 判别，不开两列（两列会出现
    -- 「都空 / 都不空」的非法组合，靠应用层约束不住）。
    media       JSONB NOT NULL DEFAULT '[]',
    media_kind  SMALLINT NOT NULL DEFAULT 0,
    visibility  SMALLINT NOT NULL DEFAULT 0,
    flagged     BOOLEAN NOT NULL DEFAULT FALSE,
    created_at  TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    deleted_at  TIMESTAMPTZ,
    CONSTRAINT moments_posts_not_empty CHECK (content <> '' OR media_kind <> 0)
);

-- feed 与个人主页都是「按作者集合 + 时间倒序」，作者列在前；
-- id 进索引尾部是因为游标用 (created_at, id) 复合比较（同毫秒多帖不漏不重）。
CREATE INDEX IF NOT EXISTS idx_moments_posts_feed
    ON moments_posts (user_id, created_at DESC, id DESC) WHERE deleted_at IS NULL;
CREATE INDEX IF NOT EXISTS idx_moments_posts_flagged
    ON moments_posts (created_at DESC) WHERE flagged = TRUE AND deleted_at IS NULL;

-- 对象 ACL 与 GC 都要按 key 反查媒体归属（object_acl_repo.go）。
-- jsonb_path_ops 只支持 @> 一类包含运算，不支持存在性 ? 运算符；
-- 本处只用 @>，故选它（索引比 jsonb_ops 小）。
CREATE INDEX IF NOT EXISTS idx_moments_media_gin
    ON moments_posts USING gin (media jsonb_path_ops);

CREATE TABLE IF NOT EXISTS moments_likes (
    post_id    UUID NOT NULL REFERENCES moments_posts (id) ON DELETE CASCADE,
    user_id    UUID NOT NULL REFERENCES users (id) ON DELETE CASCADE,
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    PRIMARY KEY (post_id, user_id)
);
CREATE INDEX IF NOT EXISTS idx_moments_likes_post ON moments_likes (post_id, created_at);

CREATE TABLE IF NOT EXISTS moments_comments (
    id                UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    post_id           UUID NOT NULL REFERENCES moments_posts (id) ON DELETE CASCADE,
    user_id           UUID NOT NULL REFERENCES users (id) ON DELETE CASCADE,
    reply_to_user_id  UUID REFERENCES users (id) ON DELETE SET NULL,
    content           TEXT NOT NULL,
    flagged           BOOLEAN NOT NULL DEFAULT FALSE,
    created_at        TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    deleted_at        TIMESTAMPTZ,
    CONSTRAINT moments_comments_not_empty CHECK (content <> '')
);
CREATE INDEX IF NOT EXISTS idx_moments_comments_post
    ON moments_comments (post_id, created_at) WHERE deleted_at IS NULL;

-- 互动消息（红点数据源）。user_id 是被通知人，actor_id 是操作者；
-- 自赞自评不写行，否则自己的操作会把自己的红点点亮。
CREATE TABLE IF NOT EXISTS moments_activities (
    id         UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    user_id    UUID NOT NULL REFERENCES users (id) ON DELETE CASCADE,
    actor_id   UUID NOT NULL REFERENCES users (id) ON DELETE CASCADE,
    post_id    UUID NOT NULL REFERENCES moments_posts (id) ON DELETE CASCADE,
    comment_id UUID REFERENCES moments_comments (id) ON DELETE CASCADE,
    kind       SMALLINT NOT NULL,
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    read_at    TIMESTAMPTZ
);
CREATE INDEX IF NOT EXISTS idx_moments_act_user
    ON moments_activities (user_id, created_at DESC);
CREATE INDEX IF NOT EXISTS idx_moments_act_unread
    ON moments_activities (user_id) WHERE read_at IS NULL;

-- 个人状态（K11）。过期只在读时判定（EffectiveStatus），不开定时清理任务。
ALTER TABLE users ADD COLUMN IF NOT EXISTS status_emoji VARCHAR(16) NOT NULL DEFAULT '';
ALTER TABLE users ADD COLUMN IF NOT EXISTS status_text VARCHAR(64) NOT NULL DEFAULT '';
ALTER TABLE users ADD COLUMN IF NOT EXISTS status_expires_at TIMESTAMPTZ;
-- +goose StatementEnd

-- +goose Down
-- +goose StatementBegin
ALTER TABLE users DROP COLUMN IF EXISTS status_expires_at;
ALTER TABLE users DROP COLUMN IF EXISTS status_text;
ALTER TABLE users DROP COLUMN IF EXISTS status_emoji;
DROP TABLE IF EXISTS moments_activities;
DROP TABLE IF EXISTS moments_comments;
DROP TABLE IF EXISTS moments_likes;
DROP TABLE IF EXISTS moments_posts;
-- +goose StatementEnd
```

- [ ] **Step 2: 写模型文件**

`server/internal/model/moments.go`：

```go
package model

import (
	"time"

	"github.com/google/uuid"
)

// MomentMediaKind 帖子媒体类型判别：图与视频互斥，故用单列 media + 本判别位。
const (
	MomentMediaKindNone  int16 = 0
	MomentMediaKindImage int16 = 1
	MomentMediaKindVideo int16 = 2
)

// MomentVisibility 帖子可见性。v1 只有两档，分组可见性（标签/部分好友）未做。
const (
	MomentVisibilityFriends int16 = 0 // 所有好友可见
	MomentVisibilityPrivate int16 = 1 // 仅自己可见
)

// MomentActivityKind 互动消息类型。
const (
	MomentActivityKindLike    int16 = 1
	MomentActivityKindComment int16 = 2
)

// 帖子内容与媒体的应用层上限。
const (
	MomentContentMaxLen = 1000
	MomentCommentMaxLen = 500
	MomentMaxImages     = 9
)

// MomentMediaItem 落库 media jsonb 数组里的单项。
//
// 字段名与消息 content 的媒体字段逐字一致（key / thumb_key），
// 使对象 ACL 与 GC 的按 key 反查能用同一套表达式（见 ObjectACLRepository）。
// 图片项只填 Key/W/H；视频项额外填 ThumbKey/Duration。
type MomentMediaItem struct {
	Key      string  `json:"key"`
	ThumbKey string  `json:"thumb_key,omitempty"`
	Duration float64 `json:"duration,omitempty"`
	W        int     `json:"w"`
	H        int     `json:"h"`
}

// MomentPost 一条朋友圈动态。
//
// Media 存 JSON 原文而非结构化切片，与 Message.Content 同一惯例：
// 落 jsonb 列，读写时由 service 层 Marshal/Unmarshal。
type MomentPost struct {
	ID        uuid.UUID  `gorm:"type:uuid;primaryKey;default:gen_random_uuid()" json:"id"`
	UserID    uuid.UUID  `gorm:"type:uuid;not null" json:"user_id"`
	Content   string     `gorm:"type:text;not null;default:''" json:"content"`
	Media     string     `gorm:"type:jsonb;not null;default:'[]'" json:"media"`
	MediaKind int16      `gorm:"type:smallint;not null;default:0" json:"media_kind"`
	Visibility int16     `gorm:"type:smallint;not null;default:0" json:"visibility"`
	Flagged   bool       `gorm:"not null;default:false" json:"flagged"`
	CreatedAt time.Time  `json:"created_at"`
	DeletedAt *time.Time `gorm:"index" json:"-"`
}

// TableName 指定表名。
func (MomentPost) TableName() string { return "moments_posts" }

// MomentLike 点赞记录。复合主键 (post_id, user_id) 即幂等约束，不另建 id 列。
type MomentLike struct {
	PostID    uuid.UUID `gorm:"type:uuid;primaryKey" json:"post_id"`
	UserID    uuid.UUID `gorm:"type:uuid;primaryKey" json:"user_id"`
	CreatedAt time.Time `json:"created_at"`
}

// TableName 指定表名。
func (MomentLike) TableName() string { return "moments_likes" }

// MomentComment 评论。ReplyToUserID 非空表示回复某人（楼中楼只记被回复者，不建树）。
type MomentComment struct {
	ID            uuid.UUID  `gorm:"type:uuid;primaryKey;default:gen_random_uuid()" json:"id"`
	PostID        uuid.UUID  `gorm:"type:uuid;not null" json:"post_id"`
	UserID        uuid.UUID  `gorm:"type:uuid;not null" json:"user_id"`
	ReplyToUserID *uuid.UUID `gorm:"type:uuid" json:"reply_to_user_id,omitempty"`
	Content       string     `gorm:"type:text;not null" json:"content"`
	Flagged       bool       `gorm:"not null;default:false" json:"flagged"`
	CreatedAt     time.Time  `json:"created_at"`
	DeletedAt     *time.Time `gorm:"index" json:"-"`
}

// TableName 指定表名。
func (MomentComment) TableName() string { return "moments_comments" }

// MomentActivity 一条互动消息（红点数据源）。UserID 为被通知人。
type MomentActivity struct {
	ID        uuid.UUID  `gorm:"type:uuid;primaryKey;default:gen_random_uuid()" json:"id"`
	UserID    uuid.UUID  `gorm:"type:uuid;not null" json:"user_id"`
	ActorID   uuid.UUID  `gorm:"type:uuid;not null" json:"actor_id"`
	PostID    uuid.UUID  `gorm:"type:uuid;not null" json:"post_id"`
	CommentID *uuid.UUID `gorm:"type:uuid" json:"comment_id,omitempty"`
	Kind      int16      `gorm:"type:smallint;not null" json:"kind"`
	CreatedAt time.Time  `json:"created_at"`
	ReadAt    *time.Time `json:"read_at,omitempty"`
}

// TableName 指定表名。
func (MomentActivity) TableName() string { return "moments_activities" }
```

- [ ] **Step 3: 给 User 加状态三列与读时过期判定**

`server/internal/model/user.go`：在 `Birthday` 之后插入三个字段。**json tag 一律 `-`** ——
状态必须经 `EffectiveStatus` 才能出网，直接序列化会把已过期状态泄漏出去：

```go
	// 个人状态（K11）：过期判定只在读时做（EffectiveStatus），不开定时清理任务。
	// json tag 统一为 "-"：直接序列化会把已过期的状态原样吐出去，
	// 所有出网路径必须经 EffectiveStatus 取值。
	StatusEmoji     string     `gorm:"type:varchar(16);not null;default:''" json:"-"`
	StatusText      string     `gorm:"type:varchar(64);not null;default:''" json:"-"`
	StatusExpiresAt *time.Time `json:"-"`
```

在文件末尾追加：

```go
// EffectiveStatus 返回在 now 时刻仍有效的个人状态；已过期或未设置时返回两个空串。
//
// StatusExpiresAt 为 nil 表示「不自动清除」。调用方传入 now 而非内部取 time.Now()，
// 使过期边界可在测试里确定性地断言。
func (u *User) EffectiveStatus(now time.Time) (emoji, text string) {
	if u.StatusExpiresAt != nil && !now.Before(*u.StatusExpiresAt) {
		return "", ""
	}
	return u.StatusEmoji, u.StatusText
}
```

- [ ] **Step 4: 加 admin 动作常量**

`server/internal/model/admin_action_log.go`，在 `AdminActionClearPackFlag` 之后追加：

```go
	// 朋友圈处置：删帖 / 删评论（内容本体软删，本表只记审计）
	AdminActionDeleteMomentPost    = "delete_moment_post"
	AdminActionDeleteMomentComment = "delete_moment_comment"
```

- [ ] **Step 5: 写 EffectiveStatus 的失败测试**

`server/internal/model/user_status_test.go`（新建）：

```go
package model

import (
	"testing"
	"time"
)

// TestEffectiveStatus 过期即视为无状态；nil 过期时间表示长期有效。
func TestEffectiveStatus(t *testing.T) {
	base := time.Date(2026, 9, 13, 12, 0, 0, 0, time.UTC)
	expired := base.Add(-time.Minute)
	future := base.Add(time.Hour)

	cases := []struct {
		name      string
		user      User
		wantEmoji string
		wantText  string
	}{
		{"未过期", User{StatusEmoji: "🌞", StatusText: "在学习", StatusExpiresAt: &future}, "🌞", "在学习"},
		{"已过期", User{StatusEmoji: "🌞", StatusText: "在学习", StatusExpiresAt: &expired}, "", ""},
		{"不自动清除", User{StatusEmoji: "🌞", StatusText: "在学习"}, "🌞", "在学习"},
		{"边界即过期", User{StatusEmoji: "🌞", StatusText: "在学习", StatusExpiresAt: &base}, "", ""},
		{"未设置", User{}, "", ""},
	}
	for _, c := range cases {
		t.Run(c.name, func(t *testing.T) {
			emoji, text := c.user.EffectiveStatus(base)
			if emoji != c.wantEmoji || text != c.wantText {
				t.Fatalf("EffectiveStatus = (%q, %q), want (%q, %q)", emoji, text, c.wantEmoji, c.wantText)
			}
		})
	}
}
```

- [ ] **Step 6: 跑测试确认通过**

```bash
cd server && go test ./internal/model/ -run TestEffectiveStatus -v
```

Expected: PASS（5 个子用例全绿）。若「边界即过期」失败，说明用了 `now.After` 而非 `!now.Before` —— 过期时刻本身应判为已过期。

- [ ] **Step 7: 跑迁移确认可上可下**

```bash
cd server && go run ./cmd/migrate up && go run ./cmd/migrate down && go run ./cmd/migrate up
```

Expected: 三步都无错误。`down` 一步验证回滚不会因外键顺序失败（activities → comments → likes → posts 的删除顺序是刻意的）。

- [ ] **Step 8: 提交**

```bash
git add server/internal/database/migrations/018_g1_moments.sql server/internal/model/
git commit -m "feat(moments): 朋友圈四表迁移与模型，用户个人状态字段"
```

---

### Task 2: 可见性作用域与帖子数据访问

**Files:**

- Create: `server/internal/repository/moments_repo.go`
- Create: `server/internal/repository/moments_repo_test.go`

**Interfaces:**

- Consumes: Task 1 的全部 model 类型
- Produces:
  - `func NewMomentsRepository(db *gorm.DB) *MomentsRepository`
  - `func (r *MomentsRepository) VisiblePostsScope(userID uuid.UUID) func(*gorm.DB) *gorm.DB`
  - `func (r *MomentsRepository) CreatePost(ctx context.Context, p *model.MomentPost) error`
  - `func (r *MomentsRepository) PostByID(ctx context.Context, viewerID, postID uuid.UUID) (*model.MomentPost, error)`
  - `func (r *MomentsRepository) Feed(ctx context.Context, viewerID uuid.UUID, cursor *MomentCursor, limit int) ([]model.MomentPost, error)`
  - `func (r *MomentsRepository) UserPosts(ctx context.Context, viewerID, authorID uuid.UUID, cursor *MomentCursor, limit int) ([]model.MomentPost, error)`
  - `func (r *MomentsRepository) SoftDeletePost(ctx context.Context, postID uuid.UUID) error`
  - `type MomentCursor struct{ CreatedAt time.Time; ID uuid.UUID }`
  - `func ParseMomentCursor(s string) (*MomentCursor, error)` / `func (c MomentCursor) String() string`

- [ ] **Step 1: 写可见性的失败测试**

`server/internal/repository/moments_repo_test.go`（新建）。本包已有两个助手，**直接复用，不要重复定义**：
`testDB(t)` 与 `newTestUser(t, db, tag)`（均在 `blocklist_repo_test.go`，后者已处理短号/手机号冲突与清理）：

```go
package repository

import (
	"context"
	"testing"

	"github.com/google/uuid"
	"github.com/yuanchat/server/internal/model"
	"gorm.io/gorm"
)

// befriend 建立 a→b 的单向已接受好友行（contacts 是单向两行表）。
func befriend(t *testing.T, db *gorm.DB, a, b uuid.UUID) {
	t.Helper()
	c := &model.Contact{UserID: a, ContactUserID: b, Status: model.ContactStatusAccepted}
	if err := db.Create(c).Error; err != nil {
		t.Fatalf("create contact: %v", err)
	}
	t.Cleanup(func() { db.Unscoped().Delete(c) })
}

// newPost 建一条帖子并登记清理。
func newPost(t *testing.T, db *gorm.DB, author uuid.UUID, visibility int16) *model.MomentPost {
	t.Helper()
	p := &model.MomentPost{UserID: author, Content: "hello", Visibility: visibility}
	if err := db.Create(p).Error; err != nil {
		t.Fatalf("create post: %v", err)
	}
	t.Cleanup(func() { db.Unscoped().Delete(p) })
	return p
}

// TestVisiblePostsScope 可见性是全部读路径的唯一闸门，四种关系逐一验证。
func TestVisiblePostsScope(t *testing.T) {
	db := testDB(t)
	repo := NewMomentsRepository(db)
	ctx := context.Background()

	author := newTestUser(t, db, "author").ID
	friend := newTestUser(t, db, "friend").ID
	stranger := newTestUser(t, db, "stranger").ID
	befriend(t, db, friend, author)
	befriend(t, db, author, friend)

	friendsPost := newPost(t, db, author, model.MomentVisibilityFriends)
	privatePost := newPost(t, db, author, model.MomentVisibilityPrivate)

	// 好友看得到 friends 档
	if got, err := repo.PostByID(ctx, friend, friendsPost.ID); err != nil || got == nil {
		t.Fatalf("好友应能看到 friends 档帖子: got=%v err=%v", got, err)
	}
	// 好友看不到 private 档
	if got, _ := repo.PostByID(ctx, friend, privatePost.ID); got != nil {
		t.Fatal("好友不应看到 private 档帖子")
	}
	// 作者自己看得到 private 档
	if got, err := repo.PostByID(ctx, author, privatePost.ID); err != nil || got == nil {
		t.Fatalf("作者应能看到自己的 private 帖子: got=%v err=%v", got, err)
	}
	// 非好友看不到 friends 档
	if got, _ := repo.PostByID(ctx, stranger, friendsPost.ID); got != nil {
		t.Fatal("非好友不应看到 friends 档帖子")
	}
}

// TestVisiblePostsScope_Blocked 拉黑是双向阻断：任一方向的拉黑行都让帖子互相不可见。
func TestVisiblePostsScope_Blocked(t *testing.T) {
	db := testDB(t)
	repo := NewMomentsRepository(db)
	ctx := context.Background()

	author := newTestUser(t, db, "blkauthor").ID
	viewer := newTestUser(t, db, "blkviewer").ID
	befriend(t, db, viewer, author)
	befriend(t, db, author, viewer)
	post := newPost(t, db, author, model.MomentVisibilityFriends)

	// 前置：未拉黑时可见
	if got, _ := repo.PostByID(ctx, viewer, post.ID); got == nil {
		t.Fatal("拉黑前应可见")
	}

	// 作者拉黑观看者（viewer 是被拉黑方）
	b := &model.Blocklist{UserID: author, TargetID: viewer}
	if err := db.Create(b).Error; err != nil {
		t.Fatalf("create blocklist: %v", err)
	}
	t.Cleanup(func() { db.Unscoped().Delete(b) })

	if got, _ := repo.PostByID(ctx, viewer, post.ID); got != nil {
		t.Fatal("被作者拉黑后不应看到该作者的帖子")
	}
}

// TestUserPosts_StrangerGetsEmpty 非好友查别人主页得到空列表，而不是错误。
//
// 回 403 会泄漏「这个人存在且发过帖」；空列表让调用方无从区分
// 「没发过」与「不给你看」。feed 同理（非好友的帖子根本不进结果集）。
func TestUserPosts_StrangerGetsEmpty(t *testing.T) {
	db := testDB(t)
	repo := NewMomentsRepository(db)
	ctx := context.Background()

	author := newTestUser(t, db, "upauthor").ID
	stranger := newTestUser(t, db, "upstranger").ID
	newPost(t, db, author, model.MomentVisibilityFriends)

	got, err := repo.UserPosts(ctx, stranger, author, nil, 20)
	if err != nil {
		t.Fatalf("UserPosts 不应报错，应回空列表: %v", err)
	}
	if len(got) != 0 {
		t.Fatalf("非好友看到了 %d 条帖子", len(got))
	}

	feed, err := repo.Feed(ctx, stranger, nil, 20)
	if err != nil {
		t.Fatalf("feed: %v", err)
	}
	for _, p := range feed {
		if p.UserID == author {
			t.Fatal("非好友的帖子进了 feed")
		}
	}
}

// TestFeedCursor 同一时刻写入的多帖必须不漏不重——单 created_at 游标会在这里翻车。
func TestFeedCursor(t *testing.T) {
	db := testDB(t)
	repo := NewMomentsRepository(db)
	ctx := context.Background()

	author := newTestUser(t, db, "cursor").ID
	// 同一事务里连写 5 帖：NOW() 在事务内是同一时刻，created_at 完全相同
	var posts []*model.MomentPost
	err := db.Transaction(func(tx *gorm.DB) error {
		for i := 0; i < 5; i++ {
			p := &model.MomentPost{UserID: author, Content: "p", Visibility: model.MomentVisibilityFriends}
			if err := tx.Create(p).Error; err != nil {
				return err
			}
			posts = append(posts, p)
		}
		return nil
	})
	if err != nil {
		t.Fatalf("create posts: %v", err)
	}
	t.Cleanup(func() {
		for _, p := range posts {
			db.Unscoped().Delete(p)
		}
	})

	seen := map[uuid.UUID]int{}
	var cursor *MomentCursor
	for page := 0; page < 5; page++ {
		batch, err := repo.Feed(ctx, author, cursor, 2)
		if err != nil {
			t.Fatalf("feed page %d: %v", page, err)
		}
		if len(batch) == 0 {
			break
		}
		for _, p := range batch {
			seen[p.ID]++
		}
		last := batch[len(batch)-1]
		cursor = &MomentCursor{CreatedAt: last.CreatedAt, ID: last.ID}
	}

	if len(seen) != 5 {
		t.Fatalf("分页共见到 %d 帖，want 5（漏页）", len(seen))
	}
	for id, n := range seen {
		if n != 1 {
			t.Fatalf("帖子 %s 出现 %d 次，want 1（重页）", id, n)
		}
	}
}
```

- [ ] **Step 2: 跑测试确认失败**

```bash
cd server && go test ./internal/repository/ -run 'TestVisiblePostsScope|TestFeedCursor|TestUserPosts_StrangerGetsEmpty' -v
```

Expected: 编译失败 `undefined: NewMomentsRepository`。

- [ ] **Step 3: 实现 repository**

`server/internal/repository/moments_repo.go`：

```go
package repository

import (
	"context"
	"encoding/base64"
	"fmt"
	"strconv"
	"strings"
	"time"

	"github.com/google/uuid"
	"github.com/yuanchat/server/internal/model"
	"gorm.io/gorm"
)

// MomentsRepository 朋友圈数据访问。
//
// 全部读路径（feed / 主页 / 单帖 / 点赞 / 评论 / 互动）都必须先过 VisiblePostsScope：
// 只要有一条读写路径绕过它，就是越权——例如 feed 过滤了而点赞接口没过滤时，
// 非好友能给私密帖点赞并把互动消息推给作者。
type MomentsRepository struct {
	db *gorm.DB
}

func NewMomentsRepository(db *gorm.DB) *MomentsRepository {
	return &MomentsRepository{db: db}
}

// MomentCursor feed 复合游标。
//
// 只用 created_at 会在同毫秒多帖时漏页/重页（批量导入或同事务连发都会撞时刻），
// 故以 (created_at, id) 做行比较，id 充当 tie-breaker。
type MomentCursor struct {
	CreatedAt time.Time
	ID        uuid.UUID
}

// String 编码为 URL 安全的不透明串（微秒时间戳_uuid，再 base64）。
// 对外不透出内部形状，便于将来换排序键。
func (c MomentCursor) String() string {
	raw := strconv.FormatInt(c.CreatedAt.UnixMicro(), 10) + "_" + c.ID.String()
	return base64.RawURLEncoding.EncodeToString([]byte(raw))
}

// ParseMomentCursor 解析游标串；空串返回 (nil, nil) 表示从最新一页开始。
func ParseMomentCursor(s string) (*MomentCursor, error) {
	if s == "" {
		return nil, nil
	}
	decoded, err := base64.RawURLEncoding.DecodeString(s)
	if err != nil {
		return nil, fmt.Errorf("invalid cursor encoding: %w", err)
	}
	parts := strings.SplitN(string(decoded), "_", 2)
	if len(parts) != 2 {
		return nil, fmt.Errorf("invalid cursor format")
	}
	micro, err := strconv.ParseInt(parts[0], 10, 64)
	if err != nil {
		return nil, fmt.Errorf("invalid cursor time: %w", err)
	}
	id, err := uuid.Parse(parts[1])
	if err != nil {
		return nil, fmt.Errorf("invalid cursor id: %w", err)
	}
	return &MomentCursor{CreatedAt: time.UnixMicro(micro).UTC(), ID: id}, nil
}

// VisiblePostsScope 「userID 可见的帖子」作用域，是可见性的唯一判定处。
//
// 可见 = 自己的帖子（含 private）∪ 好友的 friends 档帖子（且双向未拉黑）。
//
// 表结构要点（与 model/contact.go、model/blocklist.go 一致）：
//   - contacts 是单向两行表，列名 contact_user_id，status=1 才算好友，带 deleted_at 软删；
//     删好友是双向软删，故只查「我方向」的行即可
//   - blocklists 列名 target_id，解除拉黑是物理删（无 deleted_at），故不加软删条件
//
// 用 moments_posts 的表别名 p：调用方必须以 Table("moments_posts p") 或
// Model(&model.MomentPost{}).Table(...) 形式建查询，否则别名对不上。
func (r *MomentsRepository) VisiblePostsScope(userID uuid.UUID) func(*gorm.DB) *gorm.DB {
	return func(db *gorm.DB) *gorm.DB {
		return db.Where(`p.deleted_at IS NULL AND (
			p.user_id = ?
			OR (
				p.visibility = ?
				AND EXISTS (SELECT 1 FROM contacts c
					WHERE c.user_id = ? AND c.contact_user_id = p.user_id
					  AND c.status = ? AND c.deleted_at IS NULL)
				AND NOT EXISTS (SELECT 1 FROM blocklists b
					WHERE (b.user_id = ? AND b.target_id = p.user_id)
					   OR (b.user_id = p.user_id AND b.target_id = ?))
			)
		)`, userID, model.MomentVisibilityFriends, userID, model.ContactStatusAccepted, userID, userID)
	}
}

// baseVisible 建一个已挂可见性作用域、带别名 p 的查询。
func (r *MomentsRepository) baseVisible(ctx context.Context, viewerID uuid.UUID) *gorm.DB {
	return r.db.WithContext(ctx).
		Table("moments_posts p").
		Scopes(r.VisiblePostsScope(viewerID))
}

// CreatePost 插入帖子。
func (r *MomentsRepository) CreatePost(ctx context.Context, p *model.MomentPost) error {
	return r.db.WithContext(ctx).Create(p).Error
}

// PostByID 取单帖；对 viewerID 不可见时返回 (nil, nil)——调用方据此回 404，
// 不区分「不存在」与「无权看」，避免泄漏帖子存在性。
func (r *MomentsRepository) PostByID(ctx context.Context, viewerID, postID uuid.UUID) (*model.MomentPost, error) {
	var post model.MomentPost
	err := r.baseVisible(ctx, viewerID).Select("p.*").Where("p.id = ?", postID).Take(&post).Error
	if err != nil {
		if err == gorm.ErrRecordNotFound {
			return nil, nil
		}
		return nil, fmt.Errorf("post by id: %w", err)
	}
	return &post, nil
}

// applyCursor 给查询加复合游标条件与排序。
func applyCursor(q *gorm.DB, cursor *MomentCursor, limit int) *gorm.DB {
	if cursor != nil {
		// 行比较：Postgres 原生支持 (a, b) < (x, y) 的字典序语义
		q = q.Where("(p.created_at, p.id) < (?, ?)", cursor.CreatedAt, cursor.ID)
	}
	return q.Order("p.created_at DESC, p.id DESC").Limit(limit)
}

// Feed 信息流：本人 + 好友的可见帖，时间倒序。
func (r *MomentsRepository) Feed(ctx context.Context, viewerID uuid.UUID, cursor *MomentCursor, limit int) ([]model.MomentPost, error) {
	var posts []model.MomentPost
	q := applyCursor(r.baseVisible(ctx, viewerID).Select("p.*"), cursor, limit)
	if err := q.Scan(&posts).Error; err != nil {
		return nil, fmt.Errorf("feed: %w", err)
	}
	return posts, nil
}

// UserPosts 某人的主页帖子（同样过可见性作用域）。
//
// 非好友查别人主页会得到空列表而非错误：存在性不泄漏。
func (r *MomentsRepository) UserPosts(ctx context.Context, viewerID, authorID uuid.UUID, cursor *MomentCursor, limit int) ([]model.MomentPost, error) {
	var posts []model.MomentPost
	q := applyCursor(r.baseVisible(ctx, viewerID).Select("p.*").Where("p.user_id = ?", authorID), cursor, limit)
	if err := q.Scan(&posts).Error; err != nil {
		return nil, fmt.Errorf("user posts: %w", err)
	}
	return posts, nil
}

// SoftDeletePost 软删帖子。软删而非物理删：与全仓口径一致，且
// GC 对软删帖的媒体仍算「在用」（见 ObjectACLRepository.ReferencedKeys 注释）。
func (r *MomentsRepository) SoftDeletePost(ctx context.Context, postID uuid.UUID) error {
	now := time.Now()
	return r.db.WithContext(ctx).
		Model(&model.MomentPost{}).
		Where("id = ? AND deleted_at IS NULL", postID).
		Update("deleted_at", now).Error
}
```

- [ ] **Step 4: 跑测试确认通过**

```bash
cd server && go test ./internal/repository/ -run 'TestVisiblePostsScope|TestFeedCursor|TestUserPosts_StrangerGetsEmpty' -v
```

Expected: 5 个用例 PASS。若本机无测试 Postgres，会整批 skip —— 那样**不算通过**，必须
起库（`pnpm dev:db` 或 deploy/docker-compose）后重跑，可见性是安全边界，不能靠 skip 蒙混。

- [ ] **Step 5: 提交**

```bash
git add server/internal/repository/moments_repo.go server/internal/repository/moments_repo_test.go
git commit -m "feat(moments): 帖子可见性作用域与复合游标分页"
```

---

### Task 3: 点赞 / 评论 / 互动消息数据访问

**Files:**

- Modify: `server/internal/repository/moments_repo.go`（追加方法）
- Modify: `server/internal/repository/moments_repo_test.go`（追加用例）

**Interfaces:**

- Consumes: Task 2 的 `MomentsRepository`、`VisiblePostsScope`、`MomentCursor`
- Produces:
  - `func (r *MomentsRepository) Like(ctx context.Context, postID, userID uuid.UUID) (inserted bool, err error)`
  - `func (r *MomentsRepository) Unlike(ctx context.Context, postID, userID uuid.UUID) error`
  - `func (r *MomentsRepository) LikersByPost(ctx context.Context, postIDs []uuid.UUID) (map[uuid.UUID][]uuid.UUID, error)`
  - `func (r *MomentsRepository) CreateComment(ctx context.Context, c *model.MomentComment) error`
  - `func (r *MomentsRepository) CommentByID(ctx context.Context, id uuid.UUID) (*model.MomentComment, error)`
  - `func (r *MomentsRepository) CommentsByPost(ctx context.Context, postIDs []uuid.UUID) (map[uuid.UUID][]model.MomentComment, error)`
  - `func (r *MomentsRepository) SoftDeleteComment(ctx context.Context, id uuid.UUID) error`
  - `func (r *MomentsRepository) CreateActivity(ctx context.Context, a *model.MomentActivity) error`
  - `func (r *MomentsRepository) Activities(ctx context.Context, userID uuid.UUID, cursor *MomentCursor, limit int) ([]model.MomentActivity, error)`
  - `func (r *MomentsRepository) UnreadActivityCount(ctx context.Context, userID uuid.UUID) (int64, error)`
  - `func (r *MomentsRepository) MarkActivitiesRead(ctx context.Context, userID uuid.UUID, ids []uuid.UUID) error`

- [ ] **Step 1: 写失败测试**

追加到 `moments_repo_test.go`：

```go
// TestLikeIdempotent 连点两次只留一行，且第二次 inserted=false（据此不重复推互动消息）。
func TestLikeIdempotent(t *testing.T) {
	db := testDB(t)
	repo := NewMomentsRepository(db)
	ctx := context.Background()

	author := newTestUser(t, db, "likeauthor").ID
	liker := newTestUser(t, db, "liker").ID
	post := newPost(t, db, author, model.MomentVisibilityFriends)

	first, err := repo.Like(ctx, post.ID, liker)
	if err != nil || !first {
		t.Fatalf("首次点赞应 inserted=true: %v %v", first, err)
	}
	second, err := repo.Like(ctx, post.ID, liker)
	if err != nil {
		t.Fatalf("重复点赞不应报错: %v", err)
	}
	if second {
		t.Fatal("重复点赞应 inserted=false，否则会重复推送互动消息")
	}

	var count int64
	db.Model(&model.MomentLike{}).Where("post_id = ?", post.ID).Count(&count)
	if count != 1 {
		t.Fatalf("点赞行数 = %d, want 1", count)
	}

	// 取消点赞同样幂等
	if err := repo.Unlike(ctx, post.ID, liker); err != nil {
		t.Fatalf("取消点赞: %v", err)
	}
	if err := repo.Unlike(ctx, post.ID, liker); err != nil {
		t.Fatalf("重复取消点赞不应报错: %v", err)
	}
}

// TestCommentsByPost 软删评论不得出现在批量读结果里。
func TestCommentsByPost(t *testing.T) {
	db := testDB(t)
	repo := NewMomentsRepository(db)
	ctx := context.Background()

	author := newTestUser(t, db, "cmtauthor").ID
	post := newPost(t, db, author, model.MomentVisibilityFriends)

	alive := &model.MomentComment{PostID: post.ID, UserID: author, Content: "留着"}
	dead := &model.MomentComment{PostID: post.ID, UserID: author, Content: "删掉"}
	for _, c := range []*model.MomentComment{alive, dead} {
		if err := repo.CreateComment(ctx, c); err != nil {
			t.Fatalf("create comment: %v", err)
		}
		t.Cleanup(func() { db.Unscoped().Delete(c) })
	}
	if err := repo.SoftDeleteComment(ctx, dead.ID); err != nil {
		t.Fatalf("soft delete comment: %v", err)
	}

	got, err := repo.CommentsByPost(ctx, []uuid.UUID{post.ID})
	if err != nil {
		t.Fatalf("comments by post: %v", err)
	}
	list := got[post.ID]
	if len(list) != 1 || list[0].ID != alive.ID {
		t.Fatalf("软删评论仍被读出：%+v", list)
	}
}

// TestUnreadActivityCount 未读计数与批量标已读。
func TestUnreadActivityCount(t *testing.T) {
	db := testDB(t)
	repo := NewMomentsRepository(db)
	ctx := context.Background()

	owner := newTestUser(t, db, "actowner").ID
	actor := newTestUser(t, db, "actactor").ID
	post := newPost(t, db, owner, model.MomentVisibilityFriends)

	for i := 0; i < 3; i++ {
		a := &model.MomentActivity{UserID: owner, ActorID: actor, PostID: post.ID, Kind: model.MomentActivityKindLike}
		if err := repo.CreateActivity(ctx, a); err != nil {
			t.Fatalf("create activity: %v", err)
		}
		t.Cleanup(func() { db.Unscoped().Delete(a) })
	}

	n, err := repo.UnreadActivityCount(ctx, owner)
	if err != nil || n != 3 {
		t.Fatalf("未读计数 = %d (err=%v), want 3", n, err)
	}
	// ids 为空表示全部标已读
	if err := repo.MarkActivitiesRead(ctx, owner, nil); err != nil {
		t.Fatalf("mark read: %v", err)
	}
	n, err = repo.UnreadActivityCount(ctx, owner)
	if err != nil || n != 0 {
		t.Fatalf("标已读后未读计数 = %d (err=%v), want 0", n, err)
	}
}

// TestActivitiesExcludeDeletedPost 删帖后互动消息必须一并消失（读时 join 过滤）。
func TestActivitiesExcludeDeletedPost(t *testing.T) {
	db := testDB(t)
	repo := NewMomentsRepository(db)
	ctx := context.Background()

	owner := newTestUser(t, db, "delowner").ID
	actor := newTestUser(t, db, "delactor").ID
	post := newPost(t, db, owner, model.MomentVisibilityFriends)
	a := &model.MomentActivity{UserID: owner, ActorID: actor, PostID: post.ID, Kind: model.MomentActivityKindLike}
	if err := repo.CreateActivity(ctx, a); err != nil {
		t.Fatalf("create activity: %v", err)
	}
	t.Cleanup(func() { db.Unscoped().Delete(a) })

	if err := repo.SoftDeletePost(ctx, post.ID); err != nil {
		t.Fatalf("soft delete post: %v", err)
	}

	list, err := repo.Activities(ctx, owner, nil, 20)
	if err != nil {
		t.Fatalf("activities: %v", err)
	}
	if len(list) != 0 {
		t.Fatalf("删帖后互动消息仍可见：%+v", list)
	}
	n, err := repo.UnreadActivityCount(ctx, owner)
	if err != nil || n != 0 {
		t.Fatalf("删帖后未读计数 = %d (err=%v), want 0", n, err)
	}
}
```

- [ ] **Step 2: 跑测试确认失败**

```bash
cd server && go test ./internal/repository/ -run 'TestLikeIdempotent|TestCommentsByPost|TestUnreadActivityCount|TestActivitiesExcludeDeletedPost' -v
```

Expected: 编译失败 `repo.Like undefined` 等。

- [ ] **Step 3: 实现追加方法**

追加到 `server/internal/repository/moments_repo.go`：

```go
// Like 点赞。返回 inserted 表示本次是否真的新增了一行——
// 重复点赞返回 false，调用方据此**不重复写互动消息**（否则连点能刷屏作者红点）。
func (r *MomentsRepository) Like(ctx context.Context, postID, userID uuid.UUID) (bool, error) {
	res := r.db.WithContext(ctx).
		Clauses(clause.OnConflict{DoNothing: true}).
		Create(&model.MomentLike{PostID: postID, UserID: userID})
	if res.Error != nil {
		return false, fmt.Errorf("like: %w", res.Error)
	}
	return res.RowsAffected > 0, nil
}

// Unlike 取消点赞（不存在时静默成功，幂等）。
func (r *MomentsRepository) Unlike(ctx context.Context, postID, userID uuid.UUID) error {
	return r.db.WithContext(ctx).
		Where("post_id = ? AND user_id = ?", postID, userID).
		Delete(&model.MomentLike{}).Error
}

// LikersByPost 批量取点赞者 ID（按点赞时间正序），供 feed 一次性组装 DTO，
// 避免每帖一次查询的 N+1。
func (r *MomentsRepository) LikersByPost(ctx context.Context, postIDs []uuid.UUID) (map[uuid.UUID][]uuid.UUID, error) {
	out := make(map[uuid.UUID][]uuid.UUID, len(postIDs))
	if len(postIDs) == 0 {
		return out, nil
	}
	var rows []model.MomentLike
	err := r.db.WithContext(ctx).
		Where("post_id IN ?", postIDs).
		Order("created_at ASC").
		Find(&rows).Error
	if err != nil {
		return nil, fmt.Errorf("likers by post: %w", err)
	}
	for _, row := range rows {
		out[row.PostID] = append(out[row.PostID], row.UserID)
	}
	return out, nil
}

// CreateComment 插入评论。
func (r *MomentsRepository) CreateComment(ctx context.Context, c *model.MomentComment) error {
	return r.db.WithContext(ctx).Create(c).Error
}

// CommentByID 取单条未删评论（删评论鉴权要先拿到它的作者）。
func (r *MomentsRepository) CommentByID(ctx context.Context, id uuid.UUID) (*model.MomentComment, error) {
	var c model.MomentComment
	err := r.db.WithContext(ctx).Where("id = ? AND deleted_at IS NULL", id).Take(&c).Error
	if err != nil {
		if err == gorm.ErrRecordNotFound {
			return nil, nil
		}
		return nil, fmt.Errorf("comment by id: %w", err)
	}
	return &c, nil
}

// CommentsByPost 批量取未删评论（按时间正序），同样是为了避免 feed 的 N+1。
func (r *MomentsRepository) CommentsByPost(ctx context.Context, postIDs []uuid.UUID) (map[uuid.UUID][]model.MomentComment, error) {
	out := make(map[uuid.UUID][]model.MomentComment, len(postIDs))
	if len(postIDs) == 0 {
		return out, nil
	}
	var rows []model.MomentComment
	err := r.db.WithContext(ctx).
		Where("post_id IN ? AND deleted_at IS NULL", postIDs).
		Order("created_at ASC").
		Find(&rows).Error
	if err != nil {
		return nil, fmt.Errorf("comments by post: %w", err)
	}
	for _, row := range rows {
		out[row.PostID] = append(out[row.PostID], row)
	}
	return out, nil
}

// SoftDeleteComment 软删评论。
func (r *MomentsRepository) SoftDeleteComment(ctx context.Context, id uuid.UUID) error {
	return r.db.WithContext(ctx).
		Model(&model.MomentComment{}).
		Where("id = ? AND deleted_at IS NULL", id).
		Update("deleted_at", time.Now()).Error
}

// CreateActivity 写一条互动消息。
func (r *MomentsRepository) CreateActivity(ctx context.Context, a *model.MomentActivity) error {
	return r.db.WithContext(ctx).Create(a).Error
}

// activitiesBase 互动消息的公共查询：join 帖子过滤软删。
//
// 删帖只置 moments_posts.deleted_at，互动行仍在表里；不在读路径 join 过滤的话，
// 删掉的帖子会继续在互动列表里留条目、继续点亮红点，且点进去是 404。
func (r *MomentsRepository) activitiesBase(ctx context.Context, userID uuid.UUID) *gorm.DB {
	return r.db.WithContext(ctx).
		Table("moments_activities a").
		Joins("JOIN moments_posts p ON p.id = a.post_id AND p.deleted_at IS NULL").
		Where("a.user_id = ?", userID)
}

// Activities 互动消息列表（时间倒序，复合游标）。
func (r *MomentsRepository) Activities(ctx context.Context, userID uuid.UUID, cursor *MomentCursor, limit int) ([]model.MomentActivity, error) {
	q := r.activitiesBase(ctx, userID).Select("a.*")
	if cursor != nil {
		q = q.Where("(a.created_at, a.id) < (?, ?)", cursor.CreatedAt, cursor.ID)
	}
	var list []model.MomentActivity
	if err := q.Order("a.created_at DESC, a.id DESC").Limit(limit).Scan(&list).Error; err != nil {
		return nil, fmt.Errorf("activities: %w", err)
	}
	return list, nil
}

// UnreadActivityCount 未读互动数（红点用）。
func (r *MomentsRepository) UnreadActivityCount(ctx context.Context, userID uuid.UUID) (int64, error) {
	var n int64
	err := r.activitiesBase(ctx, userID).Where("a.read_at IS NULL").Count(&n).Error
	if err != nil {
		return 0, fmt.Errorf("unread activity count: %w", err)
	}
	return n, nil
}

// MarkActivitiesRead 标记已读；ids 为空表示把该用户的未读全部标掉。
func (r *MomentsRepository) MarkActivitiesRead(ctx context.Context, userID uuid.UUID, ids []uuid.UUID) error {
	q := r.db.WithContext(ctx).
		Model(&model.MomentActivity{}).
		Where("user_id = ? AND read_at IS NULL", userID)
	if len(ids) > 0 {
		q = q.Where("id IN ?", ids)
	}
	return q.Update("read_at", time.Now()).Error
}
```

import 需加 `"gorm.io/gorm/clause"`。

- [ ] **Step 4: 跑测试确认通过**

```bash
cd server && go test ./internal/repository/ -run 'TestLikeIdempotent|TestCommentsByPost|TestUnreadActivityCount|TestActivitiesExcludeDeletedPost' -v
```

Expected: 4 个用例 PASS。

- [ ] **Step 5: 提交**

```bash
git add server/internal/repository/moments_repo.go server/internal/repository/moments_repo_test.go
git commit -m "feat(moments): 点赞幂等、评论与互动消息数据访问"
```

---

### Task 4: WS 帧与 service 层

**Files:**

- Create: `server/internal/service/moments_service.go`
- Create: `server/internal/service/moments_service_test.go`
- Modify: `server/internal/ws/protocol.go`
- Modify: `server/internal/ws/golden_server_frames_test.go`
- Modify: `contracts/server-frames.golden.json`

**Interfaces:**

- Consumes: Task 2 / 3 的全部 repository 方法；`ModerationService.Check`；`FlaggedUGCRepository`
- Produces:
  - `ws.TypeMomentsActivity = "moments.activity"` + `ws.MomentsActivityPayload`
  - `func NewMomentsService(repo *repository.MomentsRepository, userRepo *repository.UserRepository, dispatcher ws.Dispatcher, logger *zap.Logger) *MomentsService`
  - `func (s *MomentsService) SetModeration(m *ModerationService, r *repository.FlaggedUGCRepository)`
  - `CreatePost(ctx, userID, req CreatePostInput) (*MomentPostDTO, error)`
  - `Feed(ctx, viewerID, cursor string, limit int) (*MomentFeedDTO, error)`
  - `UserPosts(ctx, viewerID, authorID uuid.UUID, cursor string, limit int) (*MomentFeedDTO, error)`
  - `Post(ctx, viewerID, postID uuid.UUID) (*MomentPostDTO, error)`
  - `DeletePost(ctx, userID, postID uuid.UUID) error`
  - `SetLike(ctx, userID, postID uuid.UUID, liked bool) error`
  - `AddComment(ctx, userID, postID uuid.UUID, content string, replyTo *uuid.UUID) (*MomentCommentDTO, error)`
  - `DeleteComment(ctx, userID, commentID uuid.UUID) error`
  - `Activities(ctx, userID, cursor string, limit int) (*MomentActivitiesDTO, error)`
  - `MarkActivitiesRead(ctx, userID uuid.UUID, ids []uuid.UUID) error`
  - 错误哨兵：`ErrMomentNotFound`、`ErrMomentForbidden`、`ErrMomentInvalidMedia`
- DTO 字段名（前端按此编码，不得改名）：见 Step 4 的结构体 json tag

- [ ] **Step 1: 加 WS 帧类型与 payload**

`server/internal/ws/protocol.go`：在服务端帧常量块里追加

```go
	TypeMomentsActivity = "moments.activity"
```

在 payload 结构区追加：

```go
// MomentsActivityPayload 朋友圈互动推送（点赞/评论），只推给帖子作者。
//
// 带上 actor 昵称与评论预览而非只给 id：接收端要在互动列表直接渲染一行，
// 省掉一次回查往返（与 message.edited 直接下发正文同一惯例）。
// 自赞自评不产生本帧（service 层已挡）。
type MomentsActivityPayload struct {
	ID             uuid.UUID `json:"id"`
	Kind           int16     `json:"kind"`
	PostID         uuid.UUID `json:"post_id"`
	ActorID        uuid.UUID `json:"actor_id"`
	ActorNickname  string    `json:"actor_nickname"`
	ActorAvatarURL string    `json:"actor_avatar_url"`
	CommentPreview string    `json:"comment_preview"`
	CreatedAt      time.Time `json:"created_at"`
}
```

- [ ] **Step 2: 登记 golden 契约（两处，缺一必红）**

`contracts/server-frames.golden.json` 的 `cases` 数组追加：

```json
{
  "name": "moments.activity — 好友评论了我的动态",
  "frame": {
    "type": "moments.activity",
    "payload": {
      "id": "9b7c1f2e-1a3b-4c5d-8e9f-0a1b2c3d4e5f",
      "kind": 2,
      "post_id": "1f2e3d4c-5b6a-4798-8a9b-0c1d2e3f4a5b",
      "actor_id": "2e3d4c5b-6a79-4812-9b0c-1d2e3f4a5b6c",
      "actor_nickname": "张三",
      "actor_avatar_url": "https://cdn.example.com/avatars/2026/09/a.jpg",
      "comment_preview": "这张拍得好",
      "created_at": "2026-09-13T10:00:00Z"
    }
  }
}
```

`server/internal/ws/golden_server_frames_test.go`：

1. `payloadPrototypes()` 的 map 里加 `TypeMomentsActivity: MomentsActivityPayload{},`
2. `TestGoldenServerFramesDecodeIntoPayloadStructs` 的 switch 里加分支：

```go
			case TypeMomentsActivity:
				var p MomentsActivityPayload
				if err := dec.Decode(&p); err != nil {
					t.Fatalf("decode into MomentsActivityPayload: %v", err)
				}
```

- [ ] **Step 3: 跑契约测试**

```bash
cd server && go test ./internal/ws/ -run TestGoldenServerFrames -v
```

Expected: PASS。故意漏登记 `payloadPrototypes` 时应报 `帧类型 "moments.activity" 未在 payloadPrototypes 登记` —— 可临时注释掉那行验证门禁有效，验证完恢复。

- [ ] **Step 4: 写 service 的失败测试**

`server/internal/service/moments_service_test.go`（新建）。本包已有 `testDB(t)` 与建用户助手
（`contact_service_test.go`），复用之：

```go
package service

import (
	"context"
	"strings"
	"testing"

	"github.com/google/uuid"
	"github.com/yuanchat/server/internal/model"
	"github.com/yuanchat/server/internal/repository"
	"go.uber.org/zap"
)

// TestCreatePost_MediaValidation 媒体数量与判别位必须自洽，否则前端能塞出畸形帖。
func TestCreatePost_MediaValidation(t *testing.T) {
	db := testDB(t)
	svc := NewMomentsService(repository.NewMomentsRepository(db), repository.NewUserRepository(db), nil, zap.NewNop())
	ctx := context.Background()
	author := newTestUser(t, db, "mediaval")

	nine := make([]model.MomentMediaItem, 9)
	for i := range nine {
		nine[i] = model.MomentMediaItem{Key: "images/2026/09/" + uuid.NewString() + ".jpg", W: 10, H: 10}
	}
	ten := append(append([]model.MomentMediaItem{}, nine...), model.MomentMediaItem{Key: "images/x.jpg", W: 1, H: 1})

	cases := []struct {
		name    string
		input   CreatePostInput
		wantErr bool
	}{
		{"九图合法", CreatePostInput{MediaKind: model.MomentMediaKindImage, Media: nine}, false},
		{"十图越界", CreatePostInput{MediaKind: model.MomentMediaKindImage, Media: ten}, true},
		{"图片零项", CreatePostInput{MediaKind: model.MomentMediaKindImage}, true},
		{"视频两项", CreatePostInput{MediaKind: model.MomentMediaKindVideo, Media: nine[:2]}, true},
		{"纯文本", CreatePostInput{Content: "今天天气不错"}, false},
		{"空帖", CreatePostInput{}, true},
	}
	for _, c := range cases {
		t.Run(c.name, func(t *testing.T) {
			got, err := svc.CreatePost(ctx, author.ID, c.input)
			if c.wantErr && err == nil {
				t.Fatalf("应报错但成功了: %+v", got)
			}
			if !c.wantErr && err != nil {
				t.Fatalf("应成功但报错: %v", err)
			}
		})
	}
}

// TestCreatePost_Flagged 敏感词命中只打标，不阻塞发布（与消息/贴纸同口径）。
func TestCreatePost_Flagged(t *testing.T) {
	db := testDB(t)
	repo := repository.NewMomentsRepository(db)
	svc := NewMomentsService(repo, repository.NewUserRepository(db), nil, zap.NewNop())
	svc.SetModeration(NewModerationService([]string{"违禁词"}), repository.NewFlaggedUGCRepository(db))
	ctx := context.Background()
	author := newTestUser(t, db, "flagged")

	dto, err := svc.CreatePost(ctx, author.ID, CreatePostInput{Content: "包含违禁词的正文"})
	if err != nil {
		t.Fatalf("命中敏感词不应阻塞发布: %v", err)
	}
	var post model.MomentPost
	if err := db.Where("id = ?", dto.ID).Take(&post).Error; err != nil {
		t.Fatalf("回查帖子: %v", err)
	}
	if !post.Flagged {
		t.Fatal("命中敏感词应置 flagged=true")
	}
}

// TestSetLike_Forbidden 非好友点赞必须被拒，且不留下互动消息。
//
// 这是越权的典型形态：feed 过滤了而写路径没过滤时，陌生人能给私密帖点赞并推红点。
func TestSetLike_Forbidden(t *testing.T) {
	db := testDB(t)
	repo := repository.NewMomentsRepository(db)
	svc := NewMomentsService(repo, repository.NewUserRepository(db), nil, zap.NewNop())
	ctx := context.Background()

	author := newTestUser(t, db, "likeauth")
	stranger := newTestUser(t, db, "likestr")
	post := &model.MomentPost{UserID: author.ID, Content: "hi", Visibility: model.MomentVisibilityFriends}
	if err := repo.CreatePost(ctx, post); err != nil {
		t.Fatalf("create post: %v", err)
	}
	t.Cleanup(func() { db.Unscoped().Delete(post) })

	if err := svc.SetLike(ctx, stranger.ID, post.ID, true); err == nil {
		t.Fatal("非好友点赞应被拒")
	}
	var acts int64
	db.Model(&model.MomentActivity{}).Where("post_id = ?", post.ID).Count(&acts)
	if acts != 0 {
		t.Fatalf("被拒的点赞仍写了 %d 条互动消息", acts)
	}
}

// TestSelfInteractionNoActivity 自赞自评不写互动消息，否则自己的操作会点亮自己的红点。
func TestSelfInteractionNoActivity(t *testing.T) {
	db := testDB(t)
	repo := repository.NewMomentsRepository(db)
	svc := NewMomentsService(repo, repository.NewUserRepository(db), nil, zap.NewNop())
	ctx := context.Background()

	author := newTestUser(t, db, "selfact")
	post := &model.MomentPost{UserID: author.ID, Content: "hi", Visibility: model.MomentVisibilityFriends}
	if err := repo.CreatePost(ctx, post); err != nil {
		t.Fatalf("create post: %v", err)
	}
	t.Cleanup(func() { db.Unscoped().Delete(post) })

	if err := svc.SetLike(ctx, author.ID, post.ID, true); err != nil {
		t.Fatalf("自己点赞自己应允许: %v", err)
	}
	if _, err := svc.AddComment(ctx, author.ID, post.ID, "自评", nil); err != nil {
		t.Fatalf("自己评论自己应允许: %v", err)
	}

	n, err := repo.UnreadActivityCount(ctx, author.ID)
	if err != nil {
		t.Fatalf("unread count: %v", err)
	}
	if n != 0 {
		t.Fatalf("自赞自评产生了 %d 条互动消息，want 0", n)
	}
}

// TestDeleteComment_Permission 评论作者与帖子作者都能删，第三方不能。
func TestDeleteComment_Permission(t *testing.T) {
	db := testDB(t)
	repo := repository.NewMomentsRepository(db)
	svc := NewMomentsService(repo, repository.NewUserRepository(db), nil, zap.NewNop())
	ctx := context.Background()

	author := newTestUser(t, db, "delcauth")
	commenter := newTestUser(t, db, "delccmt")
	third := newTestUser(t, db, "delcthird")
	for _, pair := range [][2]uuid.UUID{{author.ID, commenter.ID}, {commenter.ID, author.ID}, {author.ID, third.ID}, {third.ID, author.ID}} {
		c := &model.Contact{UserID: pair[0], ContactUserID: pair[1], Status: model.ContactStatusAccepted}
		if err := db.Create(c).Error; err != nil {
			t.Fatalf("create contact: %v", err)
		}
		t.Cleanup(func() { db.Unscoped().Delete(c) })
	}

	post := &model.MomentPost{UserID: author.ID, Content: "hi", Visibility: model.MomentVisibilityFriends}
	if err := repo.CreatePost(ctx, post); err != nil {
		t.Fatalf("create post: %v", err)
	}
	t.Cleanup(func() { db.Unscoped().Delete(post) })

	mk := func(t *testing.T) uuid.UUID {
		t.Helper()
		dto, err := svc.AddComment(ctx, commenter.ID, post.ID, "评论正文", nil)
		if err != nil {
			t.Fatalf("add comment: %v", err)
		}
		return dto.ID
	}

	if err := svc.DeleteComment(ctx, third.ID, mk(t)); err == nil {
		t.Fatal("第三方不应能删评论")
	}
	if err := svc.DeleteComment(ctx, commenter.ID, mk(t)); err != nil {
		t.Fatalf("评论作者应能删: %v", err)
	}
	if err := svc.DeleteComment(ctx, author.ID, mk(t)); err != nil {
		t.Fatalf("帖子作者应能删: %v", err)
	}
}

// TestCommentTooLong 评论超长被拒（边界值：500 通过、501 拒）。
func TestCommentTooLong(t *testing.T) {
	db := testDB(t)
	repo := repository.NewMomentsRepository(db)
	svc := NewMomentsService(repo, repository.NewUserRepository(db), nil, zap.NewNop())
	ctx := context.Background()

	author := newTestUser(t, db, "cmtlen")
	post := &model.MomentPost{UserID: author.ID, Content: "hi", Visibility: model.MomentVisibilityFriends}
	if err := repo.CreatePost(ctx, post); err != nil {
		t.Fatalf("create post: %v", err)
	}
	t.Cleanup(func() { db.Unscoped().Delete(post) })

	// 用中文字符验证按 rune 而非 byte 计长
	if _, err := svc.AddComment(ctx, author.ID, post.ID, strings.Repeat("字", model.MomentCommentMaxLen), nil); err != nil {
		t.Fatalf("500 字应通过: %v", err)
	}
	if _, err := svc.AddComment(ctx, author.ID, post.ID, strings.Repeat("字", model.MomentCommentMaxLen+1), nil); err == nil {
		t.Fatal("501 字应被拒")
	}
}
```

- [ ] **Step 5: 跑测试确认失败**

```bash
cd server && go test ./internal/service/ -run 'TestCreatePost|TestSetLike|TestSelfInteraction|TestDeleteComment_Permission|TestCommentTooLong' -v
```

Expected: 编译失败 `undefined: NewMomentsService`。

- [ ] **Step 6: 实现 service**

`server/internal/service/moments_service.go`。要点逐条对应上面的测试：

```go
package service

import (
	"context"
	"encoding/json"
	"errors"
	"fmt"
	"time"
	"unicode/utf8"

	"github.com/google/uuid"
	"github.com/yuanchat/server/internal/model"
	"github.com/yuanchat/server/internal/repository"
	"github.com/yuanchat/server/internal/ws"
	"go.uber.org/zap"
)

// 朋友圈错误哨兵，handler 据此映射 HTTP 状态码。
var (
	// ErrMomentNotFound 帖子/评论不存在**或对调用者不可见**。
	// 两种情况合并成同一个错误是刻意的：区分开会泄漏帖子存在性。
	ErrMomentNotFound = errors.New("moment not found")
	// ErrMomentForbidden 可见但无权操作（删他人帖、删他人评论）。
	ErrMomentForbidden = errors.New("moment forbidden")
	// ErrMomentInvalidMedia 媒体数量与判别位不自洽，或正文超长。
	ErrMomentInvalidMedia = errors.New("moment invalid media")
)

// CreatePostInput 发布入参。
type CreatePostInput struct {
	Content    string
	Media      []model.MomentMediaItem
	MediaKind  int16
	Visibility int16
}

// MomentUserDTO 帖子/评论/互动里内嵌的用户摘要。
type MomentUserDTO struct {
	ID          uuid.UUID `json:"id"`
	Nickname    string    `json:"nickname"`
	AvatarURL   string    `json:"avatar_url"`
	StatusEmoji string    `json:"status_emoji"`
}

// MomentCommentDTO 评论。
type MomentCommentDTO struct {
	ID          uuid.UUID      `json:"id"`
	User        MomentUserDTO  `json:"user"`
	ReplyToUser *MomentUserDTO `json:"reply_to_user"`
	Content     string         `json:"content"`
	CreatedAt   time.Time      `json:"created_at"`
}

// MomentPostDTO 帖子。
//
// media 出的是对象 key 而非签好名的 URL：客户端调既有 /files/download-url 换签
//（那边有进程内签名缓存），服务端逐帖签名会让一次 feed 响应签最多 20×9=180 个 URL。
type MomentPostDTO struct {
	ID        uuid.UUID               `json:"id"`
	User      MomentUserDTO           `json:"user"`
	Content   string                  `json:"content"`
	MediaKind int16                   `json:"media_kind"`
	Media     []model.MomentMediaItem `json:"media"`
	Visibility int16                  `json:"visibility"`
	LikeCount int                     `json:"like_count"`
	LikedByMe bool                    `json:"liked_by_me"`
	Likes     []MomentUserDTO         `json:"likes"`
	Comments  []MomentCommentDTO      `json:"comments"`
	CreatedAt time.Time               `json:"created_at"`
	Deletable bool                    `json:"deletable"`
}

// MomentFeedDTO 一页帖子。NextCursor 为空表示已到底。
type MomentFeedDTO struct {
	Posts      []MomentPostDTO `json:"posts"`
	NextCursor string          `json:"next_cursor"`
}

// MomentActivityDTO 一条互动消息。
type MomentActivityDTO struct {
	ID             uuid.UUID     `json:"id"`
	Kind           int16         `json:"kind"`
	PostID         uuid.UUID     `json:"post_id"`
	Actor          MomentUserDTO `json:"actor"`
	CommentPreview string        `json:"comment_preview"`
	PostPreview    string        `json:"post_preview"`
	PostThumbKey   string        `json:"post_thumb_key"`
	Read           bool          `json:"read"`
	CreatedAt      time.Time     `json:"created_at"`
}

// MomentActivitiesDTO 互动消息一页 + 未读数。
type MomentActivitiesDTO struct {
	Activities  []MomentActivityDTO `json:"activities"`
	UnreadCount int64               `json:"unread_count"`
	NextCursor  string              `json:"next_cursor"`
}

// MomentsService 朋友圈业务逻辑。
//
// 权限的唯一来源是 repository 的可见性作用域：本层每个写操作都先用
// PostByID 取一次帖子，取不到即视为不可见 → ErrMomentNotFound，
// 不另写一套判定（两套判定必然漂移，漂移处就是越权洞）。
type MomentsService struct {
	repo       *repository.MomentsRepository
	userRepo   *repository.UserRepository
	dispatcher ws.Dispatcher
	logger     *zap.Logger

	moderation *ModerationService
	ugcRepo    *repository.FlaggedUGCRepository
}

func NewMomentsService(repo *repository.MomentsRepository, userRepo *repository.UserRepository, dispatcher ws.Dispatcher, logger *zap.Logger) *MomentsService {
	return &MomentsService{repo: repo, userRepo: userRepo, dispatcher: dispatcher, logger: logger}
}

// SetModeration 注入敏感词审核（与 UserService.SetUGCModeration 同一惯例，可不注入）。
func (s *MomentsService) SetModeration(m *ModerationService, r *repository.FlaggedUGCRepository) {
	s.moderation = m
	s.ugcRepo = r
}
```

**必须实现的行为**（每条都有对应测试，实现时逐条对照）：

1. `CreatePost`：
   - 校验 `MediaKind`：`Image` → `1 <= len(Media) <= model.MomentMaxImages`；`Video` → `len(Media) == 1`；`None` → `len(Media) == 0`
   - 校验空帖：`Content == "" && MediaKind == None` → `ErrMomentInvalidMedia`
   - 校验正文长度按 `utf8.RuneCountInString` ≤ `MomentContentMaxLen`
   - 校验 `Visibility` 只能是 0/1
   - `json.Marshal(Media)` 写入 `Media` 字段
   - 敏感词：`s.moderation.Check(content)` 命中 → `post.Flagged = true`，并写 flagged UGC 台账（`ugc_type` 用 `"moment_post"`），**不阻塞发布**
2. `SetLike`：先 `PostByID(viewer)`，nil → `ErrMomentNotFound`；`liked=true` 时 `repo.Like`，仅当 `inserted && post.UserID != userID` 才写 activity 并推 WS 帧
3. `AddComment`：先 `PostByID(viewer)`，nil → `ErrMomentNotFound`；长度按 rune 校验 ≤ `MomentCommentMaxLen` 且非空；敏感词打标（`ugc_type` 用 `"moment_comment"`）；`post.UserID != userID` 才写 activity + 推帧
4. `DeleteComment`：`CommentByID` → nil 则 `ErrMomentNotFound`；再 `PostByID(viewer)` 确认可见；允许 `comment.UserID == userID || post.UserID == userID`，否则 `ErrMomentForbidden`
5. `DeletePost`：`PostByID(viewer)`；`post.UserID != userID` → `ErrMomentForbidden`；否则软删
6. `Feed` / `UserPosts`：取 `limit+1` 条判断是否还有下一页，多出的一条不返回、只用来生成 `NextCursor`；再用 `LikersByPost` / `CommentsByPost` 批量组装，**禁止每帖一次查询**
7. DTO 里的用户摘要 `StatusEmoji` 必须走 `user.EffectiveStatus(time.Now())` 取，不得直接读字段
8. `Activities` 组装 `PostPreview` / `PostThumbKey`：按 `post_id` 批量取回帖子，
   `PostPreview` 取 `Content` 前 20 个 rune（按 rune 截断，按 byte 会把中文截半），
   `PostThumbKey` 取 `media[0]` 的 `ThumbKey`（视频）或 `Key`（图片），无媒体则空串。
   这两个字段只在 REST 出现、WS 帧不带 —— 帧是增量通知，列表页会重拉
9. WS 推送：`s.dispatcher` 为 nil 时跳过（测试里传 nil）；推送失败只记日志不影响主流程

推送帧的组装（`Dispatcher` 是 `ws.Dispatcher`，已核对 `server/internal/ws/hub.go:15`：
唯一方法 `SendToUsers(userIDs []uuid.UUID, data []byte)` —— **复数、无 ctx、无返回值**）：

```go
// notifyActivity 把互动消息推给帖子作者的全部在线设备。
//
// 不因推送失败回滚点赞/评论：互动消息已落库，前端下次拉 /activities 仍能看到。
// SendToUsers 本身不返回错误（投递是尽力而为），故这里只在编码失败时记日志。
func (s *MomentsService) notifyActivity(act *model.MomentActivity, actor *model.User, preview string) {
	if s.dispatcher == nil {
		return
	}
	avatar := ""
	if actor.AvatarURL != nil {
		avatar = *actor.AvatarURL
	}
	frame, err := ws.Encode(ws.TypeMomentsActivity, ws.MomentsActivityPayload{
		ID:             act.ID,
		Kind:           act.Kind,
		PostID:         act.PostID,
		ActorID:        actor.ID,
		ActorNickname:  actor.Nickname,
		ActorAvatarURL: avatar,
		CommentPreview: preview,
		CreatedAt:      act.CreatedAt,
	})
	if err != nil {
		s.logger.Error("encode moments activity frame failed", zap.Error(err))
		return
	}
	s.dispatcher.SendToUsers([]uuid.UUID{act.UserID}, frame)
}
```

`MomentsService` 的字段与构造参数类型同样用 `ws.Dispatcher`：

```go
	dispatcher ws.Dispatcher
```

- [ ] **Step 7: 跑测试确认通过**

```bash
cd server && go test ./internal/service/ -run 'TestCreatePost|TestSetLike|TestSelfInteraction|TestDeleteComment_Permission|TestCommentTooLong' -v
```

Expected: 全部 PASS（含 6 个媒体校验子用例）。

- [ ] **Step 8: 提交**

```bash
git add server/internal/service/moments_service.go server/internal/service/moments_service_test.go \
        server/internal/ws/protocol.go server/internal/ws/golden_server_frames_test.go \
        contracts/server-frames.golden.json
git commit -m "feat(moments): 朋友圈业务逻辑与互动推送帧"
```

---

### Task 5: REST handler 与路由

**Files:**

- Create: `server/internal/handler/moments.go`
- Create: `server/internal/handler/moments_test.go`
- Modify: `server/internal/router/router.go`
- Modify: `server/internal/router/router_test.go`

**Interfaces:**

- Consumes: Task 4 的 `MomentsService` 全部方法与错误哨兵
- Produces: `func NewMomentsHandler(svc *service.MomentsService, logger *zap.Logger) *MomentsHandler` + 11 个端点方法

- [ ] **Step 1: 实现 handler**

`server/internal/handler/moments.go`。错误映射统一走一个助手，避免每个端点重复
`errors.Is` 三连：

```go
package handler

import (
	"errors"
	"net/http"
	"strconv"

	"github.com/gin-gonic/gin"
	"github.com/google/uuid"
	"github.com/yuanchat/server/internal/middleware"
	"github.com/yuanchat/server/internal/model"
	"github.com/yuanchat/server/internal/service"
	"go.uber.org/zap"
)

// MomentsHandler 朋友圈 REST 端点。
type MomentsHandler struct {
	svc    *service.MomentsService
	logger *zap.Logger
}

func NewMomentsHandler(svc *service.MomentsService, logger *zap.Logger) *MomentsHandler {
	return &MomentsHandler{svc: svc, logger: logger}
}

// feedLimit 解析 limit 参数，钳到 [1, 20]，缺省 20。
//
// 上限 20 是与媒体签名成本绑的：一页 20 帖 × 9 图 = 180 个对象，
// 客户端逐个换签已是极限，再放大会把首屏拖垮。
func feedLimit(c *gin.Context) int {
	n, err := strconv.Atoi(c.Query("limit"))
	if err != nil || n <= 0 || n > 20 {
		return 20
	}
	return n
}

// fail 把 service 的错误哨兵映射成 HTTP 状态。
//
// ErrMomentNotFound 同时覆盖「不存在」与「对我不可见」，统一回 404：
// 若不可见回 403 而不存在回 404，攻击者能靠状态码差异探测帖子是否存在。
func (h *MomentsHandler) fail(c *gin.Context, err error, msg string) {
	switch {
	case errors.Is(err, service.ErrMomentNotFound):
		NotFound(c, "moment not found")
	case errors.Is(err, service.ErrMomentForbidden):
		Error(c, http.StatusForbidden, 403, "forbidden")
	case errors.Is(err, service.ErrMomentInvalidMedia):
		BadRequest(c, err.Error())
	default:
		h.logger.Error(msg, zap.Error(err))
		InternalError(c, msg)
	}
}
```

端点清单（每个都先 `middleware.GetUserID(c)`，取不到回 `Unauthorized`）：

| 方法     | 路径                       | Handler 方法    | 说明                                             |
| -------- | -------------------------- | --------------- | ------------------------------------------------ |
| `POST`   | `/moments`                 | `Create`        | body → `CreatePostInput`，成功 `Created(c, dto)` |
| `GET`    | `/moments/feed`            | `Feed`          | `cursor` / `limit` query                         |
| `GET`    | `/moments/user/:id`        | `UserPosts`     | 路径 id 解析失败回 400                           |
| `GET`    | `/moments/:id`             | `Get`           | 单帖                                             |
| `DELETE` | `/moments/:id`             | `Delete`        | 成功 `Success(c, gin.H{"deleted": true})`        |
| `POST`   | `/moments/:id/like`        | `Like`          | `SetLike(..., true)`                             |
| `DELETE` | `/moments/:id/like`        | `Unlike`        | `SetLike(..., false)`                            |
| `POST`   | `/moments/:id/comments`    | `AddComment`    | body `{content, reply_to_user_id?}`              |
| `DELETE` | `/moments/comments/:id`    | `DeleteComment` | 注意与 `/moments/:id` 的路由冲突，见 Step 2      |
| `GET`    | `/moments/activities`      | `Activities`    | 同上，静态段优先                                 |
| `POST`   | `/moments/activities/read` | `MarkRead`      | body `{ids?: []}`，空表示全部                    |

请求结构：

```go
// CreateMomentRequest 发布动态请求体。
type CreateMomentRequest struct {
	Content    string                  `json:"content" binding:"max=1000"`
	Media      []model.MomentMediaItem `json:"media" binding:"max=9,dive"`
	MediaKind  int16                   `json:"media_kind" binding:"oneof=0 1 2"`
	Visibility int16                   `json:"visibility" binding:"oneof=0 1"`
}

// AddMomentCommentRequest 评论请求体。
type AddMomentCommentRequest struct {
	Content       string     `json:"content" binding:"required,max=500"`
	ReplyToUserID *uuid.UUID `json:"reply_to_user_id"`
}

// MarkActivitiesReadRequest 标已读；ids 为空表示全部标已读。
type MarkActivitiesReadRequest struct {
	IDs []uuid.UUID `json:"ids"`
}
```

> `binding:"max=1000"` 按 **字节/元素数** 而非 rune 计，中文会提前触顶。
> 故 binding 只当粗筛，**真正的长度校验在 service 层按 rune 做**（Task 4 已实现），
> 两处都要有：binding 挡住明显的超大 body，service 保证语义正确。

- [ ] **Step 2: 接路由（注意 gin 通配符冲突）**

`server/internal/router/router.go`，在 `chat` 组内追加。**静态段路由必须写在
`:id` 参数路由之前**，否则 gin 会把 `activities` 当成 `:id` 去解析 UUID：

```go
		// 朋友圈。注意注册顺序：activities / comments 这些静态段必须在 :id 之前，
		// 否则 gin 会把 "activities" 当作 :id 传进来（解析 UUID 失败回 400）。
		moments := chat.Group("/moments")
		{
			moments.GET("/feed", middleware.LimitByIP(30, 60), momentsH.Feed)
			moments.GET("/activities", middleware.LimitByIP(30, 60), momentsH.Activities)
			moments.POST("/activities/read", middleware.LimitByIP(20, 40), momentsH.MarkRead)
			moments.DELETE("/comments/:id", middleware.LimitByIP(20, 40), momentsH.DeleteComment)
			moments.GET("/user/:id", middleware.LimitByIP(30, 60), momentsH.UserPosts)

			moments.POST("", middleware.LimitByIP(10, 20), momentsH.Create)
			moments.GET("/:id", middleware.LimitByIP(30, 60), momentsH.Get)
			moments.DELETE("/:id", middleware.LimitByIP(20, 40), momentsH.Delete)
			moments.POST("/:id/like", middleware.LimitByIP(30, 60), momentsH.Like)
			moments.DELETE("/:id/like", middleware.LimitByIP(30, 60), momentsH.Unlike)
			moments.POST("/:id/comments", middleware.LimitByIP(20, 40), momentsH.AddComment)
		}
```

装配（在 `presenceH` 附近）：

```go
	momentsRepo := repository.NewMomentsRepository(db)
	momentsSvc := service.NewMomentsService(momentsRepo, userRepo, hub, logger)
	momentsSvc.SetModeration(moderationSvc, flaggedUGCRepo)
	momentsH := handler.NewMomentsHandler(momentsSvc, logger)
```

> `moderationSvc` / `flaggedUGCRepo` 的真实变量名以 router.go 现场为准
> （`grep -n "SetUGCModeration" server/internal/router/router.go` 找到既有注入点照抄）。

- [ ] **Step 3: 补路由注册测试**

`server/internal/router/router_test.go` 的路由表里追加 11 条断言，格式同现有项：

```go
		{"POST", "/api/v1/moments", "Create"},
		{"GET", "/api/v1/moments/feed", "Feed"},
		{"GET", "/api/v1/moments/activities", "Activities"},
		{"POST", "/api/v1/moments/activities/read", "MarkRead"},
		{"GET", "/api/v1/moments/user/:id", "UserPosts"},
		{"GET", "/api/v1/moments/:id", "Get"},
		{"DELETE", "/api/v1/moments/:id", "Delete"},
		{"POST", "/api/v1/moments/:id/like", "Like"},
		{"DELETE", "/api/v1/moments/:id/like", "Unlike"},
		{"POST", "/api/v1/moments/:id/comments", "AddComment"},
		{"DELETE", "/api/v1/moments/comments/:id", "DeleteComment"},
```

- [ ] **Step 4: 写 handler 的 404 不泄漏测试**

`server/internal/handler/moments_test.go`：非好友取帖必须 404 而非 403，
且 body 里不得出现任何帖子字段。具体夹具沿用本包既有 handler 测试的建法
（`grep -n "func newTestRouter\|gin.New()" server/internal/handler/*_test.go` 找现成的）。

- [ ] **Step 5: 跑测试**

```bash
cd server && go test ./internal/router/ ./internal/handler/ -run 'Route|Moment' -v
```

Expected: PASS。若报 `:id` 冲突（`panic: ':id' in new path conflicts with existing wildcard`），
说明静态段注册在了参数路由之后 —— 按 Step 2 的顺序调整。

- [ ] **Step 6: 提交**

```bash
git add server/internal/handler/moments.go server/internal/handler/moments_test.go server/internal/router/
git commit -m "feat(moments): 朋友圈 REST 端点与路由装配"
```

---

### Task 6: 对象 ACL 与 GC 的 moments 分支

**本任务是整批最容易漏且漏了必炸的一项**：不做则好友 feed 里每张图 403，
或 GC 把朋友圈媒体当孤儿删掉。

**Files:**

- Modify: `server/internal/repository/object_acl_repo.go`
- Modify: `server/internal/repository/object_acl_repo_test.go`

**Interfaces:**

- Consumes: Task 1 的 `moments_posts` 表与 `idx_moments_media_gin`；Task 2 的可见性 SQL
- Produces: 不新增导出符号，只扩展 `CanRead` / `ReferencedKeys` 的判定范围

- [ ] **Step 1: 写失败测试**

追加到 `server/internal/repository/object_acl_repo_test.go`：

```go
// momentsACLFixture 建一条带媒体 key 的朋友圈帖子。
func momentsACLFixture(t *testing.T, db *gorm.DB, author uuid.UUID, key, thumbKey string) *model.MomentPost {
	t.Helper()
	media := fmt.Sprintf(`[{"key":%q,"thumb_key":%q,"w":10,"h":10}]`, key, thumbKey)
	p := &model.MomentPost{
		UserID:    author,
		Content:   "带图",
		Media:     media,
		MediaKind: model.MomentMediaKindVideo,
	}
	if err := db.Create(p).Error; err != nil {
		t.Fatalf("create moment post: %v", err)
	}
	t.Cleanup(func() { db.Unscoped().Delete(p) })
	return p
}

// TestObjectACLCanRead_ViaMoment 好友可读朋友圈媒体，非好友不可读，删帖后不可读。
//
// 缺这条分支时，好友打开 feed 每张图都签不出下载 URL（403），图全裂。
func TestObjectACLCanRead_ViaMoment(t *testing.T) {
	db := testDB(t)
	repo := NewObjectACLRepository(db)
	ctx := context.Background()

	author := newTestUser(t, db, "aclauthor")
	friend := newTestUser(t, db, "aclfriend")
	stranger := newTestUser(t, db, "aclstranger")
	for _, pair := range [][2]uuid.UUID{{friend.ID, author.ID}, {author.ID, friend.ID}} {
		c := &model.Contact{UserID: pair[0], ContactUserID: pair[1], Status: model.ContactStatusAccepted}
		if err := db.Create(c).Error; err != nil {
			t.Fatalf("create contact: %v", err)
		}
		t.Cleanup(func() { db.Unscoped().Delete(c) })
	}

	key := "files/2026/09/" + uuid.NewString() + ".mp4"
	thumbKey := "images/2026/09/" + uuid.NewString() + ".jpg"
	post := momentsACLFixture(t, db, author.ID, key, thumbKey)

	for _, tc := range []struct {
		name   string
		viewer uuid.UUID
		target string
		want   bool
	}{
		{"好友读主对象", friend.ID, key, true},
		{"好友读封面", friend.ID, thumbKey, true},
		{"作者读自己的", author.ID, key, true},
		{"非好友不可读", stranger.ID, key, false},
		{"非好友不可读封面", stranger.ID, thumbKey, false},
	} {
		t.Run(tc.name, func(t *testing.T) {
			got, err := repo.CanRead(ctx, tc.viewer, tc.target)
			if err != nil {
				t.Fatalf("CanRead: %v", err)
			}
			if got != tc.want {
				t.Fatalf("CanRead = %v, want %v", got, tc.want)
			}
		})
	}

	// 删帖 = 撤销访问：与消息撤回同语义
	if err := db.Model(&model.MomentPost{}).Where("id = ?", post.ID).
		Update("deleted_at", time.Now()).Error; err != nil {
		t.Fatalf("soft delete: %v", err)
	}
	if got, _ := repo.CanRead(ctx, friend.ID, key); got {
		t.Fatal("删帖后好友仍能签出下载 URL，访问撤销失效")
	}
}

// TestObjectACLReferencedKeys_Moment GC 必须认朋友圈媒体为在用对象。
//
// 与 CanRead 的刻意不对称：软删帖的媒体**仍算被引用**（软删可恢复，
// GC 删了对象就恢复不了）。
func TestObjectACLReferencedKeys_Moment(t *testing.T) {
	db := testDB(t)
	repo := NewObjectACLRepository(db)
	ctx := context.Background()

	author := newTestUser(t, db, "gcauthor")
	key := "files/2026/09/" + uuid.NewString() + ".mp4"
	thumbKey := "images/2026/09/" + uuid.NewString() + ".jpg"
	orphan := "images/2026/09/" + uuid.NewString() + ".jpg"
	post := momentsACLFixture(t, db, author.ID, key, thumbKey)

	got, err := repo.ReferencedKeys(ctx, []string{key, thumbKey, orphan})
	if err != nil {
		t.Fatalf("ReferencedKeys: %v", err)
	}
	if _, ok := got[key]; !ok {
		t.Fatal("朋友圈主对象未被判定为在用，会被 GC 误删")
	}
	if _, ok := got[thumbKey]; !ok {
		t.Fatal("朋友圈封面未被判定为在用，会被 GC 误删")
	}
	if _, ok := got[orphan]; ok {
		t.Fatal("无人引用的 key 不应算在用")
	}

	// 软删后仍算在用
	if err := db.Model(&model.MomentPost{}).Where("id = ?", post.ID).
		Update("deleted_at", time.Now()).Error; err != nil {
		t.Fatalf("soft delete: %v", err)
	}
	got, err = repo.ReferencedKeys(ctx, []string{key})
	if err != nil {
		t.Fatalf("ReferencedKeys after soft delete: %v", err)
	}
	if _, ok := got[key]; !ok {
		t.Fatal("软删帖的媒体被判成孤儿——软删可恢复，GC 删了对象就恢复不了")
	}
}
```

- [ ] **Step 2: 跑测试确认失败**

```bash
cd server && go test ./internal/repository/ -run 'TestObjectACL.*Moment' -v
```

Expected: FAIL —— `CanRead` 对好友返回 false，`ReferencedKeys` 不含 moments key。

- [ ] **Step 3: 给 CanRead 加第三分支**

在 `CanRead` 的 `viaSticker` 判定**之后**（贴纸判定更廉价，放前面短路）追加：

```go
	// 朋友圈媒体：key 出现在某条对 userID 可见的帖子的 media 数组里。
	//
	// 用 @> 包含运算而非 jsonb_array_elements 展开：@> 对数组元素逐个匹配，
	// 两次包含测试就够，且整条走 idx_moments_media_gin（jsonb_path_ops）。
	// 可见性条件与 MomentsRepository.VisiblePostsScope 逐字同源——两处判定
	// 若漂移，漂移的那一侧就是越权洞（能签出不该看的图）。
	var viaMoment bool
	err = r.db.WithContext(ctx).Raw(`
		SELECT EXISTS (
			SELECT 1 FROM moments_posts p
			WHERE (p.media @> jsonb_build_array(jsonb_build_object('key', ?::text))
			    OR p.media @> jsonb_build_array(jsonb_build_object('thumb_key', ?::text)))
			  AND p.deleted_at IS NULL
			  AND (
			    p.user_id = ?
			    OR (
			      p.visibility = 0
			      AND EXISTS (SELECT 1 FROM contacts c
			        WHERE c.user_id = ? AND c.contact_user_id = p.user_id
			          AND c.status = 1 AND c.deleted_at IS NULL)
			      AND NOT EXISTS (SELECT 1 FROM blocklists b
			        WHERE (b.user_id = ? AND b.target_id = p.user_id)
			           OR (b.user_id = p.user_id AND b.target_id = ?))
			    )
			  )
		)`, objectKey, objectKey, userID, userID, userID, userID).Scan(&viaMoment).Error
	if err != nil {
		return false, fmt.Errorf("acl via moment: %w", err)
	}
	return viaMoment, nil
```

并把原来 `return viaSticker, nil` 改成 `if viaSticker { return true, nil }`。

同时更新 `CanRead` 的 godoc 注释，把第 3 条判定写进去（现有注释只列了两条）。

- [ ] **Step 4: 给 ReferencedKeys 加 moments 分支**

在 `collect(...)` 的贴纸分支之后追加：

```go
	// 朋友圈媒体。这里必须用 jsonb_array_elements 展开而非 @>：
	// 要**取出** key 值做集合比对，不是测试包含关系。
	//
	// 刻意不过滤 deleted_at：软删帖的媒体仍算在用。与 CanRead 的不对称是设计，
	// 不是疏漏——软删可恢复，对象删了恢复不了。若将来要回收软删帖的媒体，
	// 应在 GC 里按「软删超过 N 天」单独处理，而不是让本方法漏掉它们。
	if err := collect(`
		SELECT DISTINCT m.elem ->> 'key' FROM moments_posts p
		  CROSS JOIN jsonb_array_elements(p.media) AS m(elem)
		WHERE m.elem ->> 'key' IN ?
		UNION
		SELECT DISTINCT m.elem ->> 'thumb_key' FROM moments_posts p
		  CROSS JOIN jsonb_array_elements(p.media) AS m(elem)
		WHERE m.elem ->> 'thumb_key' IN ?`,
		keys, keys); err != nil {
		return nil, fmt.Errorf("referenced by moments: %w", err)
	}
```

同时更新 `ReferencedKeys` 的 godoc：引用来源从「四处」改为「五处」。

- [ ] **Step 5: 跑测试确认通过**

```bash
cd server && go test ./internal/repository/ -run TestObjectACL -v
```

Expected: 新增 2 个用例（含 5 个子用例）PASS，**且既有 ACL 用例全部仍 PASS**
（消息/贴纸/头像三条老分支不能被改坏）。

- [ ] **Step 6: 提交**

```bash
git add server/internal/repository/object_acl_repo.go server/internal/repository/object_acl_repo_test.go
git commit -m "feat(moments): 对象读授权与 GC 引用判定纳入朋友圈媒体"
```

---

### Task 7: admin 删帖与删评论

**Files:**

- Modify: `server/internal/service/admin_service.go`
- Modify: `server/internal/repository/admin_repo.go`
- Modify: `server/internal/handler/admin.go`
- Modify: `server/internal/router/router.go`
- Modify: `server/internal/service/admin_service_test.go`

**Interfaces:**

- Consumes: Task 1 的动作常量；Task 3 的软删方法
- Produces:
  - `func (s *AdminService) DeleteMomentPost(ctx context.Context, actorID, postID uuid.UUID) error`
  - `func (s *AdminService) DeleteMomentComment(ctx context.Context, actorID, commentID uuid.UUID) error`
  - `ErrMomentPostNotFound` / `ErrMomentCommentNotFound`（定义在 `admin_service.go`）

> **与 Task 4 的哨兵不是同一个，别合并**：`service.ErrMomentNotFound`（Task 4）表示
> 「不存在**或对调用者不可见**」，服务于普通用户路径；本任务的
> `ErrMomentPostNotFound` / `ErrMomentCommentNotFound` 只表示「行不存在或已删」——
> admin 不受可见性约束，两者语义不同。沿用既有 admin 哨兵的命名风格
> （`ErrMessageNotFound` 就在同一文件）。

> **实现位置修正**：审计日志统一由 `AdminService.audit(...)` 写入，不散到 handler
> （见 `admin_service.go:132` 的 `DeleteMessage` 范式）。因此**不新建 `admin_moments.go`**，
> 而是把两个方法加进既有 `AdminService` / `AdminHandler`，与 `DeleteMessage` 并列。

- [ ] **Step 1: 写失败测试**

追加到 `server/internal/service/admin_service_test.go`：

```go
// TestAdminDeleteMomentPost admin 删帖：内容软删 + 落审计日志。
func TestAdminDeleteMomentPost(t *testing.T) {
	db := testDB(t)
	svc := newTestAdminService(t, db) // 沿用本文件既有构造助手
	ctx := context.Background()

	admin := newTestUser(t, db, "adminmoment")
	author := newTestUser(t, db, "momentvictim")
	post := &model.MomentPost{UserID: author.ID, Content: "违规内容"}
	if err := db.Create(post).Error; err != nil {
		t.Fatalf("create post: %v", err)
	}
	t.Cleanup(func() { db.Unscoped().Delete(post) })

	if err := svc.DeleteMomentPost(ctx, admin.ID, post.ID); err != nil {
		t.Fatalf("admin delete post: %v", err)
	}

	var got model.MomentPost
	if err := db.Where("id = ?", post.ID).Take(&got).Error; err != nil {
		t.Fatalf("回查帖子: %v", err)
	}
	if got.DeletedAt == nil {
		t.Fatal("admin 删帖后 deleted_at 应非空")
	}

	var logs int64
	db.Model(&model.AdminActionLog{}).
		Where("action = ? AND target_id = ?", model.AdminActionDeleteMomentPost, post.ID.String()).
		Count(&logs)
	if logs != 1 {
		t.Fatalf("审计日志 %d 条，want 1", logs)
	}

	// 幂等性：已删的帖子再删应回 not found，不重复写审计
	if err := svc.DeleteMomentPost(ctx, admin.ID, post.ID); !errors.Is(err, ErrMomentPostNotFound) {
		t.Fatalf("重复删除应回 ErrMomentPostNotFound，got %v", err)
	}
}
```

> `newTestAdminService(t, db)` 若本文件无此助手，照现有 admin 用例的构造方式内联即可
> （`grep -n "NewAdminService" server/internal/service/admin_service_test.go`）。

- [ ] **Step 2: 跑测试确认失败**

```bash
cd server && go test ./internal/service/ -run TestAdminDeleteMoment -v
```

Expected: 编译失败 `svc.DeleteMomentPost undefined`。

- [ ] **Step 3: 实现 repository + service + handler + 路由**

`admin_repo.go` 加两个软删方法（返回 `(bool, error)`，`RowsAffected > 0` 即成功，
与既有 `DeleteMessage` 同形）：

```go
// SoftDeleteMomentPost 管理端软删朋友圈帖子；返回是否真的删到了行。
func (r *AdminRepository) SoftDeleteMomentPost(ctx context.Context, postID uuid.UUID) (bool, error) {
	res := r.db.WithContext(ctx).
		Model(&model.MomentPost{}).
		Where("id = ? AND deleted_at IS NULL", postID).
		Update("deleted_at", time.Now())
	return res.RowsAffected > 0, res.Error
}
```

评论同形（`SoftDeleteMomentComment`）。

`admin_service.go` 加两个方法，完全照 `DeleteMessage` 的形状：

```go
// DeleteMomentPost 管理端删除朋友圈帖子（软删 + 审计）。
func (s *AdminService) DeleteMomentPost(ctx context.Context, actorID, postID uuid.UUID) error {
	ok, err := s.repo.SoftDeleteMomentPost(ctx, postID)
	if err != nil {
		return fmt.Errorf("delete moment post: %w", err)
	}
	if !ok {
		return ErrMomentPostNotFound
	}
	s.audit(ctx, actorID, model.AdminActionDeleteMomentPost, "moment_post", postID.String(), nil)
	return nil
}
```

`admin.go` 加两个 handler 方法（照 `DeleteMessage`），路由注册进 `admin` 组：

```go
	admin.DELETE("/moments/:id", adminH.DeleteMomentPost)
	admin.DELETE("/moments/comments/:id", adminH.DeleteMomentComment)
```

同样注意静态段 `comments` 要在 `:id` 之前注册。

- [ ] **Step 4: 跑测试并提交**

```bash
cd server && go test ./internal/service/ -run TestAdminDeleteMoment -v
git add server/internal/service/admin_service.go server/internal/service/admin_service_test.go \
        server/internal/repository/admin_repo.go server/internal/handler/admin.go server/internal/router/router.go
git commit -m "feat(moments): 管理端删除动态与评论并留审计"
```

---

### Task 8: 个人状态后端（K11）

**Files:**

- Modify: `server/internal/service/user_service.go`
- Modify: `server/internal/handler/user.go`
- Modify: `server/internal/service/user_service_profile_test.go`

**Interfaces:**

- Consumes: Task 1 的 `User.EffectiveStatus`
- Produces:
  - `type ProfilePatch struct { Nickname, AvatarURL, Bio *string; Gender *int16; StatusEmoji, StatusText *string; StatusDuration *int64 }`
  - `func (s *UserService) UpdateProfile(ctx context.Context, userID uuid.UUID, patch ProfilePatch) (*model.User, error)`（**签名变更**）
  - `func BuildUserStatus(u *model.User, now time.Time) gin.H` 等价的 DTO 组装（见下）

- [ ] **Step 1: 把 UpdateProfile 改成 patch 结构体**

现签名是 `UpdateProfile(ctx, userID, nickname, avatarURL, bio *string, gender *int16)`。
再加三个状态字段会变成 8 个位置参数，调用点极易错位。改为结构体入参：

```go
// ProfilePatch 资料更新补丁：nil 字段表示本次不改。
type ProfilePatch struct {
	Nickname  *string
	AvatarURL *string
	Bio       *string
	Gender    *int16

	// StatusEmoji / StatusText 个人状态（K11）。传空串表示清除状态。
	StatusEmoji *string
	StatusText  *string
	// StatusDuration 状态有效秒数：0 或 nil 表示不自动清除。
	//
	// 收秒数而非绝对过期时刻：客户端时钟可能偏移，直接收 expires_at 会让
	// 快了几分钟的设备把状态写成「一设置就已过期」。「今天」这类语义由
	// 客户端按本地时区换算成秒数，服务端不猜时区。
	StatusDuration *int64
}
```

`UpdateProfile` 内部对状态的处理：

```go
	if patch.StatusEmoji != nil {
		user.StatusEmoji = *patch.StatusEmoji
	}
	if patch.StatusText != nil {
		user.StatusText = *patch.StatusText
	}
	// 只要本次提交涉及状态，就重算过期时刻
	if patch.StatusEmoji != nil || patch.StatusText != nil {
		if patch.StatusDuration != nil && *patch.StatusDuration > 0 {
			exp := time.Now().Add(time.Duration(*patch.StatusDuration) * time.Second)
			user.StatusExpiresAt = &exp
		} else {
			user.StatusExpiresAt = nil
		}
	}
```

改完后修所有调用点（`grep -rn "UpdateProfile(" server/`）。

- [ ] **Step 2: handler 侧加字段并让状态经 EffectiveStatus 出网**

`UpdateProfileRequest` 追加：

```go
	StatusEmoji    *string `json:"status_emoji" binding:"omitempty,max=16"`
	StatusText     *string `json:"status_text" binding:"omitempty,max=64"`
	StatusDuration *int64  `json:"status_duration" binding:"omitempty,min=0,max=2592000"`
```

`GetProfile` / `UpdateProfile` 现在直接 `Success(c, user)` 把整个 `model.User` 吐出去。
因 Task 1 已把状态三列打成 `json:"-"`，必须显式组装，否则前端拿不到状态：

```go
// userProfileResponse 组装本人资料响应。
//
// 状态必须经 EffectiveStatus 取——直接读字段会把已过期的状态吐给客户端。
func userProfileResponse(u *model.User) gin.H {
	emoji, text := u.EffectiveStatus(time.Now())
	return gin.H{
		"id":           u.ID,
		"nickname":     u.Nickname,
		"avatar_url":   u.AvatarURL,
		"short_id":     u.ShortID,
		"bio":          u.Bio,
		"gender":       u.Gender,
		"phone":        u.Phone,
		"email":        u.Email,
		"status_emoji": emoji,
		"status_text":  text,
	}
}
```

`GetPublicProfile` 的 `gin.H` 同样补 `status_emoji` / `status_text`（**不含** phone/email）。

> 注意：改 `GetProfile` 的响应形状会波及前端 `mapProfile` 与既有测试，
> Step 1 之前先 `grep -rn "short_id" packages/shared/src` 确认前端读的字段名，
> 保持 `phone` / `email` 等既有字段一字不改，只**新增**两个 key。

- [ ] **Step 3: 补测试**

追加到 `server/internal/service/user_service_profile_test.go`：

```go
// TestUpdateProfile_Status 设置状态与到期清除。
func TestUpdateProfile_Status(t *testing.T) {
	db := testDB(t)
	svc := newTestUserService(t, db)
	ctx := context.Background()
	u := newTestUser(t, db, "状态用户")

	emoji, text := "🌞", "在学习"
	dur := int64(3600)
	got, err := svc.UpdateProfile(ctx, u.ID, ProfilePatch{StatusEmoji: &emoji, StatusText: &text, StatusDuration: &dur})
	if err != nil {
		t.Fatalf("update: %v", err)
	}
	if got.StatusExpiresAt == nil {
		t.Fatal("传了时长应写入过期时刻")
	}
	if e, tx := got.EffectiveStatus(time.Now()); e != emoji || tx != text {
		t.Fatalf("有效状态 = (%q,%q)", e, tx)
	}
	// 过期后视为无状态
	if e, tx := got.EffectiveStatus(got.StatusExpiresAt.Add(time.Second)); e != "" || tx != "" {
		t.Fatalf("过期后应为空，got (%q,%q)", e, tx)
	}

	// 不传时长 = 不自动清除
	zero := int64(0)
	got, err = svc.UpdateProfile(ctx, u.ID, ProfilePatch{StatusEmoji: &emoji, StatusText: &text, StatusDuration: &zero})
	if err != nil {
		t.Fatalf("update: %v", err)
	}
	if got.StatusExpiresAt != nil {
		t.Fatal("时长为 0 应表示不自动清除")
	}
}
```

- [ ] **Step 4: 跑后端全量并提交**

```bash
cd server && go vet ./... && go test ./... 2>&1 | grep -E '^(ok|FAIL|---)' | head -40
```

Expected: 无 `FAIL`。（覆盖率门禁另算，见 Global Constraints 里的 covdata 说明。）

```bash
git add server/internal/service/user_service.go server/internal/handler/user.go \
        server/internal/service/user_service_profile_test.go
git commit -m "feat(moments): 个人状态字段与到期判定接入用户资料"
```

---

## 批次 B — 前端 G2

> **依赖**：批次 B 的全部 DTO 字段名、WS 帧字段名来自批次 A 的 Task 4/5/8，
> 已在那里逐字写死。B 的实现者**不得自行改名**；若发现字段名对不上，
> 以后端实际响应为准并回头修本计划，不要在前端悄悄做映射兜底。

### Task 9: shared 层 — API / store / WS 帧 / mock

**Files:**

- Create: `packages/shared/src/api/moments.ts`
- Create: `packages/shared/src/store/momentsStore.ts`
- Create: `packages/shared/src/__tests__/momentsStore.test.ts`
- Modify: `packages/shared/src/ws/chatSocket.ts`
- Modify: `packages/shared/src/hooks/useChatBootstrap.ts`
- Modify: `packages/shared/src/store/resetStores.ts`
- Modify: `packages/shared/src/mocks/handlers.ts`
- Modify: `packages/shared/src/index.ts`

**Interfaces:**

- Consumes: 后端 `/api/v1/moments/*` 全部端点与 `moments.activity` 帧
- Produces:
  - 类型：`MomentPost`、`MomentComment`、`MomentUser`、`MomentActivity`、`MomentMediaItem`
  - API：`createPost`、`fetchFeed`、`fetchUserPosts`、`fetchPost`、`deletePost`、`likePost`、`unlikePost`、`addComment`、`deleteComment`、`fetchActivities`、`markActivitiesRead`
  - Store：`useMomentsStore`，字段 `posts / nextCursor / loading / hasMore / unreadCount / activities`，动作 `loadFeed / loadMore / toggleLike / comment / removePost / removeComment / loadActivities / markRead / pushActivity`

- [ ] **Step 1: 写 API 封装**

`packages/shared/src/api/moments.ts`。**命名转换在此层完成**：后端 snake_case →
前端 camelCase，组件层只见 camelCase（与 `users.ts` 的 `mapProfile` 同惯例）：

```ts
/**
 * 朋友圈 REST API — 发布 / 信息流 / 点赞 / 评论 / 互动消息
 *
 * @description
 * 媒体字段出的是对象 key 而非签好名的 URL：调用方拿到 key 后走
 * `getDownloadUrl`（files.ts，带进程内签名缓存）换预签名 GET。
 * 服务端不逐帖签名——一页 20 帖 × 9 图会是 180 次签名。
 */
import { apiDelete, apiGet, apiPost } from "./client";

/** 帖子媒体项：图片只有 key/w/h，视频另带 thumbKey/duration。 */
export interface MomentMediaItem {
  key: string;
  thumbKey?: string;
  duration?: number;
  w: number;
  h: number;
}

/** 帖子/评论/互动里内嵌的用户摘要。 */
export interface MomentUser {
  id: string;
  nickname: string;
  avatarUrl: string;
  /** 个人状态 emoji（K11），已过期的状态服务端吐空串 */
  statusEmoji: string;
}

export interface MomentComment {
  id: string;
  user: MomentUser;
  replyToUser: MomentUser | null;
  content: string;
  createdAt: string;
}

/** 0=无媒体 1=图片 2=视频 */
export type MomentMediaKind = 0 | 1 | 2;
/** 0=好友可见 1=仅自己 */
export type MomentVisibility = 0 | 1;

export interface MomentPost {
  id: string;
  user: MomentUser;
  content: string;
  mediaKind: MomentMediaKind;
  media: MomentMediaItem[];
  visibility: MomentVisibility;
  likeCount: number;
  likedByMe: boolean;
  likes: MomentUser[];
  comments: MomentComment[];
  createdAt: string;
  /** 本人可删（本人帖）——由服务端判定，前端不自行推导 */
  deletable: boolean;
}

/** 1=点赞 2=评论 */
export type MomentActivityKind = 1 | 2;

export interface MomentActivity {
  id: string;
  kind: MomentActivityKind;
  postId: string;
  actor: MomentUser;
  commentPreview: string;
  postPreview: string;
  postThumbKey: string;
  read: boolean;
  createdAt: string;
}
```

DTO 与映射函数（逐字对应后端 json tag）：

```ts
interface MomentUserDTO {
  id: string;
  nickname: string;
  avatar_url: string;
  status_emoji: string;
}

function mapUser(d: MomentUserDTO): MomentUser {
  return { id: d.id, nickname: d.nickname, avatarUrl: d.avatar_url, statusEmoji: d.status_emoji };
}
```

其余 `mapComment` / `mapPost` / `mapActivity` 同法（字段名见 Task 4 的 DTO 结构体）。
**`media` 项的 `thumb_key` → `thumbKey` 必须映射**，漏了视频封面拿不到。

函数签名：

```ts
export async function createPost(input: {
  content: string;
  media: MomentMediaItem[];
  mediaKind: MomentMediaKind;
  visibility: MomentVisibility;
}): Promise<MomentPost>;

export async function fetchFeed(
  cursor?: string,
): Promise<{ posts: MomentPost[]; nextCursor: string }>;
export async function fetchUserPosts(
  userId: string,
  cursor?: string,
): Promise<{ posts: MomentPost[]; nextCursor: string }>;
export async function fetchPost(id: string): Promise<MomentPost>;
export async function deletePost(id: string): Promise<void>;
export async function likePost(id: string): Promise<void>;
export async function unlikePost(id: string): Promise<void>;
export async function addComment(
  postId: string,
  content: string,
  replyToUserId?: string,
): Promise<MomentComment>;
export async function deleteComment(commentId: string): Promise<void>;
export async function fetchActivities(
  cursor?: string,
): Promise<{ activities: MomentActivity[]; unreadCount: number; nextCursor: string }>;
export async function markActivitiesRead(ids?: string[]): Promise<void>;
```

- [ ] **Step 2: 写 store 的失败测试**

`packages/shared/src/__tests__/momentsStore.test.ts`：

```ts
/**
 * momentsStore 单测：分页追加、乐观点赞回滚、未读计数增减
 */
import { describe, it, expect, beforeEach, vi, afterEach } from "vitest";
import { useMomentsStore } from "../store/momentsStore";
import * as api from "../api/moments";
import type { MomentPost } from "../api/moments";

function makePost(id: string, overrides: Partial<MomentPost> = {}): MomentPost {
  return {
    id,
    user: { id: "u1", nickname: "张三", avatarUrl: "", statusEmoji: "" },
    content: "内容 " + id,
    mediaKind: 0,
    media: [],
    visibility: 0,
    likeCount: 0,
    likedByMe: false,
    likes: [],
    comments: [],
    createdAt: "2026-09-13T10:00:00Z",
    deletable: true,
    ...overrides,
  };
}

beforeEach(() => {
  useMomentsStore.setState({
    posts: [],
    nextCursor: "",
    hasMore: true,
    loading: false,
    unreadCount: 0,
    activities: [],
  });
});

afterEach(() => vi.restoreAllMocks());

describe("momentsStore", () => {
  it("loadMore 追加而非覆盖，并推进游标", async () => {
    vi.spyOn(api, "fetchFeed").mockResolvedValueOnce({ posts: [makePost("p1")], nextCursor: "c1" });
    await useMomentsStore.getState().loadFeed();
    expect(useMomentsStore.getState().posts.map((p) => p.id)).toEqual(["p1"]);

    vi.spyOn(api, "fetchFeed").mockResolvedValueOnce({ posts: [makePost("p2")], nextCursor: "" });
    await useMomentsStore.getState().loadMore();
    expect(useMomentsStore.getState().posts.map((p) => p.id)).toEqual(["p1", "p2"]);
    // 空游标 = 到底
    expect(useMomentsStore.getState().hasMore).toBe(false);
  });

  it("点赞乐观更新，失败回滚", async () => {
    useMomentsStore.setState({ posts: [makePost("p1")] });
    vi.spyOn(api, "likePost").mockRejectedValueOnce(new Error("network"));

    await useMomentsStore.getState().toggleLike("p1");

    const post = useMomentsStore.getState().posts[0];
    expect(post.likedByMe).toBe(false);
    expect(post.likeCount).toBe(0);
  });

  it("点赞成功后保持新状态", async () => {
    useMomentsStore.setState({ posts: [makePost("p1")] });
    vi.spyOn(api, "likePost").mockResolvedValueOnce(undefined);

    await useMomentsStore.getState().toggleLike("p1");

    const post = useMomentsStore.getState().posts[0];
    expect(post.likedByMe).toBe(true);
    expect(post.likeCount).toBe(1);
  });

  it("pushActivity 累加未读，markRead 归零", async () => {
    useMomentsStore.getState().pushActivity({
      id: "a1",
      kind: 2,
      postId: "p1",
      actor: { id: "u2", nickname: "李四", avatarUrl: "", statusEmoji: "" },
      commentPreview: "好看",
      postPreview: "",
      postThumbKey: "",
      read: false,
      createdAt: "2026-09-13T10:01:00Z",
    });
    expect(useMomentsStore.getState().unreadCount).toBe(1);

    vi.spyOn(api, "markActivitiesRead").mockResolvedValueOnce(undefined);
    await useMomentsStore.getState().markRead();
    expect(useMomentsStore.getState().unreadCount).toBe(0);
  });
});
```

- [ ] **Step 3: 跑测试确认失败**

```bash
LANG=C.UTF-8 pnpm --filter @yuanchat/shared test -- momentsStore
```

Expected: 模块不存在而失败。

- [ ] **Step 4: 实现 store**

`packages/shared/src/store/momentsStore.ts`。要点：

- `loadFeed()` 重置列表；`loadMore()` 用 `nextCursor` 追加，`nextCursor === ""` 时置 `hasMore=false` 并直接返回
- `loading` 期间 `loadMore` 直接 return，防重复请求
- `toggleLike(id)`：**先改本地再发请求**，catch 里回滚到操作前的快照（连 `likeCount` 一起回滚）
- `comment(postId, content, replyToUserId?)`：成功后把返回的评论 append 进该帖
- `removePost(id)` / `removeComment(postId, commentId)`：同样乐观 + 失败回滚
- `pushActivity(a)`：`unreadCount += 1` 且 `activities` 头部插入（WS 帧到达时调用）
- `markRead()`：先本地清零再发请求（红点消得要跟手），失败不回滚——下次拉列表会以服务端为准

- [ ] **Step 5: 接 WS 帧**

`chatSocket.ts` 的 `ServerFrames` 追加（字段名逐字对齐 golden 契约）：

```ts
  /** 朋友圈互动：好友点赞/评论了我的动态，只推给帖子作者 */
  "moments.activity": {
    id: string;
    kind: number;
    post_id: string;
    actor_id: string;
    actor_nickname: string;
    actor_avatar_url: string;
    comment_preview: string;
    created_at: string;
  };
```

`useChatBootstrap.ts` 的 `setHandlers` 里追加：

```ts
    "moments.activity": (p) => {
      useMomentsStore.getState().pushActivity({
        id: p.id,
        kind: p.kind === 1 ? 1 : 2,
        postId: p.post_id,
        actor: {
          id: p.actor_id,
          nickname: p.actor_nickname,
          avatarUrl: p.actor_avatar_url,
          statusEmoji: "",
        },
        commentPreview: p.comment_preview,
        postPreview: "",
        postThumbKey: "",
        read: false,
        createdAt: p.created_at,
      });
    },
```

`resetStores.ts` 的 `resetChatStores()` 里追加清空（登出后残留会让下个账号看到上个账号的红点）：

```ts
useMomentsStore.setState({
  posts: [],
  nextCursor: "",
  hasMore: true,
  loading: false,
  unreadCount: 0,
  activities: [],
});
```

- [ ] **Step 6: 补 MSW mock（四态）**

`packages/shared/src/mocks/handlers.ts` 追加 moments 全端点。必须覆盖正常/空/错误/加载：

- `GET /api/v1/moments/feed`：默认返回 3 帖（1 纯文本 + 1 单图 + 1 九图），`next_cursor: ""`
- `?scenario=empty` → `{posts: [], next_cursor: ""}`
- `?scenario=error` → `HttpResponse.json({code: 500, message: "mock error"}, {status: 500})`
- `?scenario=slow` → `await delay(2000)` 后返回正常数据（骨架屏可视）
- 其余端点返回成功空壳即可

- [ ] **Step 7: 跑测试并导出**

`packages/shared/src/index.ts` 追加 `export * from "./api/moments";` 与
`export { useMomentsStore } from "./store/momentsStore";`。

```bash
LANG=C.UTF-8 pnpm --filter @yuanchat/shared test
npx tsc --noEmit -p packages/shared
```

Expected: momentsStore 4 个用例 PASS，既有用例不回归。

- [ ] **Step 8: 提交**

```bash
git add packages/shared/src/
git commit -m "feat(moments): 前端朋友圈 API、状态管理与互动帧接线"
```

---

### Task 10: 媒体网格与帖子卡片

**Files:**

- Create: `packages/ui/src/MomentMediaGrid.tsx`
- Create: `packages/ui/src/MomentPostCard.tsx`
- Create: `packages/ui/src/__tests__/MomentMediaGrid.test.tsx`
- Create: `packages/ui/src/__tests__/MomentPostCard.test.tsx`
- Modify: `packages/ui/src/index.ts`

**Interfaces:**

- Consumes: Task 9 的 `MomentPost` / `MomentMediaItem` 类型；既有 `ImageLightbox`、`Avatar`、`getDownloadUrl`
- Produces:
  - `<MomentMediaGrid media={MomentMediaItem[]} mediaKind={MomentMediaKind} />`
  - `<MomentPostCard post={MomentPost} onLike onComment onDelete onOpenUser />`

- [ ] **Step 1: 写媒体网格测试**

`packages/ui/src/__tests__/MomentMediaGrid.test.tsx`：

```tsx
/**
 * MomentMediaGrid 布局测试：图数决定列数，视频走单卡
 */
import { describe, it, expect, vi } from "vitest";
import { render, screen } from "@testing-library/react";
import { MomentMediaGrid } from "../MomentMediaGrid";
import type { MomentMediaItem } from "@yuanchat/shared";

// 下载签名走网络，测试里桩掉
vi.mock("@yuanchat/shared", async () => {
  const actual = await vi.importActual<typeof import("@yuanchat/shared")>("@yuanchat/shared");
  return { ...actual, getDownloadUrl: vi.fn(async (key: string) => "blob:" + key) };
});

function items(n: number): MomentMediaItem[] {
  return Array.from({ length: n }, (_, i) => ({ key: `images/${i}.jpg`, w: 100, h: 100 }));
}

describe("MomentMediaGrid", () => {
  it("单图不进网格，按原始宽高比渲染", () => {
    const { container } = render(<MomentMediaGrid media={items(1)} mediaKind={1} />);
    expect(container.querySelectorAll("[data-media-cell]")).toHaveLength(1);
    expect(container.querySelector("[data-grid-cols]")?.getAttribute("data-grid-cols")).toBe("1");
  });

  it("四图走 2×2", () => {
    const { container } = render(<MomentMediaGrid media={items(4)} mediaKind={1} />);
    expect(container.querySelector("[data-grid-cols]")?.getAttribute("data-grid-cols")).toBe("2");
    expect(container.querySelectorAll("[data-media-cell]")).toHaveLength(4);
  });

  it("九图走三列", () => {
    const { container } = render(<MomentMediaGrid media={items(9)} mediaKind={1} />);
    expect(container.querySelector("[data-grid-cols]")?.getAttribute("data-grid-cols")).toBe("3");
    expect(container.querySelectorAll("[data-media-cell]")).toHaveLength(9);
  });

  it("视频渲染单卡与播放角标", () => {
    render(
      <MomentMediaGrid
        media={[{ key: "files/a.mp4", thumbKey: "images/a.jpg", duration: 12, w: 1280, h: 720 }]}
        mediaKind={2}
      />,
    );
    expect(screen.getByTestId("moment-video-card")).toBeInTheDocument();
  });

  it("无媒体渲染空", () => {
    const { container } = render(<MomentMediaGrid media={[]} mediaKind={0} />);
    expect(container.querySelectorAll("[data-media-cell]")).toHaveLength(0);
  });
});
```

- [ ] **Step 2: 跑测试确认失败，然后实现**

```bash
LANG=C.UTF-8 pnpm --filter @yuanchat/ui test -- MomentMediaGrid
```

实现要点：

- 列数：`1 → 1`、`2-3 → 列数=图数`、`4 → 2`、`5-9 → 3`；在容器上打 `data-grid-cols`
- 每格打 `data-media-cell`
- **CLS = 0**：单图用 `media[0].w/h` 算 `aspect-ratio` 直接占位；多图格子 `aspect-square`
- 图片 key → URL 走 `getDownloadUrl`，加载中显示固定尺寸骨架
- 点击开 `ImageLightbox`，传全部图与当前索引
- 视频：封面用 `thumbKey`，中心播放角标，`data-testid="moment-video-card"`
- 圆角一律 `rounded-lg`；单图最大宽 `max-w-[240px]`

- [ ] **Step 3: 写帖子卡片测试**

`packages/ui/src/__tests__/MomentPostCard.test.tsx` 覆盖：

- 文本超 6 行折叠，点「全文」展开（断言展开前后 `data-expanded` 变化）
- `deletable=false` 时不渲染删除按钮
- 点赞按钮触发 `onLike`，已赞态图标有 `data-liked="true"`
- 评论列表渲染「A 回复 B：内容」形态（`replyToUser` 非空时）
- 昵称旁渲染 `user.statusEmoji`（非空时）

- [ ] **Step 4: 实现卡片并跑测试通过**

```bash
LANG=C.UTF-8 pnpm --filter @yuanchat/ui test -- MomentPostCard MomentMediaGrid
```

- [ ] **Step 5: 提交**

```bash
git add packages/ui/src/MomentMediaGrid.tsx packages/ui/src/MomentPostCard.tsx \
        packages/ui/src/__tests__/MomentMediaGrid.test.tsx packages/ui/src/__tests__/MomentPostCard.test.tsx \
        packages/ui/src/index.ts
git commit -m "feat(moments): 朋友圈媒体网格与动态卡片组件"
```

---

### Task 11: 朋友圈三视图与路由页壳

**Files:**

- Create: `packages/ui/src/MomentsScreen.tsx`
- Create: `packages/ui/src/MomentComposeView.tsx`
- Create: `packages/ui/src/MomentActivitiesView.tsx`
- Create: `packages/ui/src/useBackTo.ts`
- Delete: `packages/ui/src/useStickerBack.ts`
- Create: `apps/web/src/pages/MomentsPage.tsx` / `MomentComposePage.tsx` / `MomentActivitiesPage.tsx` / `MomentUserPage.tsx`
- Create: `apps/desktop/src/pages/`（同名四个）
- Modify: `apps/web/src/App.tsx`、`apps/desktop/src/App.tsx`
- Modify: `packages/ui/src/index.ts`
- Create: `packages/ui/src/__tests__/MomentsScreen.test.tsx`

**Interfaces:**

- Consumes: Task 9 store；Task 10 组件；既有 `compressImage` / `extractVideoMeta` / `getUploadUrl` / `uploadToTicket`
- Produces：`<MomentsScreen />`、`<MomentComposeView />`、`<MomentActivitiesView />`、`useBackTo(target)`

- [ ] **Step 1: useStickerBack 改名为 useBackTo**

现实现与 sticker 无任何耦合（只是 `registerBackInterceptor` + `navigate(target)`），
直接改名并更新 JSDoc，然后把 **5 处** sticker 调用点同步改名（已核对）：
`StickerPackDetailView.tsx:42`、`StickerMarketView.tsx:84`、`StickerMineView.tsx:26`、
`StickerPackEditView.tsx:40`、`StickerPublishView.tsx:49`（各自的 import 行一并改）。

```bash
grep -rn "useStickerBack" packages/ apps/
```

**纯重命名，不改行为**。改完 `npx tsc --noEmit` 应无引用残留。

- [ ] **Step 2: 实现 MomentsScreen**

编排：顶栏（标题 + 铃铛按钮带未读数 + 发布按钮）+ feed 列表 + 触底加载。

- 挂载时 `loadFeed()`；滚动到底 `loadMore()`
- 空态：`t("moments.empty")` +「发一条」按钮
- 错误态：重试按钮
- 加载态：3 个固定高度骨架卡（CLS=0）
- 铃铛：`unreadCount > 0` 时显示数字角标，点击 `navigate("/moments/activities")`
- 移动端顶栏 sticky，桌面端同布局（本屏无双栏需求，三端同构）

- [ ] **Step 3: 实现 MomentComposeView**

- 文本域（限 1000 字，实时字数）
- 图片选择（≤9，超出禁用选择器）/ 视频选择（1 个），**二选一**：已选图片后视频按钮禁用，反之亦然
- 图片走 `compressImage` → `getUploadUrl(category: "images")` → `uploadToTicket`
- 视频走 `extractVideoMeta` 拿时长/宽高/封面 → 封面按 `category: "images"` 传、视频本体不传 category（`resolveCategory` 会把 `video/*` 归 `files/`）
- 逐张进度条；单张失败可单独重试，不作废整次发布
- 可见性选择（好友可见 / 仅自己）
- 提交后 `navigate("/moments")` 并 `loadFeed()` 刷新

- [ ] **Step 4: 实现 MomentActivitiesView**

- 挂载时 `loadActivities()` + `markRead()`
- 每行：头像 + 昵称 + 「赞了你的动态」/ 评论预览 + 帖子缩略图 + 时间
- 点击跳 `/moments`（v1 不做定位到具体帖，列表页已按时间倒序）
- 空态 / 加载骨架同上

- [ ] **Step 5: 路由页壳与注册**

web 与 desktop 各建四个页壳（照 `FavoritesPage.tsx` 的形状，一层薄壳套 UI 组件），
`App.tsx` 注册四条路由，desktop 侧用 `lazy(...)` 与既有项一致：

```tsx
            <Route path="/moments" element={<MomentsPage />} />
            <Route path="/moments/compose" element={<MomentComposePage />} />
            <Route path="/moments/activities" element={<MomentActivitiesPage />} />
            <Route path="/moments/user/:userId" element={<MomentUserPage />} />
```

三个子页在组件内调 `useBackTo("/moments")`；`/moments` 本身是底栏根页面，
走默认兜底，不拦截。

- [ ] **Step 6: 跑测试**

```bash
LANG=C.UTF-8 pnpm --filter @yuanchat/ui test -- Moment
npx tsc --noEmit -p apps/web && npx tsc --noEmit -p apps/desktop
```

- [ ] **Step 7: 提交**

```bash
git add packages/ui/src/ apps/web/src apps/desktop/src
git commit -m "feat(moments): 朋友圈信息流、发布页与互动消息页"
```

---

### Task 12: 导航结构调整

**Files:**

- Modify: `packages/ui/src/MainLayout.tsx`
- Modify: `packages/ui/src/SettingsScreen.tsx`
- Modify: `packages/ui/src/__tests__/MainLayout.test.tsx`
- Modify: `packages/ui/src/__tests__/SettingsScreen.test.tsx`

**Interfaces:**

- Consumes: Task 9 的 `useMomentsStore.unreadCount`
- Produces: 无新导出，只调整既有组件行为

- [ ] **Step 1: 写导航测试**

追加到 `packages/ui/src/__tests__/MainLayout.test.tsx`。注意 jsdom locale 是 en-US，
断言用英文标签：

```tsx
it("移动端底栏是 聊天/通讯录/朋友圈/设置 四项，不含收藏", () => {
  // 移动端断点由 useBreakpoint 决定，按本文件既有做法设置视口
  render(
    <MemoryRouter initialEntries={["/chat"]}>
      <MainLayout />
    </MemoryRouter>,
  );
  expect(screen.getByText("Moments")).toBeInTheDocument();
  expect(screen.queryByText("Favorites")).not.toBeInTheDocument();
});

it("朋友圈有未读互动时显示角标", () => {
  useMomentsStore.setState({ unreadCount: 3 });
  render(
    <MemoryRouter initialEntries={["/chat"]}>
      <MainLayout />
    </MemoryRouter>,
  );
  expect(screen.getByText("3")).toBeInTheDocument();
});
```

`SettingsScreen.test.tsx` 追加：

```tsx
it("收藏入口在关于之上，点击跳 /favorites 并带来源", () => {
  render(
    <MemoryRouter initialEntries={["/settings"]}>
      <SettingsScreen />
    </MemoryRouter>,
  );
  const favorites = screen.getByText("Favorites");
  const about = screen.getByText("About");
  // compareDocumentPosition: 收藏必须排在关于前面
  expect(favorites.compareDocumentPosition(about) & Node.DOCUMENT_POSITION_FOLLOWING).toBeTruthy();
});
```

- [ ] **Step 2: 改 MainLayout**

```tsx
/**
 * 移动端底栏导航项（4 项均分全宽）。
 *
 * @remarks 收藏与表情商城都不进底栏：底栏只放高频入口，5 项会把每项压到 20% 宽。
 * 两者的入口统一在设置页（见 SettingsScreen），桌面侧栏另有商城入口。
 */
const MOBILE_NAV_ITEMS = [
  { to: "/chat", icon: MessageCircle, labelKey: "chat.title" },
  { to: "/contacts", icon: Users, labelKey: "contacts.title" },
  { to: "/moments", icon: Camera, labelKey: "moments.title" },
  { to: "/settings", icon: Settings, labelKey: "settings.title" },
];

/**
 * 桌面/平板左侧栏导航项。
 *
 * 设置不在此列：它固定沉底（spacer 之后、主题/登出之上），
 * 这样以后新增导航项不必每次重排顺序。
 */
const DESKTOP_NAV_ITEMS = [
  { to: "/chat", icon: MessageCircle, labelKey: "chat.title" },
  { to: "/contacts", icon: Users, labelKey: "contacts.title" },
  { to: "/moments", icon: Camera, labelKey: "moments.title" },
  { to: "/stickers", icon: Sticker, labelKey: "sticker.market.title" },
];

/** 桌面侧栏沉底的设置项（与常规导航项同样式，仅位置不同）。 */
const DESKTOP_SETTINGS_ITEM = { to: "/settings", icon: Settings, labelKey: "settings.title" };
```

- `Camera` 从 lucide-react 引入（与既有图标同源）
- `badgeOf` 加分支：`to === "/moments" ? momentsUnread : ...`，`momentsUnread` 取自
  `useMomentsStore((s) => s.unreadCount)`
- 桌面侧栏：把导航项渲染抽成一个局部函数 `renderNavItem(item)`，`DESKTOP_NAV_ITEMS.map(renderNavItem)`
  照旧，`<div className="flex-1" />` 之后插 `renderNavItem(DESKTOP_SETTINGS_ITEM)`，
  再是主题切换与登出。**抽函数是为了设置项与常规项样式不漂移**

- [ ] **Step 3: 改 SettingsScreen 行模型**

现状：`NAV_GROUPS_TOP/BOTTOM` 是 `setView` 内部视图项，表情商城行是 `navigate` 且**只在移动端渲染**，
桌面左列渲染扁平 `NAV_GROUPS`（无商城、无收藏）。本批把收藏从侧栏移走后，
桌面端若不改就**完全没有收藏入口**。统一成带判别的单一数组：

```tsx
/** 设置页分组项：view 为内部视图切换，route 为跳走的独立页面。 */
type SettingsNavItem =
  | { kind: "view"; view: SettingsView; icon: typeof User; labelKey: string; descKey: string }
  | { kind: "route"; to: string; icon: typeof User; labelKey: string; descKey: string };

/**
 * 设置页分组顺序（移动端列表与桌面左列共用同一数组，保证两端顺序一致）。
 *
 * 收藏与表情商城是 route 项：它们是独立路由页而非设置页内部视图，
 * 跳转带 state.from 供安卓返回键回到设置页。
 */
const SETTINGS_NAV: SettingsNavItem[] = [
  {
    kind: "view",
    view: "account",
    icon: ShieldCheck,
    labelKey: "settings.account",
    descKey: "settings.accountDesc",
  },
  {
    kind: "view",
    view: "appearance",
    icon: Palette,
    labelKey: "settings.appearance",
    descKey: "settings.appearanceDesc",
  },
  {
    kind: "route",
    to: "/stickers",
    icon: Sticker,
    labelKey: "settings.stickerMarket",
    descKey: "settings.stickerMarketDesc",
  },
  {
    kind: "route",
    to: "/favorites",
    icon: Star,
    labelKey: "favorites.title",
    descKey: "settings.favoritesDesc",
  },
  {
    kind: "view",
    view: "about",
    icon: Info,
    labelKey: "settings.about",
    descKey: "settings.aboutDesc",
  },
];
```

两端渲染各自保留现有样式，点击分派：

```tsx
const onNavClick = (item: SettingsNavItem) => {
  if (item.kind === "route") navigate(item.to, { state: { from: "/settings" } });
  else setView(item.view);
};
```

桌面左列的 `active` 判定改为 `item.kind === "view" && view === item.view`（route 项永不高亮）。

- [ ] **Step 4: 跑测试**

```bash
LANG=C.UTF-8 pnpm --filter @yuanchat/ui test -- MainLayout SettingsScreen
```

Expected: 新增用例 PASS，既有用例中「renders three navigation items」等可能需同步更新
（它断言的是旧导航集合）—— 更新断言，不是删用例。

- [ ] **Step 5: 提交**

```bash
git add packages/ui/src/MainLayout.tsx packages/ui/src/SettingsScreen.tsx packages/ui/src/__tests__/
git commit -m "feat(moments): 导航加入朋友圈入口，收藏与设置位置调整"
```

---

### Task 13: 个人状态前端（K11）

**Files:**

- Create: `packages/ui/src/UserStatusEditor.tsx`
- Create: `packages/ui/src/__tests__/UserStatusEditor.test.tsx`
- Modify: `packages/shared/src/api/users.ts`
- Modify: `packages/ui/src/Avatar.tsx`
- Modify: `packages/ui/src/SettingsScreen.tsx`（hero 下方入口）
- Modify: `packages/ui/src/ChatWindow.tsx`（聊天页顶栏展示）
- Modify: `packages/ui/src/index.ts`

**Interfaces:**

- Consumes: Task 8 的 `status_emoji` / `status_text` / `status_duration`
- Produces: `<UserStatusEditor open onClose />`；`<Avatar statusEmoji?: string />`

- [ ] **Step 1: 扩展 users API**

`packages/shared/src/api/users.ts`：`PublicProfile` 加 `statusEmoji: string` / `statusText: string`，
`ProfilePatch` 加 `statusEmoji?: string` / `statusText?: string` / `statusDuration?: number`，
`UserProfileDTO` 加对应 snake_case 字段，`mapProfile` 补映射，`updateMyProfile` 补透传：

```ts
if (patch.statusEmoji !== undefined) body.status_emoji = patch.statusEmoji;
if (patch.statusText !== undefined) body.status_text = patch.statusText;
if (patch.statusDuration !== undefined) body.status_duration = patch.statusDuration;
```

- [ ] **Step 2: Avatar 加状态角标**

```tsx
  /** 个人状态 emoji（K11）：渲染在头像右下角的小圆底上；空串不渲染 */
  statusEmoji?: string;
```

尺寸随头像 size 缩放，绝对定位右下，底色用 `bg-surface` 加细边，`rounded-full`。
**不与 `presence` 圆点争位**：presence 在右下时，状态 emoji 取代它（同一位置只显示一个，
状态优先），在 JSDoc 里写明这条取舍。

- [ ] **Step 3: 写状态编辑器测试并实现**

`UserStatusEditor.test.tsx` 覆盖：emoji 选择、文本输入（限 64 字）、四档时长
（1h/4h/今天/不清除）、清除按钮、保存调用 `updateMyProfile` 且 `statusDuration` 正确。

「今天」的秒数按**本地时区**算到当日 23:59:59：

```ts
/** 「今天」剩余秒数：按用户本地时区算到当日 23:59:59（服务端不猜时区，只收秒数） */
function secondsUntilEndOfDay(now = new Date()): number {
  const end = new Date(now.getFullYear(), now.getMonth(), now.getDate(), 23, 59, 59, 999);
  return Math.max(1, Math.round((end.getTime() - now.getTime()) / 1000));
}
```

- [ ] **Step 4: 接入展示位**

- 设置页 hero 卡片下方：一行「个人状态」入口，显示当前状态或「未设置」
- 聊天页顶栏：单聊时在对方昵称旁显示 `statusEmoji + statusText`
- 朋友圈帖子昵称旁：已由 Task 10 的 `MomentPostCard` 处理

- [ ] **Step 5: 跑测试并提交**

```bash
LANG=C.UTF-8 pnpm --filter @yuanchat/ui test -- UserStatusEditor Avatar
git add packages/ui/src packages/shared/src/api/users.ts
git commit -m "feat(moments): 个人状态编辑与头像状态角标"
```

---

### Task 14: i18n 四语言补齐

**Files:**

- Modify: `packages/design-system/src/i18n/locales/{zh-CN,en-US,ja-JP,ko-KR}.json`

**Interfaces:**

- Consumes: 前面所有任务里出现的 `t("...")` key

- [ ] **Step 1: 收集本批新增的全部 key**

```bash
grep -rhno 't("moments\.[a-zA-Z.]*"' packages/ui/src apps/ | sed 's/.*t("//;s/"//' | sort -u
grep -rhno 't("settings\.\(favoritesDesc\|userStatus[a-zA-Z]*\)"' packages/ui/src | sed 's/.*t("//;s/"//' | sort -u
```

- [ ] **Step 2: 按清单补四语言**

最小 key 集（实现中如有增减以实际 `t()` 调用为准）：

| key                             | zh-CN                | en-US                    | ja-JP                | ko-KR                        |
| ------------------------------- | -------------------- | ------------------------ | -------------------- | ---------------------------- |
| `moments.title`                 | 朋友圈               | Moments                  | モーメンツ           | 모먼트                       |
| `moments.empty`                 | 还没有动态           | No moments yet           | 投稿がありません     | 게시물이 없습니다            |
| `moments.publish`               | 发布                 | Post                     | 投稿                 | 게시                         |
| `moments.compose.placeholder`   | 这一刻的想法…        | What's on your mind…     | いまの気持ち…        | 지금 생각…                   |
| `moments.visibility.friends`    | 好友可见             | Friends                  | 友だちに公開         | 친구 공개                    |
| `moments.visibility.private`    | 仅自己               | Only me                  | 自分のみ             | 나만 보기                    |
| `moments.like`                  | 赞                   | Like                     | いいね               | 좋아요                       |
| `moments.comment`               | 评论                 | Comment                  | コメント             | 댓글                         |
| `moments.delete`                | 删除                 | Delete                   | 削除                 | 삭제                         |
| `moments.deleteConfirm`         | 删除这条动态？       | Delete this moment?      | この投稿を削除？     | 이 게시물을 삭제할까요?      |
| `moments.activities`            | 互动消息             | Activity                 | お知らせ             | 알림                         |
| `moments.activityLike`          | 赞了你的动态         | liked your moment        | いいねしました       | 회원님의 게시물을 좋아합니다 |
| `moments.activityEmpty`         | 暂无互动             | No activity yet          | お知らせはありません | 활동이 없습니다              |
| `moments.expand`                | 全文                 | More                     | 全文                 | 더 보기                      |
| `moments.collapse`              | 收起                 | Less                     | 折りたたむ           | 접기                         |
| `moments.replyTo`               | 回复                 | Reply to                 | 返信                 | 답장                         |
| `moments.loadFailed`            | 加载失败             | Failed to load           | 読み込み失敗         | 불러오기 실패                |
| `moments.publishFailed`         | 发布失败             | Failed to post           | 投稿に失敗           | 게시 실패                    |
| `moments.mediaExclusive`        | 图片和视频只能选一种 | Choose images or a video | 画像か動画のどちらか | 이미지 또는 동영상 중 하나   |
| `settings.favoritesDesc`        | 保存的消息与图片     | Saved messages           | 保存したメッセージ   | 저장된 메시지                |
| `settings.userStatus`           | 个人状态             | Status                   | ステータス           | 상태                         |
| `settings.userStatusUnset`      | 未设置               | Not set                  | 未設定               | 설정 안 함                   |
| `settings.userStatusClear`      | 清除状态             | Clear status             | ステータスを消す     | 상태 지우기                  |
| `settings.userStatusDuration1h` | 1 小时               | 1 hour                   | 1 時間               | 1시간                        |
| `settings.userStatusDuration4h` | 4 小时               | 4 hours                  | 4 時間               | 4시간                        |
| `settings.userStatusToday`      | 今天                 | Today                    | 今日                 | 오늘                         |
| `settings.userStatusNever`      | 不清除               | Don't clear              | 消さない             | 지우지 않음                  |

- [ ] **Step 3: 跑 i18n 门禁**

```bash
node scripts/check-i18n.mjs
```

Expected: PASS。该脚本三层校验：四语言对账、代码→locale、locale→代码（死键）。
**新增了 key 却没在代码里用会判死键失败**，反之亦然。

- [ ] **Step 4: 提交**

```bash
git add packages/design-system/src/i18n/locales/
git commit -m "feat(moments): 补齐朋友圈与个人状态四语言文案"
```

---

## 批次 C — 收尾（主会话执行）

### Task 15: presence 集成用例去 Skip 与债条目更正

**Files:**

- Modify: `server/internal/ws/presence_integration_test.go`
- Modify: `docs/MASTER_PLAN.md`

> **前提已核实**：MASTER_PLAN 第 597 行登记的「`handler` 包 Redis 用例仍 `t.Skipf`」**已过时**。
> 全仓 `t.Skip` 现只剩三处：`ws/presence_integration_test.go:18`（dev Redis :6380 不可达即跳过，
> **可迁 miniredis**）、`storage/minio_test.go:33` 与 `handler/file_test.go:279,287`（MinIO 集成，
> miniredis 帮不上）。handler 包的 Redis 用例（captcha）早已迁完。
> 所以本任务做的是 ws 那一条 + 把账改对。

- [ ] **Step 1: 迁移 presence 集成用例**

`presence_integration_test.go` 的 `testRedis(t)` 改为走 miniredis（v2.38.0 支持 Pub/Sub）：

```go
// testRedis 返回 miniredis 支撑的客户端。
//
// 原先连本机 dev Redis(:6380)，不可达就整批 skip——CI 里这三个跨实例用例
// 从来没真正跑过。miniredis 支持 Pub/Sub，足以覆盖本文件的发布/订阅语义。
func testRedis(t *testing.T) *redis.Client {
	t.Helper()
	rdb, _ := testutil.NewRedis(t)
	return rdb
}
```

import 换成 `"github.com/yuanchat/server/internal/testutil"`，删掉 `t.Skipf` 分支。

- [ ] **Step 2: 跑用例确认真的执行了**

```bash
cd server && go test ./internal/ws/ -run TestRedisPresence -v -count=1
```

Expected: 三个用例 **PASS**（不是 SKIP）。若出现 `unknown command SUBSCRIBE` 之类，
说明 miniredis 版本不支持该用法 —— 那就保留 skip 并在用例注释里写明真实原因，
同时把 MASTER_PLAN 条目改成「MinIO/Redis 集成用例需真实依赖，CI 不覆盖」，不要硬凑。

- [ ] **Step 3: 更正 MASTER_PLAN 债条目**

把第 597 行那条 ⚪ 改为 ✅ 并写清事实：

```markdown
| ✅ | Redis 用例 `t.Skipf` | 已修（2026-09-13）：captcha 早于 A8 迁完，本批把 `ws/presence_integration_test.go` 也迁到 miniredis；全仓仅剩 MinIO 集成用例（`storage/minio_test.go`、`handler/file_test.go`）仍需真实对象存储，另记 |
```

- [ ] **Step 4: 提交**

```bash
git add server/internal/ws/presence_integration_test.go docs/MASTER_PLAN.md
git commit -m "test(ws): presence 跨实例用例改用 miniredis 不再跳过"
```

---

### Task 16: E2E、本地 CI 全量、真机实测与合库

**Files:**

- Create: `apps/web/e2e/moments.spec.ts`
- Modify: `docs/MASTER_PLAN.md`
- Modify: `docs/superpowers/specs/2026-09-13-moments-design.md`（如实现中有偏离，回写）

- [ ] **Step 1: 写 E2E**

`apps/web/e2e/moments.spec.ts`，照 `favorites.spec.ts` 的形状（MSW 兜底，不依赖真实后端）：

```ts
/**
 * 朋友圈 E2E
 *
 * @description
 * 覆盖：页面可达、feed 渲染、发布页可进入、互动消息页可进入。
 * 不测真实发布链路（依赖对象存储，CI 无真实服务）。
 */
import { test, expect } from "@playwright/test";
import { setAuth } from "./fixtures/auth.fixture";
import { waitForMSW } from "./utils/msw";

test.describe("Moments", () => {
  test.beforeEach(async ({ page }) => {
    await setAuth(page);
    await waitForMSW(page);
    await page.goto("/moments");
  });

  test("朋友圈页可访问且渲染动态", async ({ page }) => {
    await expect(page).toHaveURL(/\/moments/);
    await expect(page.getByText("朋友圈")).toBeVisible();
  });

  test("可进入发布页", async ({ page }) => {
    await page.getByRole("button", { name: "发布" }).click();
    await expect(page).toHaveURL(/\/moments\/compose/);
  });

  test("可进入互动消息页", async ({ page }) => {
    await page.goto("/moments/activities");
    await expect(page).toHaveURL(/\/moments\/activities/);
  });
});
```

另在 `navigation.spec.ts` 或新用例里断言底栏含朋友圈、不含收藏。

- [ ] **Step 2: 本地 CI 全量（推 dev 前的强制门禁）**

逐条跑，全绿才继续：

```bash
node scripts/check-i18n.mjs
LANG=C.UTF-8 pnpm test
pnpm turbo typecheck
pnpm --filter @yuanchat/web test:e2e
cd server && go vet ./... && go test ./... && go test -race ./internal/ws/
```

Expected: 全部无 FAIL。注意 Go 覆盖率命令的退出码不可信（见 Global Constraints）。

- [ ] **Step 3: 三端真机实测**

每段测完立即关停该端链路，再起下一端（见仓库实测纪律）：

1. **Web + 桌面**：A 发图文帖 → B 的 feed 可见 → B 点赞 + 评论 → A 侧导航红点亮、
   互动消息页有两条 → A 删帖 → B 刷新后消失、互动消息同步消失
2. **图片可读性**（本批最易炸的一项）：B 侧图片**必须能正常显示**。若出现裂图，
   去查 `CanRead` 的 moments 分支（Task 6）
3. **Android 模拟器 + adb**：底栏四项布局不挤、九宫格不溢出、Lightbox 可关、
   发布页软键盘不遮输入框、从设置进收藏按返回键回设置页
4. **个人状态**：设置状态 → 聊天页顶栏与朋友圈昵称旁可见 → 选 1 小时后到期消失
   （可临时把时长调成 60 秒验证）

- [ ] **Step 4: 回写 MASTER_PLAN**

- G1 / G2 标 ✅ 并写明交付面（含日期 2026-09-13）
- K11 标 ✅，**并把「presence 协议扩展 + 会话列表角标」这个备注改掉** —— 实际实现是
  users 表新列 + 读时过期判定，没动 presence 协议，也没加会话列表角标
- 新增债登记（来自 spec 第 13 节）：
  - ⚪ 朋友圈分组可见性
  - ⚪ 仅共同好友可见评论
  - ⚪ 帖子转发到会话、评论区 @ 提及
  - 🟡 GC 未回收「软删超 N 天」的朋友圈媒体（Task 6 的刻意不对称，需独立策略）
  - ⚪ 朋友圈独立 admin 审核页（当前复用 flagged UGC 队列，无帖子预览）
  - ⚪ 个人状态无历史/常用状态快选

- [ ] **Step 5: 合并到 dev**

```bash
git add -A && git commit -m "docs(moments): 回写实施进度与新增技术债"
git checkout dev
git merge --no-ff feature/moments
```

**禁止 squash**（保留每个独立 commit）。合并后再跑一次 Step 2 的全量，确认合并没引入问题。
推送 dev 前如需打 tag / 合 main，须当次单独征询用户同意。

---

## 自检清单（实现者每个任务结束时过一遍）

- [ ] 新增/修改的导出符号都有 JSDoc / godoc
- [ ] 注释全中文，无进度或批次字样
- [ ] 无硬编码 UI 文案，`node scripts/check-i18n.mjs` 绿
- [ ] 圆角未超 `rounded-lg`
- [ ] 加载态有固定尺寸骨架（CLS=0）
- [ ] 新接口有 MSW mock 的四态
- [ ] commit message 无版本号前缀
- [ ] 所有可见性判定都经 `VisiblePostsScope`，没有第二套判定
