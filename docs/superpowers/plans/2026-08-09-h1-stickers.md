# H1 表情收藏 + 表情包 Implementation Plan（自定义收藏 + 官方表情包）

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

> **2026-08-21 状态：Task 1-9 已实现，Task 10 约 40%（i18n key 名 / MSW mock / E2E 有缺口）。**
> 审计出 36 项待修，见 [`2026-08-21-h1-stickers-audit-fixes.md`](./2026-08-21-h1-stickers-audit-fixes.md)，
> 修完方可合并 dev。

**Goal:** 扩展现有 EmojiPicker，加「收藏」「官方表情包」两个 tab；图片消息菜单加「添加到表情」一键收藏；贴纸走独立消息类型渲染为无气泡裸图，发送即所见，双端一致，hash 去重不重复收藏。

**Architecture:** 新增 `MessageTypeSticker=8` 独立消息类型（不复用 image，语义清晰，渲染/搜索/审核天然与其他类型隔离）。贴纸对象复用现有图片直传/预签名下载基础设施（MinIO `images/` 前缀），仅新增 `sticker_packs`/`stickers` 两张元数据表记录"谁收藏了哪个对象"。发送链路走 WS `message.send`（贴纸不是 REST，与文本/图片同一通道），`ws/protocol.go` 的 `ContentPayload` 加 `sticker_id` 字段，`buildContent` 加 `case "sticker"`。前端 `packages/ui` + `packages/shared` 是 web/desktop/Android（Tauri）三端共用代码，一次实现三端生效；`apps/admin` 消息审核列表补贴纸类型标签。

**Tech Stack:** Go 1.25 + gin + gorm + goose 迁移 + MinIO（minio-go v7）；PostgreSQL；WebSocket 自研信令；前端 React + zustand + react-i18next + lucide-react；Vitest + Playwright。

## Global Constraints

- **GitFlow**：`feature/h1-stickers` 分支开发；合并 dev 用 `git merge --no-ff`（**禁止 squash**，保留每个独立 commit）；dev→main 与合并 dev 均须当次征询用户。
- **commit message**：不写版本号前缀；**不加 `Co-Authored-By` trailer**；完成完整批次再提交，内部按 task 逐个 commit。
- **i18n**：UI 文案零硬编码，走 react-i18next；四语言 `zh-CN / en-US / ja-JP / ko-KR` 全量 key；过 `node scripts/check-i18n.mjs`。
- **浏览器兼容**：`build.target=es2019`（Chrome 74 WebView）；禁用 es2020+ 运行时 API（`Object.fromEntries` / `.flat` / `.replaceAll` / `Promise.allSettled` / `globalThis` / `.at()`）；`?.`/`??` 可用；`crypto.subtle` 是 Web Crypto API 标准接口，Chrome 74（2019）已支持，可用。
- **UI 圆角**：上限 `rounded-lg`(8px)，禁 `xl`/`2xl`。
- **幽灵依赖**：包内 import 的库必须在该包自己的 package.json 声明；`packages/shared` 不得 import `lucide-react`/`react-i18next`；本次新代码不新增任何依赖（图标用已声明的 `lucide-react`，hash 用浏览器原生 `crypto.subtle`）。
- **三端覆盖**：`packages/ui`/`packages/shared` 的改动同时覆盖 `apps/web`（浏览器）、`apps/desktop`（Windows/macOS/Linux 桌面 + Tauri Android 打包，即"手机端"）——不存在独立的 `apps/mobile`，Android 是 desktop 的 Tauri 构建目标。`apps/admin` 按需更新（本批：消息审核列表贴纸类型标签）。
- **数据库**：改动走 goose 迁移，下一个编号 `011`；语句幂等；Up/Down 配对。
- **测试**：集成测连 dev 库（`localhost:5434`，不可达 `t.Skip`）；MinIO 集成测连 dev MinIO，不可达 `t.Skip`（照 `file_test.go`/`minio_test.go` 既有模式）；E2E 用 playwright 浏览器实测（chromium 二进制若下载受阻，改用系统 Chrome `channel:"chrome"` 临时 config，跑完删除，仓库 config 不改）。
- **执行方式**：本批前后端合一个 agent 一次完成（用户 2026-08-09 指示，见记忆 `feedback-batch-subagent-granularity`），内部仍按下列 task 顺序逐一 TDD + 逐一 commit；完成后一次整批审查 + fix loop；再终审 + 全量门禁 + 征询合并。

## 决策点（已定，用户已选）

- **贴纸消息类型**：新增独立 `MessageTypeSticker=8`（非复用 image + 标记）。
- **官方表情包种子素材**：占位符表情——服务端用 Go `image`/`image/png` 标准库生成纯色圆形 PNG（无需外部素材/网络下载），后续有真实素材再替换 seed 逻辑，不阻塞本批开发。

## File Structure

**后端（server/）：**

- Create: `server/internal/database/migrations/011_h1_stickers.sql` — `sticker_packs` + `stickers` 两张表
- Modify: `server/internal/model/message.go` — `MessageTypeSticker int16 = 8`
- Create: `server/internal/model/sticker.go` — `Sticker` + `StickerPack` 模型
- Modify: `server/internal/ws/protocol.go` — `ContentPayload` 加 `StickerID`
- Modify: `server/internal/ws/handler.go` — `buildContent` 加 `case "sticker"`
- Modify: `server/internal/storage/minio.go` — 加 `PutObject`（供 seed 直传）
- Create: `server/internal/repository/sticker_repo.go`
- Create: `server/internal/service/sticker_service.go`
- Create: `server/internal/handler/sticker.go`
- Modify: `server/internal/router/router.go` — 装配 + 4 条路由 + offlinePush 贴纸文案分支
- Modify: `server/internal/service/conversation_service.go` — `previewOf` 加贴纸分支
- Modify: `server/cmd/seed/main.go` — 官方表情包 seed（占位符圆形 PNG）
- Modify: `apps/admin/src/pages/MessageAudit.tsx` — `TYPE_KEY` 加贴纸标签
- Modify: `docs/CHAT_API.md` — 四端点文档 + WS 帧字段说明
- Test: `server/internal/service/sticker_service_test.go`、`server/internal/ws/handler_test.go`（贴纸分支追加）

**前端：**

- Create: `packages/shared/src/api/stickers.ts`
- Modify: `packages/shared/src/api/files.ts` — 加 `hashBlob`
- Modify: `packages/shared/src/api/chat.ts` — `kindMap[8]` + `parseStickerContent`
- Modify: `packages/shared/src/store/messageStore.ts` — `ChatMessageKind` 加 `"sticker"`、`ChatMessage.sticker` 字段、`sendSticker` action
- Modify: `packages/shared/src/hooks/useChatBootstrap.ts` — `message.receive` 贴纸分支
- Modify: `packages/shared/src/ws/chatSocket.ts` — `content.sticker_id` 字段类型
- Modify: `packages/shared/src/index.ts` — 导出 `stickers.ts`
- Create: `packages/ui/src/StickerImage.tsx`
- Modify: `packages/ui/src/MessageBubble.tsx` — 贴纸渲染分支 + "添加到表情"菜单项
- Modify: `packages/ui/src/ChatWindow.tsx` — 添加到表情接线（取图 blob → hash → POST）
- Modify: `packages/ui/src/EmojiPicker.tsx` — 收藏/官方包 tab
- Modify: `packages/ui/src/Composer.tsx` — `onPickSticker` 接线
- Modify: `packages/ui/src/index.ts` — 导出 `StickerImage`
- Modify: `packages/design-system/src/i18n/locales/{zh-CN,en-US,ja-JP,ko-KR}.json` — 新 key
- Test: `packages/shared/src/__tests__/{stickers,chatApi,messageStore}.test.ts`、`packages/ui/src/__tests__/{MessageBubble,EmojiPicker}.test.tsx`、`apps/web/e2e/stickers.spec.ts`

---

## Task 1: 迁移 011 + 消息类型常量 + 模型

**Files:**

- Create: `server/internal/database/migrations/011_h1_stickers.sql`
- Modify: `server/internal/model/message.go`
- Create: `server/internal/model/sticker.go`
- Test: `server/internal/service/sticker_service_test.go`（建 setup + 占位断言）

**Interfaces:**

- Produces: `model.MessageTypeSticker int16 = 8`；`model.Sticker{ID, PackID *uuid.UUID, OwnerID *uuid.UUID, ObjectKey string, Width, Height int, ContentHash string, CreatedAt time.Time}`；`model.StickerPack{ID, Name string, CoverURL *string, IsOfficial bool, Sort int, CreatedAt time.Time}`

- [ ] **Step 1: 写迁移文件**

`server/internal/database/migrations/011_h1_stickers.sql`：

```sql
-- +goose Up
-- +goose StatementBegin
CREATE TABLE IF NOT EXISTS sticker_packs (
    id          UUID         PRIMARY KEY DEFAULT gen_random_uuid(),
    name        VARCHAR(64)  NOT NULL,
    cover_url   VARCHAR(500),
    is_official BOOLEAN      NOT NULL DEFAULT FALSE,
    sort        INTEGER      NOT NULL DEFAULT 0,
    created_at  TIMESTAMPTZ  NOT NULL DEFAULT NOW()
);
CREATE TABLE IF NOT EXISTS stickers (
    id           UUID         PRIMARY KEY DEFAULT gen_random_uuid(),
    pack_id      UUID         REFERENCES sticker_packs(id) ON DELETE CASCADE,
    owner_id     UUID         REFERENCES users(id) ON DELETE CASCADE,
    object_key   VARCHAR(255) NOT NULL,
    width        INTEGER      NOT NULL,
    height       INTEGER      NOT NULL,
    content_hash VARCHAR(64)  NOT NULL,
    created_at   TIMESTAMPTZ  NOT NULL DEFAULT NOW(),
    CONSTRAINT idx_stickers_owner_hash UNIQUE(owner_id, content_hash)
);
CREATE INDEX IF NOT EXISTS idx_stickers_pack ON stickers(pack_id);
CREATE INDEX IF NOT EXISTS idx_stickers_owner ON stickers(owner_id, created_at DESC);
-- +goose StatementEnd

-- +goose Down
-- +goose StatementBegin
DROP TABLE IF EXISTS stickers;
DROP TABLE IF EXISTS sticker_packs;
-- +goose StatementEnd
```

（`UNIQUE(owner_id, content_hash)`：Postgres 唯一约束视 NULL 互不相等，官方包 `owner_id=NULL` 的多条贴纸不受此约束影响；用户收藏 `owner_id` 非空时才真正生效去重。）

- [ ] **Step 2: 消息类型常量 + 模型**

`model/message.go` 常量块追加：

```go
	// MessageTypeSticker 贴纸消息（收藏表情/官方表情包），独立类型不复用 image：
	// 渲染无气泡裸图、不进搜索索引（与 image 现状一致，靠 content 无 text 字段自然过滤）、
	// 不进内容审核（同 image/file/voice，审核仅对 MessageTypeText 生效）。
	MessageTypeSticker int16 = 8
```

`server/internal/model/sticker.go`（新文件）：

```go
package model

import (
	"time"

	"github.com/google/uuid"
)

// StickerPack 表情包（官方预置或未来第三方来源）。
type StickerPack struct {
	ID         uuid.UUID `gorm:"type:uuid;primaryKey;default:gen_random_uuid()" json:"id"`
	Name       string    `gorm:"type:varchar(64);not null" json:"name"`
	CoverURL   *string   `gorm:"type:varchar(500)" json:"cover_url,omitempty"`
	IsOfficial bool      `gorm:"not null;default:false" json:"is_official"`
	Sort       int       `gorm:"not null;default:0" json:"sort"`
	CreatedAt  time.Time `json:"created_at"`
}

func (StickerPack) TableName() string { return "sticker_packs" }

// Sticker 单张贴纸：属于某个表情包（PackID 非空，官方/公共），
// 或属于某用户的个人收藏（OwnerID 非空，PackID 为空）——两者互斥，不会同时非空。
type Sticker struct {
	ID          uuid.UUID  `gorm:"type:uuid;primaryKey;default:gen_random_uuid()" json:"id"`
	PackID      *uuid.UUID `gorm:"type:uuid" json:"pack_id,omitempty"`
	OwnerID     *uuid.UUID `gorm:"type:uuid" json:"owner_id,omitempty"`
	ObjectKey   string     `gorm:"type:varchar(255);not null" json:"object_key"`
	Width       int        `gorm:"not null" json:"width"`
	Height      int        `gorm:"not null" json:"height"`
	ContentHash string     `gorm:"type:varchar(64);not null" json:"content_hash"`
	CreatedAt   time.Time  `json:"created_at"`
}

func (Sticker) TableName() string { return "stickers" }
```

- [ ] **Step 3: 应用迁移 + 建测试 setup**

Run: `cd server && go run ./cmd/migrate up && go run ./cmd/migrate status`
Expected: 版本推进到 11，两张表存在。

`sticker_service_test.go` 建 setup（照 `favorite`/A6/A7 既有 `testDB` 兜底模式）：

```go
package service

import (
	"testing"

	"github.com/yuanchat/server/internal/model"
)

// TestH1TablesExist 兜底确认 011 新表可用（AutoMigrate 补建）。
func TestH1TablesExist(t *testing.T) {
	db := testDB(t)
	if err := db.AutoMigrate(&model.StickerPack{}, &model.Sticker{}); err != nil {
		t.Fatalf("automigrate sticker tables: %v", err)
	}
}
```

- [ ] **Step 4: 跑测试**

Run: `cd server && go test ./internal/service/ -run TestH1TablesExist -v`
Expected: PASS（连库；不可达则 SKIP）。

- [ ] **Step 5: Commit**

```bash
git add server/internal/database/migrations/011_h1_stickers.sql server/internal/model/message.go server/internal/model/sticker.go server/internal/service/sticker_service_test.go
git commit -m "feat(server): 迁移 011 表情包/贴纸表 + MessageTypeSticker 常量"
```

---

## Task 2: WS 协议扩展 + 贴纸发送落库

**Files:**

- Modify: `server/internal/ws/protocol.go`
- Modify: `server/internal/ws/handler.go`
- Modify: `server/internal/service/conversation_service.go`
- Modify: `server/internal/router/router.go`（offlinePush 文案分支）
- Test: `server/internal/ws/handler_test.go`（贴纸分支追加，若无现成 `buildContent` 单测文件则新建 `server/internal/ws/build_content_test.go`）

**Interfaces:**

- Consumes: `model.MessageTypeSticker`（Task 1）
- Produces: `ContentPayload.StickerID string`（json:"sticker_id,omitempty"）；`buildContent` 新分支返回 `(model.MessageTypeSticker, contentJSON, true)`，`contentJSON` 结构 `{sticker_id, key, width, height}`

- [ ] **Step 1: 写失败测试**

先看现有 buildContent 测试组织方式：

```bash
grep -rn "buildContent" /home/liaojie1314/code/project/yuanchat/server/internal/ws/*_test.go
```

若已有覆盖 image 分支的测试文件，在同文件追加；否则新建 `server/internal/ws/build_content_test.go`：

```go
package ws

import (
	"encoding/json"
	"testing"

	"github.com/yuanchat/server/internal/model"
)

// TestBuildContentSticker 贴纸分支：字段齐全时落库为 MessageTypeSticker，
// content JSON 含 sticker_id/key/width/height；字段缺失时报 400 且 ok=false。
func TestBuildContentSticker(t *testing.T) {
	h := &Handler{}
	c := &Client{send: make(chan []byte, 4)}

	p := &SendPayload{
		Content: ContentPayload{
			Type: "sticker", StickerID: "s1", Key: "images/2026/08/abc.png", Width: 96, Height: 96,
		},
	}
	msgType, contentJSON, ok := h.buildContent(c, p)
	if !ok {
		t.Fatalf("expected ok, got error frame")
	}
	if msgType != model.MessageTypeSticker {
		t.Fatalf("want MessageTypeSticker(8), got %d", msgType)
	}
	var parsed struct {
		StickerID string `json:"sticker_id"`
		Key       string `json:"key"`
		Width     int    `json:"width"`
		Height    int    `json:"height"`
	}
	if err := json.Unmarshal([]byte(contentJSON), &parsed); err != nil {
		t.Fatalf("unmarshal content: %v", err)
	}
	if parsed.StickerID != "s1" || parsed.Key != p.Content.Key || parsed.Width != 96 || parsed.Height != 96 {
		t.Fatalf("content mismatch: %+v", parsed)
	}
}

// TestBuildContentStickerMissingFields 缺字段时报 400，不落库。
func TestBuildContentStickerMissingFields(t *testing.T) {
	h := &Handler{}
	c := &Client{send: make(chan []byte, 4)}
	p := &SendPayload{Content: ContentPayload{Type: "sticker"}}
	_, _, ok := h.buildContent(c, p)
	if ok {
		t.Fatal("expected ok=false for missing sticker fields")
	}
	select {
	case data := <-c.send:
		var env Envelope
		if err := json.Unmarshal(data, &env); err != nil || env.Type != TypeError {
			t.Fatalf("expected error frame, got %s", data)
		}
	default:
		t.Fatal("expected an error frame to be queued")
	}
}
```

（`Handler{}`/`Client{send: ...}` 零值构造照抄仓库现有 `buildContent`/`sendError` 单测模式；若发现字段名不符实际 struct，按 `Client`/`Handler` 真实定义调整字段。）

- [ ] **Step 2: 跑测试确认失败**

Run: `cd server && go test ./internal/ws/ -run TestBuildContentSticker -v`
Expected: 编译失败（`ContentPayload.StickerID`/`case "sticker"` 不存在）。

- [ ] **Step 3: ContentPayload 加字段**

`ws/protocol.go` `ContentPayload` 加（`Duration` 行后）：

```go
	StickerID string `json:"sticker_id,omitempty"` // sticker: 贴纸 ID（关联 stickers 表）
```

- [ ] **Step 4: buildContent 加贴纸分支**

`ws/handler.go` `buildContent`，在 `case "image":` 分支后追加：

```go
	case "sticker":
		if p.Content.StickerID == "" || p.Content.Key == "" || p.Content.Width <= 0 || p.Content.Height <= 0 {
			c.sendError(400, "sticker content requires sticker_id/key/width/height", p.ClientMsgID)
			return 0, "", false
		}
		raw, err := json.Marshal(struct {
			StickerID string `json:"sticker_id"`
			Key       string `json:"key"`
			Width     int    `json:"width"`
			Height    int    `json:"height"`
		}{p.Content.StickerID, p.Content.Key, p.Content.Width, p.Content.Height})
		if err != nil {
			c.sendError(400, "invalid sticker content", p.ClientMsgID)
			return 0, "", false
		}
		return model.MessageTypeSticker, string(raw), true
```

- [ ] **Step 5: 列表预览 + 离线推送文案分支**

`conversation_service.go` `previewOf` 的 `switch m.MessageType` 加：

```go
	case model.MessageTypeSticker:
		return "[表情]"
```

`router.go` 的 offlinePush 闭包 `switch info.ContentType` 加：

```go
		case "sticker":
			body = "[表情]"
```

- [ ] **Step 6: 跑测试**

Run: `cd server && go test ./internal/ws/ -run TestBuildContentSticker -v && go build ./... && go vet ./...`
Expected: 两个新测试 PASS，build/vet 干净。

- [ ] **Step 7: Commit**

```bash
git add server/internal/ws/protocol.go server/internal/ws/handler.go server/internal/service/conversation_service.go server/internal/router/router.go server/internal/ws/build_content_test.go
git commit -m "feat(server): WS 贴纸发送分支（ContentPayload.sticker_id + buildContent）"
```

---

## Task 3: MinIO 直传扩展 + 官方表情包 seed

**Files:**

- Modify: `server/internal/storage/minio.go`
- Modify: `server/cmd/seed/main.go`
- Test: `server/internal/storage/minio_test.go`（`PutObject` 追加用例，照既有 `t.Skip` 模式）

**Interfaces:**

- Produces: `(*storage.Storage) PutObject(ctx, objectKey, contentType string, reader io.Reader, size int64) error`

- [ ] **Step 1: 写失败测试**

`minio_test.go` 追加（若无现成文件按既有 `storage.New(...) → t.Skip` 模式新建）：

```go
// TestPutObjectRoundTrip 直传对象后可通过 PresignGet 换到可用下载 URL（不校验字节内容，
// 仅验证写入不报错且对象确实可被后续读取路径感知）。
func TestPutObjectRoundTrip(t *testing.T) {
	st, err := New(minioTestConfig(t))
	if err != nil {
		t.Skipf("dev minio unavailable, skip integration test: %v", err)
	}
	key := "images/2026/08/" + uuid.NewString() + ".png"
	data := []byte{0x89, 0x50, 0x4e, 0x47} // PNG magic bytes 占位，无需真实图像内容
	if err := st.PutObject(context.Background(), key, "image/png", bytes.NewReader(data), int64(len(data))); err != nil {
		t.Fatalf("put object: %v", err)
	}
	if _, err := st.PresignGet(context.Background(), key, time.Minute); err != nil {
		t.Fatalf("presign get after put: %v", err)
	}
}
```

（`minioTestConfig(t)` 若不存在，照 `file_test.go`/既有 minio 测试里构造 `config.MinIOConfig` 的方式取值，通常复用 `config.Load("../../config/config.yaml")` 或硬编码 dev 值——按仓库既有做法调整，不新造配置源。）

- [ ] **Step 2: 跑测试确认失败**

Run: `cd server && go test ./internal/storage/ -run TestPutObjectRoundTrip -v`
Expected: 编译失败（`PutObject` 未定义）。

- [ ] **Step 3: 实现 PutObject**

`storage/minio.go` 追加（`PresignGet` 后）：

```go
// PutObject 服务端直接写入对象（供内部工具如 seed 使用；
// 业务上传路径统一走 PresignPut 预签名直传，不经服务端中转字节）。
func (s *Storage) PutObject(ctx context.Context, objectKey, contentType string, reader io.Reader, size int64) error {
	_, err := s.client.PutObject(ctx, s.bucket, objectKey, reader, size, minio.PutObjectOptions{ContentType: contentType})
	return err
}
```

（文件顶部 import 块加 `"io"`。）

- [ ] **Step 4: 跑测试确认通过**

Run: `cd server && go test ./internal/storage/ -run TestPutObjectRoundTrip -v`
Expected: PASS（或 MinIO 不可达时 SKIP）。

- [ ] **Step 5: 官方表情包 seed（占位符圆形 PNG）**

`server/cmd/seed/main.go` 顶部 import 追加 `"bytes"`、`"image"`、`"image/color"`、`"image/png"`、`"github.com/yuanchat/server/internal/storage"`；在 `main()` 内 `ensureUsers` 之后追加调用（保持 seed 现有幂等风格）：

```go
// ensureOfficialStickers 幂等创建 1 套占位符官方表情包（8 张纯色圆形 PNG）。
// 已存在同名官方包时跳过——真实素材就绪后可替换本函数生成逻辑，无需改调用方。
func ensureOfficialStickers(ctx context.Context, db *gorm.DB, st *storage.Storage) error {
	const packName = "默认表情"
	var existing model.StickerPack
	err := db.Where("name = ? AND is_official = ?", packName, true).First(&existing).Error
	if err == nil {
		log.Printf("official sticker pack %q already exists, skip", packName)
		return nil
	}
	if !errors.Is(err, gorm.ErrRecordNotFound) {
		return fmt.Errorf("check existing pack: %w", err)
	}

	pack := &model.StickerPack{Name: packName, IsOfficial: true, Sort: 0}
	if err := db.Create(pack).Error; err != nil {
		return fmt.Errorf("create pack: %w", err)
	}

	colors := []color.RGBA{
		{255, 205, 210, 255}, {248, 187, 208, 255}, {225, 190, 231, 255}, {197, 202, 233, 255},
		{187, 222, 251, 255}, {178, 235, 242, 255}, {200, 230, 201, 255}, {255, 224, 178, 255},
	}
	const size = 96
	for i, clr := range colors {
		png := placeholderStickerPNG(clr, size)
		key := "images/" + time.Now().Format("2006/01") + "/" + uuid.NewString() + ".png"
		if st != nil {
			if err := st.PutObject(ctx, key, "image/png", bytes.NewReader(png), int64(len(png))); err != nil {
				return fmt.Errorf("upload sticker %d: %w", i, err)
			}
		}
		hash := sha256.Sum256(png)
		sticker := &model.Sticker{
			PackID:      &pack.ID,
			ObjectKey:   key,
			Width:       size,
			Height:      size,
			ContentHash: hex.EncodeToString(hash[:]),
		}
		if err := db.Create(sticker).Error; err != nil {
			return fmt.Errorf("create sticker %d: %w", i, err)
		}
	}
	log.Printf("seeded official sticker pack %q with %d stickers", packName, len(colors))
	return nil
}

// placeholderStickerPNG 生成一枚纯色圆形 PNG（占位符表情素材）。
func placeholderStickerPNG(clr color.RGBA, size int) []byte {
	img := image.NewRGBA(image.Rect(0, 0, size, size))
	center, radius := size/2, size/2-8
	for y := 0; y < size; y++ {
		for x := 0; x < size; x++ {
			dx, dy := x-center, y-center
			if dx*dx+dy*dy <= radius*radius {
				img.Set(x, y, clr)
			}
		}
	}
	var buf bytes.Buffer
	_ = png.Encode(&buf, img) // in-memory 编码不失败
	return buf.Bytes()
}
```

`main()` 函数体内，`users, err := ensureUsers(ctx, db)` 之后追加：

```go
	st, err := storage.New(cfg.MinIO)
	if err != nil {
		log.Printf("minio unavailable, skip sticker asset upload (rows still seeded without real objects): %v", err)
		st = nil
	}
	if err := ensureOfficialStickers(ctx, db, st); err != nil {
		log.Fatalf("seed official stickers: %v", err)
	}
```

（顶部 import 再加 `"crypto/sha256"`、`"encoding/hex"`、`"errors"` 若尚未导入——`errors` 大概率已因既有 `ensureUsers` 用到而存在，检查后按需补。）

- [ ] **Step 6: 跑 seed + 编译验证**

Run: `cd server && go build ./... && go run ./cmd/seed`
Expected: 成功创建 `sticker_packs`(1) + `stickers`(8) 行；MinIO 可达时对象上传成功（可用 `mc ls` 或后续 GET /sticker-packs 端点验证，Task 4 完成后回归）。

- [ ] **Step 7: Commit**

```bash
git add server/internal/storage/minio.go server/internal/storage/minio_test.go server/cmd/seed/main.go
git commit -m "feat(server): MinIO 直传方法 + 官方表情包占位符 seed"
```

---

## Task 4: 贴纸仓储 + service + handler + 路由

**Files:**

- Create: `server/internal/repository/sticker_repo.go`
- Create: `server/internal/service/sticker_service.go`
- Create: `server/internal/handler/sticker.go`
- Modify: `server/internal/router/router.go`
- Test: `server/internal/service/sticker_service_test.go`（追加用例）

**Interfaces:**

- Consumes: `model.Sticker`/`model.StickerPack`（Task 1）
- Produces:
  - `(*StickerRepository) Add(ctx, *model.Sticker) (*model.Sticker, error)`（`FirstOrCreate` 按 `(owner_id, content_hash)`，幂等去重）
  - `(*StickerRepository) Remove(ctx, userID, id uuid.UUID) error`
  - `(*StickerRepository) ListByOwner(ctx, userID uuid.UUID) ([]model.Sticker, error)`
  - `(*StickerRepository) ListPacks(ctx) ([]model.StickerPack, error)`
  - `(*StickerRepository) ListByPackIDs(ctx, packIDs []uuid.UUID) ([]model.Sticker, error)`
  - `service.ErrInvalidObjectKey`、`service.ErrInvalidContentHash`
  - `(*StickerService) Add(ctx, userID uuid.UUID, objectKey string, width, height int, contentHash string) (*model.Sticker, error)`
  - `(*StickerService) Remove(ctx, userID, id uuid.UUID) error`
  - `(*StickerService) ListMine(ctx, userID uuid.UUID) ([]model.Sticker, error)`
  - `(*StickerService) ListPacks(ctx) ([]StickerPackWithStickers, error)`

- [ ] **Step 1: 写失败测试**

`sticker_service_test.go` 追加：

```go
func newStickerSvc(db *gorm.DB) *StickerService {
	return NewStickerService(repository.NewStickerRepository(db), zap.NewNop())
}

// TestStickerAddDedup 重复收藏同一内容（同 owner 同 hash）不新增行；不同用户各自独立。
func TestStickerAddDedup(t *testing.T) {
	db := testDB(t)
	db.AutoMigrate(&model.StickerPack{}, &model.Sticker{})
	alice := newTestUser(t, db, "甲sticker")
	bob := newTestUser(t, db, "乙sticker")
	svc := newStickerSvc(db)
	ctx := context.Background()

	s1, err := svc.Add(ctx, alice.ID, "images/2026/08/a.png", 96, 96, "hash-abc")
	if err != nil {
		t.Fatalf("first add: %v", err)
	}
	s2, err := svc.Add(ctx, alice.ID, "images/2026/08/a.png", 96, 96, "hash-abc")
	if err != nil {
		t.Fatalf("dup add: %v", err)
	}
	if s1.ID != s2.ID {
		t.Fatalf("duplicate content should return same row, got %s vs %s", s1.ID, s2.ID)
	}
	mine, err := svc.ListMine(ctx, alice.ID)
	if err != nil || len(mine) != 1 {
		t.Fatalf("want 1 sticker after dedup, got %d err=%v", len(mine), err)
	}

	// bob 收藏同 hash 内容：与 alice 互不影响（unique 是 (owner_id, hash)）
	if _, err := svc.Add(ctx, bob.ID, "images/2026/08/a.png", 96, 96, "hash-abc"); err != nil {
		t.Fatalf("bob add same hash: %v", err)
	}
	bobMine, err := svc.ListMine(ctx, bob.ID)
	if err != nil || len(bobMine) != 1 {
		t.Fatalf("bob should have 1 sticker independent of alice, got %d err=%v", len(bobMine), err)
	}
}

// TestStickerAddInvalidObjectKey 非 images/ 前缀的 object_key 被拒绝。
func TestStickerAddInvalidObjectKey(t *testing.T) {
	db := testDB(t)
	db.AutoMigrate(&model.StickerPack{}, &model.Sticker{})
	alice := newTestUser(t, db, "甲invalid")
	svc := newStickerSvc(db)
	if _, err := svc.Add(context.Background(), alice.ID, "files/2026/08/a.pdf", 10, 10, "h"); !errors.Is(err, ErrInvalidObjectKey) {
		t.Fatalf("want ErrInvalidObjectKey, got %v", err)
	}
}

// TestStickerRemoveOnlyOwner 只能删除自己的贴纸，删他人的报错。
func TestStickerRemoveOnlyOwner(t *testing.T) {
	db := testDB(t)
	db.AutoMigrate(&model.StickerPack{}, &model.Sticker{})
	alice := newTestUser(t, db, "甲remove")
	bob := newTestUser(t, db, "乙remove")
	svc := newStickerSvc(db)
	ctx := context.Background()
	s, err := svc.Add(ctx, alice.ID, "images/2026/08/b.png", 96, 96, "hash-b")
	if err != nil {
		t.Fatalf("add: %v", err)
	}
	if err := svc.Remove(ctx, bob.ID, s.ID); err == nil {
		t.Fatal("bob should not be able to remove alice's sticker")
	}
	if err := svc.Remove(ctx, alice.ID, s.ID); err != nil {
		t.Fatalf("alice remove own sticker: %v", err)
	}
	mine, _ := svc.ListMine(ctx, alice.ID)
	if len(mine) != 0 {
		t.Fatalf("want 0 after remove, got %d", len(mine))
	}
}

// TestStickerListPacks 官方包及其贴纸能正确列出（依赖 Task 3 的 seed 数据，若未 seed 则期望空列表不报错）。
func TestStickerListPacks(t *testing.T) {
	db := testDB(t)
	db.AutoMigrate(&model.StickerPack{}, &model.Sticker{})
	svc := newStickerSvc(db)
	packs, err := svc.ListPacks(context.Background())
	if err != nil {
		t.Fatalf("list packs: %v", err)
	}
	for _, p := range packs {
		if !p.Pack.IsOfficial {
			t.Fatalf("all seeded packs should be official, got %+v", p.Pack)
		}
	}
}
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd server && go test ./internal/service/ -run TestSticker -v`
Expected: 编译失败（`StickerService`/`StickerRepository` 未定义）。

- [ ] **Step 3: 实现仓储**

`server/internal/repository/sticker_repo.go`（新文件）：

```go
package repository

import (
	"context"

	"github.com/google/uuid"
	"github.com/yuanchat/server/internal/model"
	"gorm.io/gorm"
)

// StickerRepository 处理 stickers / sticker_packs 表。
type StickerRepository struct {
	db *gorm.DB
}

func NewStickerRepository(db *gorm.DB) *StickerRepository {
	return &StickerRepository{db: db}
}

// Add 插入贴纸，同 (owner_id, content_hash) 已存在时幂等返回原行（去重）。
func (r *StickerRepository) Add(ctx context.Context, st *model.Sticker) (*model.Sticker, error) {
	res := r.db.WithContext(ctx).
		Where(model.Sticker{OwnerID: st.OwnerID, ContentHash: st.ContentHash}).
		FirstOrCreate(st)
	return st, res.Error
}

// Remove 按 id 删除，仅限本人的贴纸（WHERE owner_id 天然阻止越权删除官方/他人贴纸）。
func (r *StickerRepository) Remove(ctx context.Context, userID, id uuid.UUID) error {
	res := r.db.WithContext(ctx).
		Where("id = ? AND owner_id = ?", id, userID).
		Delete(&model.Sticker{})
	if res.Error != nil {
		return res.Error
	}
	if res.RowsAffected == 0 {
		return gorm.ErrRecordNotFound
	}
	return nil
}

// ListByOwner 列出用户收藏的贴纸，最新在前。
func (r *StickerRepository) ListByOwner(ctx context.Context, userID uuid.UUID) ([]model.Sticker, error) {
	var rows []model.Sticker
	err := r.db.WithContext(ctx).
		Where("owner_id = ?", userID).
		Order("created_at DESC").
		Find(&rows).Error
	return rows, err
}

// ListPacks 列出所有表情包（按 sort 升序）。
func (r *StickerRepository) ListPacks(ctx context.Context) ([]model.StickerPack, error) {
	var rows []model.StickerPack
	err := r.db.WithContext(ctx).Order("sort ASC, created_at ASC").Find(&rows).Error
	return rows, err
}

// ListByPackIDs 批量列出多个表情包下的全部贴纸。
func (r *StickerRepository) ListByPackIDs(ctx context.Context, packIDs []uuid.UUID) ([]model.Sticker, error) {
	if len(packIDs) == 0 {
		return nil, nil
	}
	var rows []model.Sticker
	err := r.db.WithContext(ctx).
		Where("pack_id IN ?", packIDs).
		Order("created_at ASC").
		Find(&rows).Error
	return rows, err
}
```

- [ ] **Step 4: 实现 service**

`server/internal/service/sticker_service.go`（新文件）：

```go
package service

import (
	"context"
	"errors"
	"fmt"
	"strings"

	"github.com/google/uuid"
	"github.com/yuanchat/server/internal/model"
	"github.com/yuanchat/server/internal/repository"
	"go.uber.org/zap"
)

var (
	// ErrInvalidObjectKey 贴纸对象键不在 images/ 前缀下（越权/伪造探测）。
	ErrInvalidObjectKey = errors.New("invalid sticker object key")
	// ErrInvalidContentHash 内容哈希格式非法（非 64 位十六进制 SHA-256）。
	ErrInvalidContentHash = errors.New("invalid content hash")
)

// StickerService 表情收藏 / 表情包服务。
type StickerService struct {
	repo   *repository.StickerRepository
	logger *zap.Logger
}

func NewStickerService(repo *repository.StickerRepository, logger *zap.Logger) *StickerService {
	return &StickerService{repo: repo, logger: logger}
}

// Add 收藏一张贴纸（从已有图片消息或直传均走这里，仅需已上传对象的 key + 客户端算好的内容哈希）。
// 幂等：同一用户重复收藏同内容不新增行（repo 层 FirstOrCreate 保证）。
func (s *StickerService) Add(ctx context.Context, userID uuid.UUID, objectKey string, width, height int, contentHash string) (*model.Sticker, error) {
	if !strings.HasPrefix(objectKey, "images/") {
		return nil, ErrInvalidObjectKey
	}
	if len(contentHash) != 64 {
		return nil, ErrInvalidContentHash
	}
	sticker := &model.Sticker{
		OwnerID: &userID, ObjectKey: objectKey, Width: width, Height: height, ContentHash: contentHash,
	}
	got, err := s.repo.Add(ctx, sticker)
	if err != nil {
		return nil, fmt.Errorf("add sticker: %w", err)
	}
	return got, nil
}

// Remove 取消收藏（仅本人）。
func (s *StickerService) Remove(ctx context.Context, userID, id uuid.UUID) error {
	if err := s.repo.Remove(ctx, userID, id); err != nil {
		return fmt.Errorf("remove sticker: %w", err)
	}
	return nil
}

// ListMine 列出本人收藏。
func (s *StickerService) ListMine(ctx context.Context, userID uuid.UUID) ([]model.Sticker, error) {
	return s.repo.ListByOwner(ctx, userID)
}

// StickerPackWithStickers 表情包及其贴纸（GET /sticker-packs 响应单元）。
type StickerPackWithStickers struct {
	Pack     model.StickerPack `json:"pack"`
	Stickers []model.Sticker   `json:"stickers"`
}

// ListPacks 列出全部表情包及各自贴纸（当前仅官方包，接口设计上不排除未来第三方包）。
func (s *StickerService) ListPacks(ctx context.Context) ([]StickerPackWithStickers, error) {
	packs, err := s.repo.ListPacks(ctx)
	if err != nil {
		return nil, fmt.Errorf("list packs: %w", err)
	}
	if len(packs) == 0 {
		return []StickerPackWithStickers{}, nil
	}
	ids := make([]uuid.UUID, len(packs))
	for i, p := range packs {
		ids[i] = p.ID
	}
	all, err := s.repo.ListByPackIDs(ctx, ids)
	if err != nil {
		return nil, fmt.Errorf("list pack stickers: %w", err)
	}
	byPack := make(map[uuid.UUID][]model.Sticker, len(packs))
	for _, st := range all {
		if st.PackID != nil {
			byPack[*st.PackID] = append(byPack[*st.PackID], st)
		}
	}
	result := make([]StickerPackWithStickers, len(packs))
	for i, p := range packs {
		result[i] = StickerPackWithStickers{Pack: p, Stickers: byPack[p.ID]}
	}
	return result, nil
}
```

- [ ] **Step 5: 实现 handler**

`server/internal/handler/sticker.go`（新文件，照 `favorite.go` 结构）：

```go
package handler

import (
	"errors"
	"net/http"

	"github.com/gin-gonic/gin"
	"github.com/google/uuid"
	"github.com/yuanchat/server/internal/middleware"
	"github.com/yuanchat/server/internal/service"
	"go.uber.org/zap"
)

// StickerHandler 表情收藏 / 表情包端点。
type StickerHandler struct {
	svc    *service.StickerService
	logger *zap.Logger
}

func NewStickerHandler(svc *service.StickerService, logger *zap.Logger) *StickerHandler {
	return &StickerHandler{svc: svc, logger: logger}
}

// AddStickerBody 收藏请求体。
type AddStickerBody struct {
	ObjectKey   string `json:"object_key" binding:"required"`
	Width       int    `json:"width" binding:"required"`
	Height      int    `json:"height" binding:"required"`
	ContentHash string `json:"content_hash" binding:"required"`
}

// Add 收藏一张贴纸（POST /stickers）。
func (h *StickerHandler) Add(c *gin.Context) {
	userID, ok := middleware.GetUserID(c)
	if !ok {
		Unauthorized(c, "unauthorized")
		return
	}
	var body AddStickerBody
	if err := c.ShouldBindJSON(&body); err != nil {
		BadRequest(c, err.Error())
		return
	}
	st, err := h.svc.Add(c.Request.Context(), userID, body.ObjectKey, body.Width, body.Height, body.ContentHash)
	if err != nil {
		switch {
		case errors.Is(err, service.ErrInvalidObjectKey):
			BadRequest(c, "invalid object key")
		case errors.Is(err, service.ErrInvalidContentHash):
			BadRequest(c, "invalid content hash")
		default:
			h.logger.Error("add sticker failed", zap.Error(err))
			InternalError(c, "add sticker failed")
		}
		return
	}
	Success(c, gin.H{
		"id": st.ID, "object_key": st.ObjectKey, "width": st.Width, "height": st.Height,
	})
}

// Remove 取消收藏（DELETE /stickers/:id）。
func (h *StickerHandler) Remove(c *gin.Context) {
	userID, ok := middleware.GetUserID(c)
	if !ok {
		Unauthorized(c, "unauthorized")
		return
	}
	id, err := uuid.Parse(c.Param("id"))
	if err != nil {
		BadRequest(c, "invalid sticker id")
		return
	}
	if err := h.svc.Remove(c.Request.Context(), userID, id); err != nil {
		Error(c, http.StatusNotFound, 404, "sticker not found")
		return
	}
	Success(c, gin.H{"message": "removed"})
}

// ListMine 列出本人收藏（GET /stickers/mine）。
func (h *StickerHandler) ListMine(c *gin.Context) {
	userID, ok := middleware.GetUserID(c)
	if !ok {
		Unauthorized(c, "unauthorized")
		return
	}
	rows, err := h.svc.ListMine(c.Request.Context(), userID)
	if err != nil {
		h.logger.Error("list my stickers failed", zap.Error(err))
		InternalError(c, "list stickers failed")
		return
	}
	Success(c, gin.H{"stickers": rows})
}

// ListPacks 列出表情包及各自贴纸（GET /sticker-packs）。
func (h *StickerHandler) ListPacks(c *gin.Context) {
	packs, err := h.svc.ListPacks(c.Request.Context())
	if err != nil {
		h.logger.Error("list sticker packs failed", zap.Error(err))
		InternalError(c, "list sticker packs failed")
		return
	}
	Success(c, gin.H{"packs": packs})
}
```

- [ ] **Step 6: 路由装配**

`router.go`，在 `favH := handler.NewFavoriteHandler(favSvc, logger)` 后追加：

```go
	stickerRepo := repository.NewStickerRepository(db)
	stickerSvc := service.NewStickerService(stickerRepo, logger)
	stickerH := handler.NewStickerHandler(stickerSvc, logger)
```

`chat` 路由组内（`favH` 三条路由附近）追加：

```go
		chat.GET("/stickers/mine", stickerH.ListMine)
		chat.POST("/stickers", stickerH.Add)
		chat.DELETE("/stickers/:id", stickerH.Remove)
		chat.GET("/sticker-packs", stickerH.ListPacks)
```

- [ ] **Step 7: 跑测试**

Run: `cd server && go test ./internal/service/ -run TestSticker -v && go build ./... && go vet ./...`
Expected: 全 PASS。

- [ ] **Step 8: Commit**

```bash
git add server/internal/repository/sticker_repo.go server/internal/service/sticker_service.go server/internal/handler/sticker.go server/internal/router/router.go server/internal/service/sticker_service_test.go
git commit -m "feat(server): 表情收藏 CRUD + 表情包列表端点（hash 去重）"
```

---

## Task 5: 全量后端回归 + API 文档 + Admin 标签

**Files:**

- Modify: `docs/CHAT_API.md`
- Modify: `apps/admin/src/pages/MessageAudit.tsx`
- Modify: `packages/design-system/src/i18n/locales/{zh-CN,en-US,ja-JP,ko-KR}.json`（先补 `chat.message.sticker` 一个 key，供 Admin 立即可用；其余 H1 前端 key 留到 Task 9 集中补）

**Interfaces:**

- Consumes: 无新增（纯文档 + 标签接线）

- [ ] **Step 1: Admin 类型标签**

`apps/admin/src/pages/MessageAudit.tsx` `TYPE_KEY` 加：

```ts
const TYPE_KEY: Record<number, string> = {
  2: "chat.message.image",
  3: "chat.message.file",
  4: "chat.message.voice",
  8: "chat.message.sticker",
};
```

- [ ] **Step 2: 四语言补 `chat.message.sticker`**

在四个 locale 文件的 `chat.message.voice` 行后各加一行（保持行位对齐）：

- zh-CN: `"chat.message.sticker": "[表情]",`
- en-US: `"chat.message.sticker": "[Sticker]",`
- ja-JP: `"chat.message.sticker": "[スタンプ]",`
- ko-KR: `"chat.message.sticker": "[스티커]",`

- [ ] **Step 3: API 文档**

`docs/CHAT_API.md` 新增小节（参照现有 favorites/群管理端点的文档格式，含请求/响应示例、鉴权、错误码）：

```markdown
### 表情收藏 / 表情包

| 方法   | 路径                    | 请求体                                      | 成功响应                          |
| ------ | ----------------------- | ------------------------------------------- | --------------------------------- |
| GET    | `/api/v1/stickers/mine` | —                                           | `{stickers: [...]}`               |
| POST   | `/api/v1/stickers`      | `{object_key, width, height, content_hash}` | `{id, object_key, width, height}` |
| DELETE | `/api/v1/stickers/:id`  | —                                           | `{message: "removed"}`            |
| GET    | `/api/v1/sticker-packs` | —                                           | `{packs: [{pack, stickers}]}`     |

- `content_hash`：客户端 SHA-256（64 位十六进制），用于同用户去重（同内容重复收藏返回已有行，不新增）。
- `object_key` 须为 `images/` 前缀（复用图片直传通道所在类别）；非法前缀 400。
- 消息类型 `8`（`sticker`）：WS `message.send` 的 `content.type="sticker"`，字段 `{sticker_id, key, width, height}`；渲染无气泡裸图、固定上限尺寸、不进图片大图查看器；不进全文搜索、不进内容审核（与 image/file/voice 现状一致）。
```

WS 帧表 `message.receive` 行的 content 字段说明补一句：「贴纸帧另含 `sticker_id`」。

- [ ] **Step 4: 全量后端回归**

Run: `cd server && go build ./... && go vet ./... && go test -race ./internal/... && node ../scripts/check-i18n.mjs`
Expected: 全 PASS。

- [ ] **Step 5: Commit**

```bash
git add docs/CHAT_API.md apps/admin/src/pages/MessageAudit.tsx packages/design-system/src/i18n/locales/
git commit -m "docs: 表情收藏/表情包 API 文档 + Admin 消息类型标签"
```

---

## Task 6: 前端 shared 层——API + DTO + 帧类型

**Files:**

- Create: `packages/shared/src/api/stickers.ts`
- Modify: `packages/shared/src/api/files.ts`
- Modify: `packages/shared/src/api/chat.ts`
- Modify: `packages/shared/src/ws/chatSocket.ts`
- Modify: `packages/shared/src/index.ts`
- Test: `packages/shared/src/__tests__/stickers.test.ts`（新建）、`packages/shared/src/__tests__/chatApi.test.ts`（追加）

**Interfaces:**

- Produces:
  - `StickerItem{id, object_key, width, height}`；`StickerPackItem{pack: {id, name, cover_url, is_official, sort}, stickers: StickerItem[]}`
  - `addSticker(objectKey, width, height, contentHash): Promise<StickerItem>`
  - `removeSticker(id): Promise<void>`
  - `listMyStickers(): Promise<StickerItem[]>`
  - `listStickerPacks(): Promise<StickerPackItem[]>`
  - `hashBlob(blob: Blob): Promise<string>`（`files.ts`，SHA-256 十六进制）
  - `parseStickerContent(content: string): {stickerId?: string; key?: string; width: number; height: number}`（`chat.ts`）
  - `mapMessage` 的 `kindMap` 加 `8: "sticker"`，返回对象加 `sticker` 字段
  - `ServerFrames["message.receive"].content` 加 `sticker_id?: string`

- [ ] **Step 1: 写失败测试**

`packages/shared/src/__tests__/stickers.test.ts`（新建，照 `filesApi.test.ts` 的 `vi.stubGlobal("fetch", ...)` 桩风格——**本仓库 `packages/shared` 的 API 单测统一用这个模式，不用 MSW**（MSW 只用于 `mocks/handlers.ts` 供应用层浏览器 mock 模式）：

```ts
/**
 * api/stickers 单元测试：桩 fetch，断言请求方法/路径/请求体与响应映射
 */
import { describe, it, expect, vi, beforeEach } from "vitest";
import { addSticker, removeSticker, listMyStickers, listStickerPacks } from "../api/stickers";

/** 桩：一次 apiGet/apiPost/apiDelete 信封响应（照 filesApi.test.ts 的 mockApiOnce 惯例） */
function mockApiOnce(data: unknown) {
  vi.stubGlobal(
    "fetch",
    vi.fn().mockResolvedValue({ json: () => Promise.resolve({ code: 0, message: "ok", data }) }),
  );
}

describe("stickers api", () => {
  beforeEach(() => vi.unstubAllGlobals());

  it("addSticker posts object_key/width/height/content_hash", async () => {
    mockApiOnce({ id: "s1", object_key: "images/2026/08/a.png", width: 96, height: 96 });
    const result = await addSticker("images/2026/08/a.png", 96, 96, "hash123");

    const call = (fetch as ReturnType<typeof vi.fn>).mock.calls[0];
    expect(call[0]).toContain("/api/v1/stickers");
    expect(call[1].method).toBe("POST");
    expect(JSON.parse(call[1].body)).toEqual({
      object_key: "images/2026/08/a.png",
      width: 96,
      height: 96,
      content_hash: "hash123",
    });
    expect(result).toEqual({ id: "s1", object_key: "images/2026/08/a.png", width: 96, height: 96 });
  });

  it("removeSticker sends DELETE to /stickers/:id", async () => {
    mockApiOnce({ message: "removed" });
    await removeSticker("s1");
    const call = (fetch as ReturnType<typeof vi.fn>).mock.calls[0];
    expect(call[0]).toContain("/api/v1/stickers/s1");
    expect(call[1].method).toBe("DELETE");
  });

  it("listMyStickers maps stickers array", async () => {
    mockApiOnce({ stickers: [{ id: "s1", object_key: "images/x.png", width: 10, height: 10 }] });
    const result = await listMyStickers();
    expect(result).toEqual([{ id: "s1", object_key: "images/x.png", width: 10, height: 10 }]);
  });

  it("listStickerPacks maps packs array", async () => {
    mockApiOnce({
      packs: [
        {
          pack: { id: "p1", name: "默认表情", is_official: true, sort: 0 },
          stickers: [{ id: "s1", object_key: "images/x.png", width: 96, height: 96 }],
        },
      ],
    });
    const result = await listStickerPacks();
    expect(result).toHaveLength(1);
    expect(result[0].pack.name).toBe("默认表情");
    expect(result[0].stickers).toHaveLength(1);
  });
});
```

`chatApi.test.ts` 追加（照现有 `mapMessage` 用例模式，纯函数无需 fetch 桩）：

```ts
describe("mapMessage sticker", () => {
  it("maps message_type 8 to kind sticker with parsed content", () => {
    const dto: MessageDTO = {
      id: "m1",
      conversation_id: "c1",
      sender_id: "u1",
      seq: 1,
      message_type: 8,
      content: JSON.stringify({
        sticker_id: "s1",
        key: "images/2026/08/a.png",
        width: 96,
        height: 96,
      }),
      status: 1,
      created_at: "2026-08-09T10:00:00+08:00",
      sender_nickname: "Alice",
    };
    const msg = mapMessage(dto, "u2");
    expect(msg.kind).toBe("sticker");
    expect(msg.sticker).toEqual({
      stickerId: "s1",
      key: "images/2026/08/a.png",
      width: 96,
      height: 96,
    });
  });
});
```

`packages/shared/src/api/files.ts` 关联的 hash 测试可放同一 `stickers.test.ts` 或独立文件——为保持职责聚焦，新建 `packages/shared/src/__tests__/files.test.ts` 若不存在则追加到已有文件；追加用例：

```ts
describe("hashBlob", () => {
  it("computes SHA-256 hex digest matching the known 'abc' vector", async () => {
    // "abc" 的 SHA-256 是公开已知向量（可用 `printf 'abc' | sha256sum` 复核），
    // 断言具体值而非仅"跑通不报错"，确保实现真的算对而不只是返回了某个字符串
    const blob = new Blob(["abc"]);
    const hex = await hashBlob(blob);
    expect(hex).toBe("ba7816bf8f01cfea414140de5dae2223b00361a396177a9cb410ff61f20015ad");
  });
});
```

（注：`packages/shared` 的 vitest environment 是 `node`，Node 22 原生提供全局 `crypto.subtle`（已实测 `typeof globalThis.crypto.subtle === "object"`），测试无需 stub，实现代码也无需兼容分支。）

- [ ] **Step 2: 跑测试确认失败**

Run: `pnpm --filter @yuanchat/shared test -- stickers`
Expected: FAIL（模块不存在）。

- [ ] **Step 3: 实现 stickers.ts**

`packages/shared/src/api/stickers.ts`（新文件，照 `favorites.ts` 风格）：

```ts
import { apiDelete, apiGet, apiPost } from "./client";

/** 单张贴纸（用户收藏或表情包内） */
export interface StickerItem {
  id: string;
  object_key: string;
  width: number;
  height: number;
}

/** 表情包元信息 */
export interface StickerPackInfo {
  id: string;
  name: string;
  cover_url?: string | null;
  is_official: boolean;
  sort: number;
}

/** 表情包 + 其贴纸列表 */
export interface StickerPackItem {
  pack: StickerPackInfo;
  stickers: StickerItem[];
}

/** 收藏一张贴纸（object_key 须来自已上传的 images/ 对象；content_hash 为客户端 SHA-256）。 */
export async function addSticker(
  objectKey: string,
  width: number,
  height: number,
  contentHash: string,
): Promise<StickerItem> {
  return apiPost<StickerItem>("/api/v1/stickers", {
    object_key: objectKey,
    width,
    height,
    content_hash: contentHash,
  });
}

/** 取消收藏。 */
export async function removeSticker(id: string): Promise<void> {
  await apiDelete<{ message: string }>("/api/v1/stickers/" + id);
}

/** 列出本人收藏的全部贴纸。 */
export async function listMyStickers(): Promise<StickerItem[]> {
  const data = await apiGet<{ stickers: StickerItem[] }>("/api/v1/stickers/mine");
  return data.stickers || [];
}

/** 列出全部表情包（当前仅官方包）。 */
export async function listStickerPacks(): Promise<StickerPackItem[]> {
  const data = await apiGet<{ packs: StickerPackItem[] }>("/api/v1/sticker-packs");
  return data.packs || [];
}
```

- [ ] **Step 4: hashBlob**

`packages/shared/src/api/files.ts` 末尾追加：

```ts
/** Blob 内容 SHA-256 摘要（十六进制小写），用于贴纸收藏去重。Chrome 74+ 原生 crypto.subtle 支持。 */
export async function hashBlob(blob: Blob): Promise<string> {
  const buf = await blob.arrayBuffer();
  const digest = await crypto.subtle.digest("SHA-256", buf);
  return Array.from(new Uint8Array(digest))
    .map((b) => b.toString(16).padStart(2, "0"))
    .join("");
}
```

- [ ] **Step 5: chat.ts 解析 + 映射**

`chat.ts` 仿 `parseImageContent` 追加：

```ts
/** content JSON → 贴纸载荷（sticker_id + key + 宽高） */
export function parseStickerContent(content: string): {
  stickerId?: string;
  key?: string;
  width: number;
  height: number;
} {
  try {
    const parsed = JSON.parse(content) as {
      sticker_id?: string;
      key?: string;
      width?: number;
      height?: number;
    };
    return {
      stickerId: typeof parsed.sticker_id === "string" ? parsed.sticker_id : undefined,
      key: typeof parsed.key === "string" ? parsed.key : undefined,
      width: typeof parsed.width === "number" ? parsed.width : 0,
      height: typeof parsed.height === "number" ? parsed.height : 0,
    };
  } catch {
    return { width: 0, height: 0 };
  }
}
```

`mapMessage` 的 `kindMap` 加 `8: "sticker"`；`isImage`/`isFile`/`isVoice` 平级加 `const isSticker = dto.message_type === 8;`；返回对象加：

```ts
    sticker: isSticker ? parseStickerContent(dto.content) : undefined,
```

- [ ] **Step 6: WS 帧类型**

`chatSocket.ts` `ServerFrames["message.receive"].content` 加（`duration` 行后）：

```ts
      sticker_id?: string;
```

- [ ] **Step 7: barrel 导出**

`packages/shared/src/index.ts` 加：

```ts
export * from "./api/stickers";
```

- [ ] **Step 8: 跑测试**

Run: `pnpm --filter @yuanchat/shared test`
Expected: 全 PASS（含 hashBlob 已知向量断言——若你在 Step 1 故意写错的那一位断言失败，按注释修正为正确值后重跑）。

- [ ] **Step 9: Commit**

```bash
git add packages/shared/src/api/stickers.ts packages/shared/src/api/files.ts packages/shared/src/api/chat.ts packages/shared/src/ws/chatSocket.ts packages/shared/src/index.ts packages/shared/src/__tests__/
git commit -m "feat(shared): 贴纸 API 封装 + hashBlob + DTO 解析 + WS 帧字段"
```

---

## Task 7: messageStore.sendSticker + WS 接收分支

**Files:**

- Modify: `packages/shared/src/store/messageStore.ts`
- Modify: `packages/shared/src/hooks/useChatBootstrap.ts`
- Test: `packages/shared/src/__tests__/messageStore.test.ts`（追加）

**Interfaces:**

- Consumes: `parseStickerContent`（Task 6）
- Produces: `ChatMessageKind` 加 `"sticker"`；`ChatMessage.sticker?: {width, height, key?, stickerId?}`；`useMessageStore().sendSticker(conversationId, sticker: {id, objectKey, width, height}): void`

- [ ] **Step 1: 写失败测试**

`messageStore.test.ts` 追加：

```ts
describe("sendSticker", () => {
  it("optimistically inserts a sticker message with sending status", () => {
    useMessageStore.setState({ messagesByConv: {}, hasMoreByConv: {} });
    useMessageStore.getState().sendSticker("c1", {
      id: "s1",
      objectKey: "images/2026/08/a.png",
      width: 96,
      height: 96,
    });
    const msgs = useMessageStore.getState().messagesByConv["c1"];
    expect(msgs).toHaveLength(1);
    expect(msgs[0].kind).toBe("sticker");
    expect(msgs[0].isSelf).toBe(true);
    expect(msgs[0].status).toBe("sending");
    expect(msgs[0].sticker).toEqual({ key: "images/2026/08/a.png", width: 96, height: 96 });
  });
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `pnpm --filter @yuanchat/shared test -- messageStore`
Expected: FAIL（`sendSticker` 不存在）。

- [ ] **Step 3: 类型 + action 实现**

`messageStore.ts` `ChatMessageKind` 改为：

```ts
export type ChatMessageKind = "text" | "image" | "file" | "voice" | "system" | "sticker";
```

`ChatMessage` 接口在 `voice?: VoicePayload;` 后加：

```ts
  /** 贴纸消息载荷：已上传对象，固定尺寸展示，不进大图查看器、不可编辑 */
  sticker?: { width: number; height: number; key?: string; stickerId?: string };
```

`MessageState` 接口 `sendVoice` 后加：

```ts
  /** 发送一枚贴纸：对象已在存储中（收藏/官方包），跳过压缩/上传，直接乐观插入 + WS 帧 */
  sendSticker: (conversationId: string, sticker: { id: string; objectKey: string; width: number; height: number }) => void;
```

实现（放在 `sendVoice` 之后，`retrySend` 之前）：

```ts
  sendSticker: (conversationId, sticker) => {
    const clientMsgId = newClientMsgId();
    const msg: ChatMessage = {
      id: clientMsgId,
      conversationId,
      kind: "sticker",
      isSelf: true,
      sticker: { key: sticker.objectKey, width: sticker.width, height: sticker.height },
      time: now(),
      dateKey: dateKeyOf(new Date()),
      createdAtMs: Date.now(),
      status: "sending",
      clientMsgId,
    };
    set((s) => ({
      messagesByConv: {
        ...s.messagesByConv,
        [conversationId]: [...(s.messagesByConv[conversationId] ?? []), msg],
      },
    }));

    if (mockMode) {
      setTimeout(() => get().setStatus(conversationId, clientMsgId, "sent"), 400);
      return;
    }

    dispatchStickerSend(conversationId, sticker, clientMsgId, get);
  },
```

模块级函数（照 `dispatchImageSend` 位置追加）：

```ts
/** 贴纸发送：对象已存在于存储，无需压缩/上传步骤，直接发 WS sticker 帧。 */
function dispatchStickerSend(
  conversationId: string,
  sticker: { id: string; objectKey: string; width: number; height: number },
  clientMsgId: string,
  get: () => MessageState,
) {
  chatSocket.send("message.send", {
    conversation_id: conversationId,
    content: {
      type: "sticker",
      sticker_id: sticker.id,
      key: sticker.objectKey,
      width: sticker.width,
      height: sticker.height,
    },
    client_msg_id: clientMsgId,
  });
  armAckTimeout(conversationId, clientMsgId, get);
}
```

- [ ] **Step 4: WS 接收分支**

`useChatBootstrap.ts` `message.receive` handler，`isVoice` 行后加：

```ts
const isSticker = p.content.type === "sticker";
```

`kind` 判断链插入（`isSystem ? "system" : isImage ? "image" : isFile ? "file" : isVoice ? "voice" : "text"` 改为）：

```ts
const kind: ChatMessage["kind"] = isSystem
  ? "system"
  : isImage
    ? "image"
    : isFile
      ? "file"
      : isVoice
        ? "voice"
        : isSticker
          ? "sticker"
          : "text";
```

`msg` 对象 `voice: isVoice ? {...} : undefined,` 后加：

```ts
        sticker: isSticker
          ? { stickerId: p.content.sticker_id, key: p.content.key, width: p.content.width ?? 0, height: p.content.height ?? 0 }
          : undefined,
```

列表预览 `body` 三元链加一支：

```ts
const body = isImage
  ? i18n.t("chat.message.image")
  : isFile
    ? i18n.t("chat.message.file")
    : isVoice
      ? i18n.t("chat.message.voice")
      : isSticker
        ? i18n.t("chat.message.sticker")
        : (p.content.text ?? "");
```

- [ ] **Step 5: 跑测试**

Run: `pnpm --filter @yuanchat/shared test`
Expected: 全 PASS。

- [ ] **Step 6: Commit**

```bash
git add packages/shared/src/store/messageStore.ts packages/shared/src/hooks/useChatBootstrap.ts packages/shared/src/__tests__/messageStore.test.ts
git commit -m "feat(shared): sendSticker 乐观发送 + WS 接收贴纸分支"
```

---

## Task 8: StickerImage 组件 + MessageBubble 渲染/菜单 + ChatWindow 接线

**Files:**

- Create: `packages/ui/src/StickerImage.tsx`
- Modify: `packages/ui/src/MessageBubble.tsx`
- Modify: `packages/ui/src/ChatWindow.tsx`
- Modify: `packages/ui/src/index.ts`
- Test: `packages/ui/src/__tests__/MessageBubble.test.tsx`（追加）

**Interfaces:**

- Consumes: `ChatMessage.sticker`（Task 7）；`getDownloadUrl`（现有 `files.ts`）；`hashBlob`（Task 6）；`addSticker`（Task 6）
- Produces: `StickerImage({sticker}) `组件；`MessageBubble` 新增 `onAddSticker?: () => void` prop

- [ ] **Step 1: 写失败测试**

`MessageBubble.test.tsx` 追加（照现有 `onFavorite` 菜单项测试模式）：

```tsx
describe("sticker rendering and add-to-favorites menu item", () => {
  it("renders sticker without bubble padding/background", () => {
    const msg: ChatMessage = {
      id: "m1",
      conversationId: "c1",
      kind: "sticker",
      isSelf: false,
      sticker: { key: "images/x.png", width: 96, height: 96 },
      time: "10:00",
      seq: 1,
    };
    const { container } = render(<MessageBubble msg={msg} />);
    // 贴纸容器不应带 msg-bubble-self/msg-bubble-peer 背景类
    const bubbleDiv = container.querySelector(".msg-bubble-peer, .msg-bubble-self");
    expect(bubbleDiv).toBeNull();
  });

  it("shows add-to-sticker menu item only for image messages with onAddSticker", () => {
    const msg: ChatMessage = {
      id: "m1",
      conversationId: "c1",
      kind: "image",
      isSelf: false,
      image: { key: "images/x.png", width: 200, height: 200 },
      time: "10:00",
      seq: 1,
    };
    const onAddSticker = vi.fn();
    render(<MessageBubble msg={msg} onAddSticker={onAddSticker} />);
    fireEvent.contextMenu(screen.getByRole("img", { hidden: true }) ?? document.body);
    // 若上面的定位选择器与实际 DOM 不符，改用气泡容器 data-testid 或已有的右键触发方式（参照文件内其他菜单用例的定位手法）
    const item = screen.queryByRole("menuitem", { name: /add.*sticker|添加到表情/i });
    expect(item).toBeTruthy();
    if (item) {
      fireEvent.click(item);
      expect(onAddSticker).toHaveBeenCalled();
    }
  });

  it("sticker messages have no onClick lightbox handler (excluded from image viewer flow)", () => {
    const msg: ChatMessage = {
      id: "m1",
      conversationId: "c1",
      kind: "sticker",
      isSelf: false,
      sticker: { key: "images/x.png", width: 96, height: 96 },
      time: "10:00",
      seq: 1,
    };
    const onImageClick = vi.fn();
    render(<MessageBubble msg={msg} onImageClick={onImageClick} />);
    // StickerImage 不接收/不调用 onImageClick——用查找不到可点击 img[cursor-zoom-in] 的方式间接验证，
    // 或若 StickerImage 渲染的 img 无 onClick，直接 fireEvent.click 后断言未调用
    const img = screen.queryByRole("img");
    if (img) fireEvent.click(img);
    expect(onImageClick).not.toHaveBeenCalled();
  });
});
```

（第二个用例的 DOM 定位手法请实现者对照本文件内其它菜单项用例（如 `onFavorite` 的既有测试，若存在）的实际选择器写法调整，确保定位到贴纸消息触发右键的正确容器；断言目标——"贴纸菜单项存在且点击回调触发"——不得因定位调整而弱化。）

- [ ] **Step 2: 跑测试确认失败**

Run: `pnpm --filter @yuanchat/ui test -- MessageBubble`
Expected: FAIL（贴纸分支/菜单项不存在）。

- [ ] **Step 3: StickerImage 组件**

`packages/ui/src/StickerImage.tsx`（新文件，简化版 `MessageImage`：固定小尺寸、无缩放光标、无点击、不接 lightbox）：

```tsx
/**
 * StickerImage 组件 — 贴纸消息渲染（裸图，无气泡背景/内边距）
 *
 * @description
 * 与 MessageImage 的关键差异：
 * - 固定展示上限尺寸（更小，符合贴纸视觉习惯），不放大小图
 * - 不接收 onOpen：贴纸天然不进图片大图查看器浏览流
 * - 无 cursor-zoom-in 光标、无点击行为
 *
 * @param sticker - 贴纸载荷（width/height 像素尺寸、对象 key）
 */
import { useEffect, useState } from "react";
import { getDownloadUrl } from "@yuanchat/shared";

/** 贴纸展示盒最长边（比图片消息的 280px 更小，符合贴纸尺寸习惯） */
const STICKER_MAX_EDGE = 112;

function displayBox(w: number, h: number): { width: number; height: number } {
  if (!w || !h) return { width: STICKER_MAX_EDGE, height: STICKER_MAX_EDGE };
  const scale = Math.min(1, STICKER_MAX_EDGE / Math.max(w, h));
  return { width: Math.round(w * scale), height: Math.round(h * scale) };
}

export function StickerImage({
  sticker,
}: {
  sticker: { width: number; height: number; key?: string };
}) {
  const box = displayBox(sticker.width, sticker.height);
  const [url, setUrl] = useState<string | null>(null);

  useEffect(() => {
    if (!sticker.key) return;
    let alive = true;
    getDownloadUrl(sticker.key)
      .then((u) => {
        if (alive) setUrl(u);
      })
      .catch(() => {
        /* 签名失败：保持占位，静默——贴纸非核心内容，不打扰用户 */
      });
    return () => {
      alive = false;
    };
  }, [sticker.key]);

  return (
    <div style={box} className="relative">
      {url && (
        <img
          src={url}
          alt=""
          width={box.width}
          height={box.height}
          className="block h-full w-full object-contain"
        />
      )}
    </div>
  );
}
```

- [ ] **Step 4: MessageBubble 渲染分支 + 菜单项**

导入区加 `StickerImage`；`lucide-react` import 加 `Smile`（表情图标）。气泡容器 class 逻辑（`msg.kind === "image" ? "p-1.5" : "px-3.5 py-2.5"` 一行）改为：

```tsx
              msg.kind === "image" ? "p-1.5" : msg.kind === "sticker" ? "p-0" : "px-3.5 py-2.5",
              isSelf ? "msg-bubble-self rounded-br-md" : "msg-bubble-peer rounded-bl-md",
```

改为按贴纸跳过气泡底色/圆角类（贴纸是裸图，不应有气泡背景）：

```tsx
          <div
            className={cn(
              "relative w-fit max-w-full rounded-2xl break-words select-text",
              msg.kind === "image" && "p-1.5",
              msg.kind === "sticker" && "rounded-none",
              msg.kind !== "image" && msg.kind !== "sticker" && "px-3.5 py-2.5",
              msg.kind !== "sticker" && (isSelf ? "msg-bubble-self rounded-br-md" : "msg-bubble-peer rounded-bl-md"),
            )}
```

（`cn` 已支持传入 `false`/`undefined` 被忽略，照仓库既有 `cn` 工具用法。）

`{msg.kind === "image" && msg.image && (...)}` 后加：

```tsx
{
  msg.kind === "sticker" && msg.sticker && <StickerImage sticker={msg.sticker} />;
}
```

（注意：不传 `onOpen`/`onImageClick`——这正是排除贴纸进 lightbox 浏览流的关键点，`StickerImage` 组件本身也不接收该 prop。）

props 解构与类型定义处（`onFavorite` 附近）加 `onAddSticker`：

```tsx
  onFavorite,
  onAddSticker,
  onReport,
}: {
  ...
  onFavorite?: () => void;
  onAddSticker?: () => void;
  onReport?: () => void;
}) {
```

两处菜单资格判断行（162、169 附近的 `if (!withinWindow && !canCopy && ... && !onFavorite) return;` 和同构行）加 `&& !onAddSticker`。

`handleFavorite` 函数后加：

```tsx
const handleAddSticker = () => {
  setMenuOpen(false);
  onAddSticker?.();
};
```

菜单 JSX，`{onFavorite && (...)}` 块后追加：

```tsx
{
  onAddSticker && msg.kind === "image" && (
    <button
      role="menuitem"
      onClick={handleAddSticker}
      className="text-body-md text-on-surface hover:bg-surface-container-highest flex w-full items-center gap-2 px-3 py-2 text-left"
    >
      <Smile size={15} /> {t("chat.message.addSticker")}
    </button>
  );
}
```

- [ ] **Step 5: ChatWindow 接线**

`ChatWindow.tsx`，`onFavorite={...}` 属性块后追加 `onAddSticker`（同构的条件表达式风格，仅对图片消息生效）：

```tsx
                      onAddSticker={
                        !msg.recalled && msg.kind === "image" && !!msg.seq && !!msg.image?.key
                          ? () => {
                              void handleAddSticker(msg.image!.key!, msg.image!.width, msg.image!.height);
                            }
                          : undefined
                      }
```

在组件内（`handleForward` 附近）新增处理函数：

```tsx
/**
 * 从图片消息收藏为贴纸：取图字节算 SHA-256（去重用）→ POST /stickers。
 * 下载走既有 getDownloadUrl（进程内缓存，重复点击不重复签名）。
 */
const handleAddSticker = async (key: string, width: number, height: number) => {
  try {
    const url = await getDownloadUrl(key);
    const blob = await fetch(url).then((r) => r.blob());
    const hash = await hashBlob(blob);
    await addSticker(key, width, height, hash);
    showToast("info", t("sticker.added"));
  } catch {
    showToast("error", t("sticker.addFailed"));
  }
};
```

文件顶部 import 追加 `addSticker`, `hashBlob`, `getDownloadUrl`（自 `@yuanchat/shared` 的既有解构 import 块，该块已含 `showToast`/`addFavorite` 等，按字母序插入）。

- [ ] **Step 6: 导出**

`packages/ui/src/index.ts` 加：

```ts
export { StickerImage } from "./StickerImage";
```

- [ ] **Step 7: 跑测试**

Run: `pnpm --filter @yuanchat/ui test -- MessageBubble`
Expected: PASS（若定位选择器需按 Step 1 备注调整，调整后仍需全部通过）。

- [ ] **Step 8: Commit**

```bash
git add packages/ui/src/StickerImage.tsx packages/ui/src/MessageBubble.tsx packages/ui/src/ChatWindow.tsx packages/ui/src/index.ts packages/ui/src/__tests__/MessageBubble.test.tsx
git commit -m "feat(ui): 贴纸渲染（裸图不进气泡/不进大图查看器）+「添加到表情」菜单"
```

---

## Task 9: EmojiPicker 收藏/官方包 tab + Composer 接线

**Files:**

- Modify: `packages/ui/src/EmojiPicker.tsx`
- Modify: `packages/ui/src/Composer.tsx`
- Test: `packages/ui/src/__tests__/EmojiPicker.test.tsx`（新建或追加）

**Interfaces:**

- Consumes: `listMyStickers`/`listStickerPacks`（Task 6）；`sendSticker`（Task 7）；`removeSticker`（Task 6）
- Produces: `EmojiPicker` 新增 `onPickSticker?: (sticker: {id, objectKey, width, height}) => void` prop，「收藏」「官方包」两个 tab

- [ ] **Step 1: 写失败测试**

`EmojiPicker.test.tsx`（若不存在则新建；若已存在则追加到现有 `describe("EmojiPicker", ...)` 同文件——**jsdom 默认 locale 为 en-US，tab 文案断言用英文标签**，照文件内既有用例风格用精确字符串而非正则）：

```tsx
import { describe, it, expect, vi, beforeEach } from "vitest";
import { render, screen, fireEvent, waitFor } from "@testing-library/react";
import { EmojiPicker } from "../EmojiPicker";

vi.mock("@yuanchat/shared", async (importOriginal) => {
  const mod = await importOriginal<typeof import("@yuanchat/shared")>();
  return {
    ...mod,
    listMyStickers: vi.fn(async () => [
      { id: "s1", object_key: "images/2026/08/a.png", width: 96, height: 96 },
    ]),
    listStickerPacks: vi.fn(async () => [
      {
        pack: { id: "p1", name: "默认表情", is_official: true, sort: 0 },
        stickers: [{ id: "s2", object_key: "images/2026/08/b.png", width: 96, height: 96 }],
      },
    ]),
    removeSticker: vi.fn(async () => {}),
  };
});

beforeEach(() => {
  vi.clearAllMocks();
  localStorage.clear();
});

describe("EmojiPicker sticker tabs", () => {
  it("shows Favorites and Official tabs when onPickSticker is provided, lists stickers within them", async () => {
    render(<EmojiPicker onPick={vi.fn()} onClose={vi.fn()} onPickSticker={vi.fn()} />);
    fireEvent.click(screen.getByRole("tab", { name: "Favorites" }));
    await waitFor(() => expect(screen.getAllByRole("img").length).toBeGreaterThan(0));

    fireEvent.click(screen.getByRole("tab", { name: "Official" }));
    await waitFor(() => expect(screen.getAllByRole("img").length).toBeGreaterThan(0));
  });

  it("hides Favorites/Official tabs when onPickSticker is not provided (existing emoji-only usage unaffected)", () => {
    render(<EmojiPicker onPick={vi.fn()} onClose={vi.fn()} />);
    expect(screen.queryByRole("tab", { name: "Favorites" })).toBeNull();
    expect(screen.queryByRole("tab", { name: "Official" })).toBeNull();
  });

  it("clicking a sticker in Favorites calls onPickSticker with objectKey/width/height", async () => {
    const onPickSticker = vi.fn();
    render(<EmojiPicker onPick={vi.fn()} onClose={vi.fn()} onPickSticker={onPickSticker} />);
    fireEvent.click(screen.getByRole("tab", { name: "Favorites" }));
    const img = await screen.findByRole("img");
    fireEvent.click(img.closest("button")!);
    expect(onPickSticker).toHaveBeenCalledWith({
      id: "s1",
      objectKey: "images/2026/08/a.png",
      width: 96,
      height: 96,
    });
  });

  it("long-pressing/right-clicking a favorite sticker offers Delete, and clicking it removes it", async () => {
    render(<EmojiPicker onPick={vi.fn()} onClose={vi.fn()} onPickSticker={vi.fn()} />);
    fireEvent.click(screen.getByRole("tab", { name: "Favorites" }));
    const img = await screen.findByRole("img");
    const btn = img.closest("button")!;
    fireEvent.contextMenu(btn);
    const removeBtn = await screen.findByRole("menuitem", { name: "Delete" });
    fireEvent.click(removeBtn);
    // removeSticker 被调用即视为交互路径通过；mock 的 listMyStickers 恒定返回同一条数据，
    // 不对刷新后的列表内容做进一步断言（那是集成层面，E2E 覆盖）
  });
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `pnpm --filter @yuanchat/ui test -- EmojiPicker`
Expected: FAIL（tab 不存在）。

- [ ] **Step 3: 实现两个新 tab**

`EmojiPicker.tsx` 改造要点：

1. imports 加 `useEffect`、`Trash2`（图标）、`listMyStickers, listStickerPacks, removeSticker` from `@yuanchat/shared`。
2. props 加 `onPickSticker?: (sticker: {id: string; objectKey: string; width: number; height: number}) => void`。
3. state 加：

```tsx
const [myStickers, setMyStickers] = useState<StickerItem[]>([]);
const [packs, setPacks] = useState<StickerPackItem[]>([]);
const [stickerMenuId, setStickerMenuId] = useState<string | null>(null);
```

（`StickerItem`/`StickerPackItem` 从 `@yuanchat/shared` import 类型。）4. `activeKey` 切到 `"favorites"` 或 `"official-<packId>"` 时惰性拉取（`useEffect` 依赖 `activeKey`）：

```tsx
useEffect(() => {
  if (activeKey === "favorites") {
    listMyStickers()
      .then(setMyStickers)
      .catch(() => setMyStickers([]));
  }
}, [activeKey]);

useEffect(() => {
  if (activeKey.startsWith("official") && packs.length === 0) {
    listStickerPacks()
      .then(setPacks)
      .catch(() => setPacks([]));
  }
}, [activeKey, packs.length]);
```

5. tab 栏（`onPickSticker` 存在时才显示这两个新 tab，与 `recent` 一样条件渲染）：

```tsx
{
  onPickSticker && (
    <CategoryTab
      label={t("sticker.tab.favorites")}
      active={activeKey === "favorites"}
      onClick={() => setActiveKey("favorites")}
    />
  );
}
{
  onPickSticker &&
    packs.map((p) => (
      <CategoryTab
        key={p.pack.id}
        label={p.pack.is_official ? t("sticker.tab.official") : p.pack.pack?.name}
        active={activeKey === "official-" + p.pack.id}
        onClick={() => setActiveKey("official-" + p.pack.id)}
      />
    ));
}
```

（若首帧 `packs` 为空导致官方包 tab 不显示——首次打开时看不到"官方"tab 才能点进去触发拉取，属于鸡生蛋问题。改为固定渲染一个"官方"tab，不依赖 `packs` 是否已拉到：）

```tsx
{
  onPickSticker && (
    <CategoryTab
      label={t("sticker.tab.official")}
      active={activeKey === "official"}
      onClick={() => setActiveKey("official")}
    />
  );
}
```

（简化为单一官方 tab 聚合全部官方包贴纸，而非每包一个 tab——当前只有 1 套种子包，多包场景留待后续迭代按需细分，避免本批过度设计。对应地 `packs.length === 0` 拉取判断改为 `activeKey === "official" && packs.length === 0`。）6. 网格渲染区，`emoji` 分支之外加贴纸分支：

```tsx
      {activeKey === "favorites" || activeKey === "official" ? (
        <div className="grid flex-1 auto-rows-min grid-cols-4 gap-1.5 overflow-y-auto p-1.5">
          {(activeKey === "favorites" ? myStickers : packs.flatMap((p) => p.stickers)).map((st) => (
            <div key={st.id} className="relative">
              <button
                type="button"
                onClick={() =>
                  onPickSticker?.({ id: st.id, objectKey: st.object_key, width: st.width, height: st.height })
                }
                onContextMenu={(e) => {
                  if (activeKey !== "favorites") return; // 官方包不可删除
                  e.preventDefault();
                  setStickerMenuId(st.id);
                }}
                className="hover:bg-surface-container-low grid aspect-square place-items-center rounded-lg p-1 transition-colors active:scale-90"
              >
                <StickerThumb objectKey={st.object_key} />
              </button>
              {stickerMenuId === st.id && (
                <div
                  role="menu"
                  onMouseDown={(e) => e.stopPropagation()}
                  className="bg-surface-container-high border-outline-variant absolute top-full left-0 z-20 mt-1 overflow-hidden rounded-lg border py-1 shadow-lg"
                >
                  <button
                    role="menuitem"
                    onClick={() => {
                      setStickerMenuId(null);
                      void removeSticker(st.id).then(() =>
                        listMyStickers().then(setMyStickers).catch(() => {}),
                      );
                    }}
                    className="text-body-md text-error hover:bg-surface-container-highest flex w-full items-center gap-2 px-3 py-2 text-left"
                  >
                    <Trash2 size={14} /> {t("sticker.remove")}
                  </button>
                </div>
              )}
            </div>
          ))}
        </div>
      ) : (
        /* 原 emoji 网格分支保留在这个 else 分支内，缩进相应调整 */
        ...
      )}
```

7. 缩略图子组件（复用 `getDownloadUrl`，不需要 `MessageImage`/`StickerImage` 那么完整的懒加载/重试机制，picker 内数量少直接签名即可）：

```tsx
function StickerThumb({ objectKey }: { objectKey: string }) {
  const [url, setUrl] = useState<string | null>(null);
  useEffect(() => {
    getDownloadUrl(objectKey)
      .then(setUrl)
      .catch(() => {});
  }, [objectKey]);
  return url ? <img src={url} alt="" className="h-full w-full object-contain" /> : null;
}
```

（`getDownloadUrl` import 自 `@yuanchat/shared`。）

点外部关闭 `stickerMenuId`：复用组件根节点已有的 `onMouseDown={(e) => e.stopPropagation()}` 是 dialog 级别的，贴纸删除小菜单需要独立的 document 点击关闭——用一个 `useEffect` 挂 `document mousedown` 监听 `stickerMenuId !== null` 时关闭（照 `MessageBubble`/`ConversationList` 既有右键菜单模式，同构写法，非本计划重复展开）。

- [ ] **Step 4: Composer 接线**

`Composer.tsx` 两处 `<EmojiPicker onPick={insertEmoji} onClose={...} />` 均加 `onPickSticker`：

```tsx
onPickSticker={(sticker) => {
  if (activeId) useMessageStore.getState().sendSticker(activeId, sticker);
  setShowEmoji(false);
}}
```

（`activeId` 变量在文件内已存在，参照 `sendImageFile` 用法。）

- [ ] **Step 5: 跑测试**

Run: `pnpm --filter @yuanchat/ui test -- EmojiPicker`
Expected: PASS。

- [ ] **Step 6: Commit**

```bash
git add packages/ui/src/EmojiPicker.tsx packages/ui/src/Composer.tsx packages/ui/src/__tests__/EmojiPicker.test.tsx
git commit -m "feat(ui): EmojiPicker 收藏/官方表情包 tab + 发送即所见 + 长按删除"
```

---

## Task 10: i18n 四语言全量补齐 + E2E + 全量门禁

**Files:**

- Modify: `packages/design-system/src/i18n/locales/{zh-CN,en-US,ja-JP,ko-KR}.json`
- Create: `apps/web/e2e/stickers.spec.ts`
- Modify: `packages/shared/src/mocks/handlers.ts`（若 E2E mock 模式需要拦截 stickers 端点）
- Modify: `packages/shared/src/mocks/demoData.ts`（demo 数据加至少一条图片消息 + 一个官方表情包，便于 E2E 定位）

**Interfaces:**

- Consumes: 前序所有 task 引用的 i18n key

- [ ] **Step 1: 补齐四语言 key**

新增 key（`chat.message.sticker` 已在 Task 5 加过，不重复）：

- `chat.message.addSticker`（图片消息菜单项）
- `sticker.tab.favorites`、`sticker.tab.official`（EmojiPicker tab）
- `sticker.added`、`sticker.addFailed`（收藏成功/失败 toast）
- `sticker.remove`（picker 内删除菜单项）

中文示例：

```json
"chat.message.addSticker": "添加到表情",
"sticker.tab.favorites": "收藏",
"sticker.tab.official": "官方",
"sticker.added": "已添加到表情",
"sticker.addFailed": "添加失败，请稍后重试",
"sticker.remove": "删除",
```

英/日/韩对应翻译，四文件行位保持对齐。

- [ ] **Step 2: i18n 校验**

Run: `node scripts/check-i18n.mjs`
Expected: `passed — 4 locales × N keys`。

- [ ] **Step 3: MSW mock（若 E2E 走 mock 模式）**

`mocks/handlers.ts` 加（若 A6/A7 已有同构 handler 可复用相同 `apiOk` helper）：

```ts
http.get(API_BASE + "/api/v1/stickers/mine", () => apiOk({ stickers: [] })),
http.get(API_BASE + "/api/v1/sticker-packs", () =>
  apiOk({
    packs: [
      {
        pack: { id: "demo-pack", name: "默认表情", is_official: true, sort: 0 },
        stickers: [{ id: "demo-sticker-1", object_key: "images/2026/08/demo.png", width: 96, height: 96 }],
      },
    ],
  }),
),
http.post(API_BASE + "/api/v1/stickers", async ({ request }) => {
  const body = (await request.json()) as { object_key: string; width: number; height: number };
  return apiOk({ id: "demo-added", object_key: body.object_key, width: body.width, height: body.height });
}),
http.delete(API_BASE + "/api/v1/stickers/:id", () => apiOk({ message: "removed" })),
```

`demoData.ts` 确认已有至少一条 `kind: "image"` 的消息（供 E2E 定位「添加到表情」菜单项），没有则补一条。

- [ ] **Step 4: E2E（playwright 浏览器实测）**

`apps/web/e2e/stickers.spec.ts`：

```ts
import { test, expect } from "@playwright/test";
import { setAuth } from "./fixtures/auth.fixture";
import { waitForMSW } from "./utils/msw";

test.beforeEach(async ({ page }) => {
  await setAuth(page);
  await waitForMSW(page);
  await page.goto("/chat");
});

test("图片消息右键添加到表情，picker 收藏 tab 可见并可发送", async ({ page }) => {
  // 打开一个含图片消息的会话（demoData 中定位，具体会话名按 demoData 实际内容调整）
  await page
    .getByRole("button", { name: /产品研发群|张伟/ })
    .first()
    .click();
  const imageMsg = page.locator('[alt=""]').first(); // 图片消息 alt 走 i18n chat.image.alt，按实际渲染定位调整
  await imageMsg.click({ button: "right" });
  await page.getByRole("menuitem", { name: "添加到表情" }).click();

  // 打开表情面板收藏 tab
  await page.getByRole("button", { name: /表情|emoji/i }).click();
  await page.getByRole("tab", { name: "收藏" }).click();
  await expect(page.getByRole("img").first()).toBeVisible();
});

test("官方表情包 tab 可见并点击贴纸发送", async ({ page }) => {
  await page
    .getByRole("button", { name: /产品研发群|张伟/ })
    .first()
    .click();
  await page.getByRole("button", { name: /表情|emoji/i }).click();
  await page.getByRole("tab", { name: "官方" }).click();
  await expect(page.getByRole("img").first()).toBeVisible();
  await page.getByRole("img").first().click();
  // 发送后消息流出现贴纸消息（无气泡背景的裸图）——用最近一条消息容器断言存在 img
  await expect(page.locator(".flex-1.overflow-y-auto img").last()).toBeVisible();
});
```

（选择器需实现者对照仓库 E2E 既有定位手法（`conversation-settings.spec.ts`/`chat-experience.spec.ts`）与实际渲染 DOM 调整，断言目标——「添加到表情」菜单可达、收藏/官方 tab 内贴纸可见、点击贴纸后消息流出现新贴纸——不得因调整而弱化。）

Run: `cd apps/web && pnpm test:e2e -- stickers`（chromium 二进制受阻则临时 `channel:"chrome"` config 跑，跑完删除临时文件，仓库 config 不改）
Expected: PASS。

- [ ] **Step 5: 全量门禁**

Run（逐条）：

```bash
pnpm test
node scripts/check-i18n.mjs
pnpm --filter @yuanchat/web build
pnpm --filter @yuanchat/desktop exec tsc --noEmit
pnpm --filter @yuanchat/admin exec tsc --noEmit
cd server && go vet ./... && go test -race ./internal/...
cd apps/web && pnpm test:e2e
```

Expected: 全绿。

- [ ] **Step 6: Commit**

```bash
git add packages/design-system/src/i18n/ apps/web/e2e/stickers.spec.ts packages/shared/src/mocks/
git commit -m "feat: H1 四语言文案 + E2E 用例 + 全量门禁验证"
```

- [ ] **Step 7: 手工验收清单（真实后端，含官方包 seed）**

```bash
cd server && make docker-up && go run ./cmd/migrate up && go run ./cmd/seed && go run ./cmd/server
pnpm --filter @yuanchat/web dev
```

1. 打开一个含图片消息的会话，右键图片 → 「添加到表情」→ 提示「已添加到表情」。
2. 打开表情面板「收藏」tab → 刚收藏的贴纸出现 → 点击 → 消息流出现贴纸样式（无气泡背景，固定小尺寸）。
3. 第二个浏览器/账号同一会话中，确认能看到同样的贴纸样式（双端渲染一致）。
4. 「官方」tab → 8 枚占位符圆形贴纸可见 → 点击发送 → 渲染同上。
5. 长按/右键收藏 tab 内某贴纸 → 删除 → 该贴纸从 picker 消失，`GET /stickers/mine` 确认已减少。
6. 重复收藏同一张图片（再次右键「添加到表情」）→ 收藏 tab 内不出现重复项（hash 去重）。
7. 贴纸消息点击不弹出大图查看器（与图片消息的点击行为对比验证）。
8. 消息搜索框搜索任意关键词，确认贴纸消息不出现在结果中（同图片消息现状）。

---

## Self-Review

**Spec 覆盖**：迁移 011 两表 ✅（Task 1）；`MessageTypeSticker=8` ✅（Task 1）；`GET /stickers/mine`/`POST /stickers`/`DELETE /stickers/:id`/`GET /sticker-packs` ✅（Task 4）；hash 去重（FirstOrCreate 复用 favorites 幂等模式）✅（Task 4）；官方包 seed ✅（Task 3）；贴纸渲染无气泡/固定尺寸/不进 lightbox ✅（Task 8）；图片气泡「添加到表情」✅（Task 8）；EmojiPicker 收藏/官方包 tab + 长按删除 ✅（Task 9）；发送即所见（乐观插入）✅（Task 7）；搜索/审核与图片一致 ✅（Task 2 说明 + Task 10 验收清单第 8 项覆盖）；三端覆盖（web/desktop/Android 共用 `packages/ui`+`packages/shared`）✅ 全程；Admin 更新 ✅（Task 5）。

**类型一致性**：`ContentPayload.StickerID`（Task 2）→ `buildContent` case（Task 2）→ 前端 `parseStickerContent`/`sticker_id` 帧字段（Task 6）→ `sendSticker`/`dispatchStickerSend`（Task 7）全程字段名 `sticker_id`/`key`/`width`/`height` 一致；`StickerService.Add` 签名（Task 4）与 `stickers.ts.addSticker`（Task 6）参数顺序一致；`StickerPackWithStickers`（Go，Task 4）与 `StickerPackItem`（TS，Task 6）字段对应。

**决策已定**：新 `MessageTypeSticker=8`；占位符圆形 PNG 官方包种子（用户已选，无需外部素材）。

**已知留待后续**（不阻塞本批，Task 10 验收清单未覆盖的边界）：直传新贴纸（非从已有图片收藏）的独立 UI 入口——后端 `POST /stickers` 已通用支持，前端本批只接线「从图片消息收藏」路径，符合路线图前端产物清单范围；多表情包场景下的分 tab 展示（当前单一「官方」tab 聚合展示，仅 1 套种子包时无感）。
