/**
 * ContactsPage — 通讯录页面（Desktop 端）
 *
 * @description
 * 双栏/栈式响应式编排收敛在 @yuanchat/ui 的 ContactsScreen 中。
 */
import { ContactsScreen } from "@yuanchat/ui";

export function ContactsPage() {
  return <ContactsScreen />;
}
