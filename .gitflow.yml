# GitFlow Configuration for YuanChat

main:
  name: main
  base: dev

dev:
  name: dev
  base: main

feature:
  prefix: feature/
  base: dev

bugfix:
  prefix: bugfix/
  base: dev

release:
  prefix: release/
  base: dev

hotfix:
  prefix: hotfix/
  base: main
