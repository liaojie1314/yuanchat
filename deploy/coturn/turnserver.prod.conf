# 生产 TURN 配置。
#
# __TURN_SECRET__ / __DOMAIN__ / __PUBLIC_IP__ 三个占位符由 deploy/install.sh 注入，
# 保持占位符原样进版本库 —— 真实密钥与 IP 不入库。

listening-port=3478
fingerprint

use-auth-secret
static-auth-secret=__TURN_SECRET__
realm=__DOMAIN__

# NAT 后必须显式告知外网 IP：否则 coturn 把内网地址写进 relay candidate，
# 客户端拿到一个自己根本连不上的地址，通话表现为「一直连接中」。
external-ip=__PUBLIC_IP__

min-port=49160
max-port=49200

# 禁止把中继指向内网：不禁的话任何持有凭据的人都能拿 TURN 当跳板扫内网
# （SSRF 式滥用），而凭据是登录用户人手一份的。
denied-peer-ip=0.0.0.0-0.255.255.255
denied-peer-ip=10.0.0.0-10.255.255.255
denied-peer-ip=127.0.0.0-127.255.255.255
denied-peer-ip=169.254.0.0-169.254.255.255
denied-peer-ip=172.16.0.0-172.31.255.255
denied-peer-ip=192.168.0.0-192.168.255.255

no-multicast-peers
no-tlsv1
no-tlsv1_1
total-quota=200
no-cli
log-file=stdout
